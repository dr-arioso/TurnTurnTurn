# Registry

The `PurposeRegistration` is the hub's internal record for each registered
Purpose. You don't construct these directly — TTT creates them via
`register_purpose()`.

## Reference

::: turnturnturn.registry.PurposeRegistration
