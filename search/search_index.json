{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"TurnTurnTurn","text":"<p>TurnTurnTurn (TTT) is a lightweight hub runtime for routing, enriching, and preserving provenance over sequential work items.</p> <p>TTT is built around a single canonical object:</p> <ul> <li>CTO \u2014 Canonical Turn Object</li> </ul> <p>TTT does not define domain semantics. It provides:</p> <ul> <li>authoritative CTO creation via <code>start_turn()</code></li> <li>hub-mediated Delta merge</li> <li>typed HubEvents</li> <li>Purpose registration and dispatch</li> <li>replayable provenance through the event stream</li> </ul> <p>The canonical example profile is <code>conversation</code>, but TTT is profile-based, not hard-coded to speaker/text semantics.</p>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>import asyncio\nfrom uuid import uuid4\nfrom turnturnturn import TTT\n\nasync def main():\n    ttt = TTT.create()\n\n    turn_id = await ttt.start_turn(\n        session_id=uuid4(),\n        content_profile=\"conversation\",\n        content={\"speaker\": \"user\", \"text\": \"hello\"},\n    )\n    print(f\"Created turn: {turn_id}\")\n\nasyncio.run(main())\n</code></pre>"},{"location":"#core-concepts","title":"Core concepts","text":"Concept Description TTT The hub runtime. Authoritative for CTO creation, Delta merge, and event emission. CTO Canonical Turn Object. The hub-authoritative work item. Purpose A registered agenda-bearing actor that receives HubEvents and may emit Deltas. Delta A purpose-proposed change, merged authoritatively by TTT. HubEvent An authoritative event emitted by TTT. The primary provenance surface."},{"location":"#status","title":"Status","text":"<p>This project is in active architectural development. Names, APIs, and module layout are still being refined. See the Architecture doc for current design direction.</p>"},{"location":"dev-guide/","title":"Developer Guide","text":""},{"location":"dev-guide/#setup","title":"Setup","text":"<pre><code>python3.12 -m venv .venv\nsource .venv/bin/activate\npip install -e .[dev]\npre-commit install\n</code></pre>"},{"location":"dev-guide/#running-tests","title":"Running tests","text":"<pre><code>pytest --cov=turnturnturn\ncoverage html          # opens htmlcov/index.html\n</code></pre>"},{"location":"dev-guide/#linting-and-formatting","title":"Linting and formatting","text":"<p>All checks run via pre-commit on every commit. To run manually:</p> <pre><code>pre-commit run --all-files\n</code></pre> <p>This covers ruff, black, isort, mypy (strict), and docstring coverage via interrogate.</p>"},{"location":"dev-guide/#docstring-coverage","title":"Docstring coverage","text":"<p>TTT uses <code>interrogate</code> to enforce docstring coverage on all public API surfaces. The threshold is set to 80% in <code>pyproject.toml</code> \u2014 new public classes and methods must be documented before they can be committed.</p> <p>Check coverage manually:</p> <pre><code>interrogate src/turnturnturn\n</code></pre> <p>Generate a coverage badge (written to <code>docs/assets/</code>):</p> <pre><code>interrogate src/turnturnturn --generate-badge docs/assets/\n</code></pre>"},{"location":"dev-guide/#docs","title":"Docs","text":"<p>The documentation site is built from two sources:</p> <ol> <li>Narrative docs \u2014 Markdown files in <code>docs/</code> (architecture, guides, this page).</li> <li>API reference \u2014 Extracted live from docstrings at build time via <code>mkdocstrings</code>.</li> </ol> <p>Serve locally:</p> <pre><code>mkdocs serve\n# \u2192 http://127.0.0.1:8000\n</code></pre> <p>Build static site:</p> <pre><code>mkdocs build   # output \u2192 site/\n</code></pre> <p>Deploy to GitHub Pages:</p> <pre><code>mkdocs gh-deploy\n</code></pre> <p>Or let CI handle it \u2014 see <code>.github/workflows/docs.yml</code>.</p>"},{"location":"dev-guide/#dependency-auditing","title":"Dependency auditing","text":"<pre><code>safety check\npip-audit\n</code></pre>"},{"location":"dev-guide/#markdown-linting","title":"Markdown linting","text":"<pre><code>markdownlint docs/\n</code></pre>"},{"location":"ollama-extension-fix/","title":"Ollama extension fix","text":""},{"location":"ollama-extension-fix/#workaround-for-vscode-ollama-017x","title":"Workaround for <code>vscode-ollama</code> 0.17.x","text":"<p>The official <code>vscode-ollama</code> extension probes the Ollama HTTP server for a <code>/version</code> endpoint when it activates. That endpoint was introduced in a later release; the current Windows build (0.17.5/0.17.6 as of March\u202f4\u202f2026) returns 404, causing the extension to display the spurious warning:</p> <pre><code>unable to verify Ollama server version...\n</code></pre> <p>The warning itself is harmless, but it also prevents the language\u2011models panel from showing the configured model.  Until the extension is updated to handle 404s gracefully you can patch it locally.</p> <ol> <li> <p>locate the installed extension in the WSL filesystem (you may be using a    remote SSH session so the path begins with <code>~/.vscode-server</code>):    <pre><code>~/.vscode-server/extensions/warm3snow.vscode-ollama-&lt;version&gt;/{dist,out}/extension.js\n</code></pre>    There are two copies of the runtime code \u2013 one under <code>dist</code> that ships on the    VS\u00a0Code marketplace and another under <code>out</code> that is actually executed by the    editor.  Be sure to edit both files (or just <code>out/extension.js</code>) when you    apply the workaround.</p> </li> <li> <p>modify <code>extension.js</code> and either replace or prepend the following snippet    near the top of the <code>activate</code> function (e.g. just after the    \"Initializing Ollama client with config\" log).  The patched code uses    <code>/version</code> and swallows any failure so 0.17\u2011series servers will no longer    trigger the warning.</p> </li> </ol> <pre><code>// version probe (workaround for 0.17.x servers)\n(function(){\n    const e = n();\n    const base = e.baseUrl.replace(/\\/+$/,'');\n    const url = `${base}/version`;\n    fetch(url, { method: 'GET' })\n        .then(r =&gt; {\n            if (!r.ok) {\n                console.log('Ollama version probe returned', r.status);\n                return;\n            }\n            return r.json();\n        })\n        .then(j =&gt; {\n            if (j &amp;&amp; j.version) {\n                console.log('Ollama server version', j.version);\n            }\n        })\n        .catch(_ =&gt; {\n            console.log('skipping Ollama version check');\n        });\n})();\n</code></pre> <ol> <li>Reload the VS\u00a0Code window (<code>Ctrl+Shift+P</code> \u2192 Reload Window) or restart the editor.  The new JavaScript will be picked up only after a reload.</li> </ol> <p>After patching and reloading you should no longer see the version warning and models will list correctly.  When the extension is updated upstream (or the server begins serving <code>/version</code>) you can remove the workaround or simply reinstall the extension to restore its original behaviour.</p> <p>Alternatively you can file an issue against the extension repository so that the upstream author can incorporate a proper fix.</p>"},{"location":"ttt_architecture_v0_15/","title":"TurnTurnTurn (TTT) \u2014 Architecture v0.15","text":""},{"location":"ttt_architecture_v0_15/#0-positioning","title":"0. Positioning","text":"<p>TurnTurnTurn (TTT) is a hub runtime built around a single canonical object \u2014 the CTO \u2014 and the principle that nothing writes to canonical state directly. TTT was extracted from real human-AI interaction research infrastructure; the abstractions reflect actual usage rather than speculative design.</p> <p>TTT is built around a single canonical object:</p> <ul> <li>CTO \u2014 Canonical Turn Object</li> </ul> <p>TTT does not define domain semantics. It provides the machinery to adopt the semantics that are important to anyone who uses it.</p> <p>The canonical example profile is <code>conversation</code>, but TTT is profile-based, not hard-coded to speaker/text semantics.</p>"},{"location":"ttt_architecture_v0_15/#1-core-nouns","title":"1. Core nouns","text":""},{"location":"ttt_architecture_v0_15/#ttt","title":"TTT","text":"<p>The public hub runtime.</p> <p>TTT is the authority for:</p> <ul> <li>creating CTOs</li> <li>merging Deltas</li> <li>emitting HubEvents</li> <li>maintaining Purpose registration</li> <li>enforcing routing and dispatch rules</li> </ul>"},{"location":"ttt_architecture_v0_15/#start_turn","title":"start_turn","text":"<p>The hub ingress method.</p> <p>A caller invokes <code>ttt.start_turn(...)</code> with a session ID, content profile, and content dict. TTT validates the content against the profile, creates a CTO, emits <code>cto_created</code>, and dispatches interested Purposes.</p>"},{"location":"ttt_architecture_v0_15/#cto","title":"CTO","text":"<p>Canonical Turn Object.</p> <p>A CTO is the authoritative, canonical work item created by TTT via <code>start_turn</code>.</p> <p>Minimal shape:</p> <ul> <li><code>turn_id</code></li> <li><code>session_id</code></li> <li><code>created_at_ms</code></li> <li><code>content_profile</code></li> <li><code>content</code></li> <li><code>observations</code></li> </ul> <p>The canonical example profile is:</p> <ul> <li><code>content_profile = \"conversation\"</code></li> <li><code>content = {\"speaker\": str, \"text\": str}</code></li> </ul> <p>Convenience attributes such as <code>speaker</code> and <code>text</code> are derived, profile-scoped accessors.</p>"},{"location":"ttt_architecture_v0_15/#purpose","title":"Purpose","text":"<p>A registered agenda-bearing actor in the TTT mesh.</p> <p>A Purpose:</p> <ul> <li>has a semantic <code>name</code></li> <li>has an instance <code>id</code></li> <li>may later receive a nullable hub-assigned <code>token</code></li> <li>subscribes to HubEvents</li> <li>may emit Deltas</li> <li>may, in future, invoke <code>start_turn</code> to submit new work items</li> </ul> <p>A Purpose is not a per-turn work parcel.</p>"},{"location":"ttt_architecture_v0_15/#delta","title":"Delta","text":"<p>A purpose-proposed change.</p> <p>A Delta does not mutate canonical state directly. TTT validates and merges Deltas into CTO observation state using deterministic, append-only rules.</p>"},{"location":"ttt_architecture_v0_15/#hubevent","title":"HubEvent","text":"<p>An authoritative event emitted by TTT.</p> <p>HubEvents record lifecycle transitions and important actions. The event stream is the primary provenance surface and remains the replay and reconstruction substrate.</p>"},{"location":"ttt_architecture_v0_15/#2-core-principles","title":"2. Core principles","text":""},{"location":"ttt_architecture_v0_15/#21-ttt-is-authoritative","title":"2.1 TTT is authoritative","text":"<p>Only TTT may create CTOs or mutate canonical CTO state.</p> <p>Purposes propose changes. TTT decides what becomes canonical.</p>"},{"location":"ttt_architecture_v0_15/#22-append-only-by-default","title":"2.2 Append-only by default","text":"<p>Canonical history is preserved.</p> <p>TTT does not silently overwrite prior contributions. Merge behavior is deterministic and history-preserving.</p>"},{"location":"ttt_architecture_v0_15/#23-provenance-is-primarily-in-the-event-stream","title":"2.3 Provenance is primarily in the event stream","text":"<p>The authoritative answer to \u201cwho contributed what, when\u201d comes from HubEvents and replayable event history, not from stuffing provenance into every in-memory field.</p>"},{"location":"ttt_architecture_v0_15/#24-profiled-core-not-conversation-only-core","title":"2.4 Profiled core, not conversation-only core","text":"<p>TTT does not assume all CTOs are speaker/text turns.</p> <p>Instead:</p> <ul> <li>CTO has <code>content_profile</code></li> <li>CTO has authoritative <code>content</code></li> <li>profiles define required content shape</li> <li><code>conversation</code> is the canonical example profile</li> </ul>"},{"location":"ttt_architecture_v0_15/#25-purpose-is-an-actor-not-a-packet","title":"2.5 Purpose is an actor, not a packet","text":"<p>A Purpose is a registered participant with an agenda.</p> <p>Per-turn dispatch, if modeled explicitly, is a runtime concern separate from the identity of the Purpose itself.</p>"},{"location":"ttt_architecture_v0_15/#26-routing-and-dependency-are-distinct","title":"2.6 Routing and dependency are distinct","text":"<p>Routing answers: who is interested?</p> <p>Dependency answers: who is eligible now?</p> <p>Subscriptions and DAG constraints are related but not identical concerns.</p>"},{"location":"ttt_architecture_v0_15/#3-minimal-lifecycle","title":"3. Minimal lifecycle","text":""},{"location":"ttt_architecture_v0_15/#31-register-purpose","title":"3.1 Register Purpose","text":"<p>Calling code registers one or more Purposes with TTT.</p> <p>Registration associates:</p> <ul> <li><code>Purpose.name</code></li> <li><code>Purpose.id</code></li> <li>nullable <code>token</code></li> <li>subscriptions</li> <li>later: optional capabilities and DAG metadata</li> </ul>"},{"location":"ttt_architecture_v0_15/#32-start-session","title":"3.2 Start session","text":"<p>TTT creates or accepts session context.</p>"},{"location":"ttt_architecture_v0_15/#33-call-start_turn","title":"3.3 Call start_turn","text":"<p>A caller invokes <code>start_turn</code> on TTT directly.</p> <p>Example mental model:</p> <pre><code>await ttt.start_turn(\n    session_id=...,\n    content_profile=\"conversation\",\n    content={\"speaker\": \"user\", \"text\": \"hello\"},\n)\n)\n</code></pre>"},{"location":"ttt_architecture_v0_15/#34-ttt-creates-cto","title":"3.4 TTT creates CTO","text":"<p>TTT validates the content against its content profile, creates the CTO, and emits:</p> <ul> <li><code>cto_created</code></li> </ul>"},{"location":"ttt_architecture_v0_15/#35-dispatch-purposes","title":"3.5 Dispatch Purposes","text":"<p>TTT routes <code>cto_created</code> to subscribed and eligible Purposes.</p>"},{"location":"ttt_architecture_v0_15/#36-purposes-emit-deltas","title":"3.6 Purposes emit Deltas","text":"<p>Purposes propose observation writes or other canonical contributions by emitting Deltas.</p>"},{"location":"ttt_architecture_v0_15/#37-ttt-merges-deltas","title":"3.7 TTT merges Deltas","text":"<p>TTT validates and merges Deltas, then emits follow-up HubEvents such as:</p> <ul> <li><code>delta_merged</code></li> <li><code>purpose_completed</code></li> </ul>"},{"location":"ttt_architecture_v0_15/#38-repeat-until-quiescent","title":"3.8 Repeat until quiescent","text":"<p>TTT continues routing and merge/dispatch cycles until no further eligible work remains.</p>"},{"location":"ttt_architecture_v0_15/#4-diagrams","title":"4. Diagrams","text":""},{"location":"ttt_architecture_v0_15/#41-system-shape","title":"4.1 System shape","text":"<pre><code>submitter / app / Purpose\n          \u2502\n          \u25bc\n    start_turn(session_id, content_profile, content, ...)\n          \u2502\n          \u25bc\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502             TTT              \u2502\n\u2502                              \u2502\n\u2502  authoritative hub runtime   \u2502\n\u2502  - validate content          \u2502\n\u2502  - create CTO                \u2502\n\u2502  - merge Deltas              \u2502\n\u2502  - emit HubEvents            \u2502\n\u2502  - enforce registry/routing  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n      \u2502                \u2502\n      \u25bc                \u25bc\n   subscriptions      DAG\n   (interest)         (eligibility)\n      \u2502                \u2502\n      \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n             \u25bc\n         Purposes\n</code></pre>"},{"location":"ttt_architecture_v0_15/#42-creation-boundary","title":"4.2 Creation boundary","text":"<pre><code>start_turn(...)  --invoked on--&gt;  TTT\n                                    \u2502\n                                    \u251c\u2500 validate profile contract\n                                    \u251c\u2500 create CTO\n                                    \u2514\u2500 emit cto_created { cto: ... }\n</code></pre>"},{"location":"ttt_architecture_v0_15/#43-canonical-event-flow","title":"4.3 Canonical event flow","text":"<pre><code>start_turn\n   \u2193\ncto_created\n   \u2193\nPurpose(s) take_turn(event)\n   \u2193\nDelta(s) proposed\n   \u2193\ndelta_merged\n   \u2193\nfollow-on Purpose dispatch\n</code></pre>"},{"location":"ttt_architecture_v0_15/#5-canonical-example-profile-conversation","title":"5. Canonical example profile: <code>conversation</code>","text":"<p>TTT is profile-based, but <code>conversation</code> remains the canonical example because it is simple, legible, and central to Adjacency.</p> <p>Required content shape:</p> <pre><code>content_profile = \"conversation\"\ncontent = {\n    \"speaker\": \"...\",\n    \"text\": \"...\",\n}\n</code></pre> <p>Convenience accessors:</p> <ul> <li><code>CTO.speaker</code></li> <li><code>CTO.text</code></li> </ul> <p>These are derived from <code>content</code> and valid only when <code>content_profile == \"conversation\"</code>.</p>"},{"location":"ttt_architecture_v0_15/#6-event-taxonomy-minimal-v015","title":"6. Event taxonomy (minimal v0.15)","text":""},{"location":"ttt_architecture_v0_15/#cto_created","title":"<code>cto_created</code>","text":"<p>A new CTO now exists and is canonical.</p> <p>Payload includes:</p> <ul> <li>full CTO</li> <li>optional submitter attribution</li> <li>schema/version metadata</li> </ul>"},{"location":"ttt_architecture_v0_15/#delta_merged","title":"<code>delta_merged</code>","text":"<p>TTT has accepted and merged a Delta into canonical state.</p>"},{"location":"ttt_architecture_v0_15/#purpose_registered","title":"<code>purpose_registered</code>","text":"<p>TTT has accepted a Purpose registration.</p>"},{"location":"ttt_architecture_v0_15/#purpose_completed","title":"<code>purpose_completed</code>","text":"<p>A Purpose has completed one unit of work associated with a CTO, if and when that concept remains useful.</p> <p>This section should stay intentionally small.</p>"},{"location":"ttt_architecture_v0_15/#7-identification-model","title":"7. Identification model","text":""},{"location":"ttt_architecture_v0_15/#purpose-identity","title":"Purpose identity","text":"<p>Each Purpose has:</p> <ul> <li><code>name</code> \u2014 semantic kind, e.g. <code>\"ca\"</code>, <code>\"embeddingizer\"</code>, <code>\"socratic\"</code></li> <li><code>id</code> \u2014 concrete instance UUID</li> <li><code>token</code> \u2014 nullable hub-assigned cryptographic token</li> </ul> <p>Many Purposes may share the same <code>name</code>. <code>id</code> distinguishes instances.</p>"},{"location":"ttt_architecture_v0_15/#cto-identity","title":"CTO identity","text":"<p><code>turn_id</code> is the canonical CTO identity key.</p>"},{"location":"ttt_architecture_v0_15/#event-identity","title":"Event identity","text":"<p>Each HubEvent has its own <code>event_id</code>.</p>"},{"location":"ttt_architecture_v0_15/#8-module-map","title":"8. Module map","text":"<p>Source of truth lives in code.</p> <p>Primary modules:</p> <ul> <li><code>hub.py</code> \u2014 TTT runtime</li> <li><code>protocols.py</code> \u2014 PurposeProtocol / TurnTakerProtocol</li> <li><code>cto.py</code> \u2014 CTO and content-profile validation</li> <li><code>events.py</code> \u2014 HubEvent and payload helpers</li> <li><code>delta.py</code> \u2014 Delta</li> <li><code>registry.py</code> \u2014 Purpose registration</li> <li><code>dag.py</code> \u2014 eligibility model</li> </ul> <p>This document is only the front door.</p>"},{"location":"ttt_architecture_v0_15/#9-non-goals-for-v015","title":"9. Non-goals for v0.15","text":"<p>TTT v0.15 does not yet attempt to fully specify:</p> <ul> <li>cross-process transport</li> <li>durable persistence layout</li> <li>auth policy beyond token seam</li> <li>profile registry mechanics beyond minimal validation</li> <li>domain semantics for observations</li> <li>final naming for <code>purpose_completed</code> event</li> </ul>"},{"location":"ttt_architecture_v0_15/#10-immediate-open-questions","title":"10. Immediate open questions","text":"<ul> <li>Decide whether <code>purpose_completed</code> survives or is replaced by a clearer event.</li> <li>Decide whether explicit per-dispatch runtime records need a named public abstraction.</li> <li>Decide how much DAG language belongs in the public doc versus in code-level docs.</li> </ul>"},{"location":"ttt_architecture_v0_15/#11-migration-notes-from-v014","title":"11. Migration notes from v0.14","text":"<p>This revision intentionally changes the public story:</p> <ul> <li>from conversational turn processing to profile-based canonical work-item routing</li> <li>from CTO fields <code>text</code> + <code>role</code> to CTO fields <code>content_profile</code> + <code>content</code></li> <li>from <code>turn_received</code> to <code>cto_created</code></li> <li>from <code>purpose_id</code> as overloaded type/instance language to <code>Purpose.name</code> + <code>Purpose.id</code></li> <li>from <code>TurnSnargle</code> + <code>submit_snargle()</code> to direct <code>start_turn()</code> invocation on the hub</li> </ul> <p>It preserves:</p> <ul> <li>hub-authoritative merge semantics</li> <li>append-only observation history</li> <li>event-stream provenance as primary record</li> <li>switch-style routing plus DAG-gated eligibility</li> </ul>"},{"location":"api/cto/","title":"CTO \u2014 Canonical Turn Object","text":"<p>A <code>CTO</code> is the hub-authoritative work item created by <code>TTT.start_turn()</code>. It is immutable once created. Purposes read CTOs and propose changes via Deltas rather than mutating them directly.</p>"},{"location":"api/cto/#content-profiles","title":"Content profiles","text":"<p>The <code>content_profile</code> field determines the required shape of <code>content</code>. The canonical example profile is <code>\"conversation\"</code>:</p> <pre><code>content_profile = \"conversation\"\ncontent = {\"speaker\": \"user\", \"text\": \"hello\"}\n</code></pre> <p>Unknown profiles are accepted in v0 but must still provide a dict-like <code>content</code>.</p>"},{"location":"api/cto/#reference","title":"Reference","text":""},{"location":"api/cto/#turnturnturn.cto.CTO","title":"<code>CTO</code>  <code>dataclass</code>","text":"<p>Canonical Turn Object.</p> <p>A CTO is the hub-authoritative, canonical work-item for one \u201cturn-like\u201d unit of processing. It is profiled: the hub stores the content under <code>content</code>, and the <code>content_profile</code> defines the required shape.</p> \"conversation\" <p>content = {\"speaker\": str, \"text\": str}</p> <p>NOTE: convenience properties (speaker/text) are derived and profile-scoped.</p> Source code in <code>src/turnturnturn/cto.py</code> <pre><code>@dataclass(frozen=True)\nclass CTO:\n    \"\"\"\n    Canonical Turn Object.\n\n    A CTO is the hub-authoritative, canonical work-item for one \u201cturn-like\u201d unit\n    of processing. It is *profiled*: the hub stores the content under `content`,\n    and the `content_profile` defines the required shape.\n\n    Canonical example profile: \"conversation\"\n      content = {\"speaker\": str, \"text\": str}\n\n    NOTE: convenience properties (speaker/text) are derived and profile-scoped.\n    \"\"\"\n\n    turn_id: UUID\n    session_id: UUID\n    created_at_ms: int\n\n    content_profile: str\n    content: dict[str, Any]\n\n    # observations are purpose-owned namespaces: {purpose_name: [obs, obs, ...]}\n    observations: dict[str, list[dict[str, Any]]] = field(default_factory=dict)\n\n    def to_dict(self) -&gt; dict[str, Any]:\n        return {\n            \"turn_id\": str(self.turn_id),\n            \"session_id\": str(self.session_id),\n            \"created_at_ms\": self.created_at_ms,\n            \"content_profile\": self.content_profile,\n            \"content\": self.content,\n            \"observations\": self.observations,\n        }\n\n    # ---- conversational conveniences ----\n\n    @property\n    def speaker(self) -&gt; str | None:\n        if self.content_profile != \"conversation\":\n            return None\n        v = self.content.get(\"speaker\")\n        return v if isinstance(v, str) else None\n\n    @property\n    def text(self) -&gt; str | None:\n        if self.content_profile != \"conversation\":\n            return None\n        v = self.content.get(\"text\")\n        return v if isinstance(v, str) else None\n</code></pre>"},{"location":"api/cto/#turnturnturn.cto.validate_content_profile","title":"<code>validate_content_profile(content_profile, content)</code>","text":"<p>Minimal v0 validation. Tighten this later into a registry of profiles.</p> <p>Raises ValueError if the content does not match the profile contract.</p> Source code in <code>src/turnturnturn/cto.py</code> <pre><code>def validate_content_profile(content_profile: str, content: Mapping[str, Any]) -&gt; None:\n    \"\"\"\n    Minimal v0 validation. Tighten this later into a registry of profiles.\n\n    Raises ValueError if the content does not match the profile contract.\n    \"\"\"\n    if content_profile == \"conversation\":\n        if not isinstance(content.get(\"speaker\"), str):\n            raise ValueError(\"conversation profile requires content['speaker']: str\")\n        if not isinstance(content.get(\"text\"), str):\n            raise ValueError(\"conversation profile requires content['text']: str\")\n        return\n\n    # v0: allow unknown profiles but require dict-like content\n    # later: require declared profile schemas in a registry\n    if not isinstance(content, Mapping):\n        raise ValueError(\"content must be a mapping\")\n</code></pre>"},{"location":"api/delta/","title":"Delta","text":"<p>A <code>Delta</code> is a purpose-proposed change submitted to the hub for authoritative merge. Purposes never mutate CTO state directly \u2014 they propose, TTT decides.</p> <p>Deltas are append-only: TTT does not silently overwrite prior contributions. The <code>patch</code> field is treated as opaque by the hub; its semantics are owned by the proposing Purpose.</p>"},{"location":"api/delta/#reference","title":"Reference","text":""},{"location":"api/delta/#turnturnturn.delta.Delta","title":"<code>Delta</code>  <code>dataclass</code>","text":"<p>A purpose-proposed change for hub merge.</p> Provenance <ul> <li>purpose_name: semantic kind (namespace owner)</li> <li>purpose_id: instance id (who said it)</li> </ul> Source code in <code>src/turnturnturn/delta.py</code> <pre><code>@dataclass(frozen=True)\nclass Delta:\n    \"\"\"\n    A purpose-proposed change for hub merge.\n\n    Provenance:\n      - purpose_name: semantic kind (namespace owner)\n      - purpose_id: instance id (who said it)\n    \"\"\"\n\n    delta_id: UUID\n    session_id: UUID\n    turn_id: UUID\n\n    purpose_name: str\n    purpose_id: UUID\n\n    # purpose-owned namespace payload (hub should treat as opaque)\n    patch: dict[str, Any]\n\n    def to_dict(self) -&gt; dict[str, Any]:\n        return {\n            \"delta_id\": str(self.delta_id),\n            \"session_id\": str(self.session_id),\n            \"turn_id\": str(self.turn_id),\n            \"purpose_name\": self.purpose_name,\n            \"purpose_id\": str(self.purpose_id),\n            \"patch\": self.patch,\n        }\n</code></pre>"},{"location":"api/events/","title":"Events","text":"<p>HubEvents are the authoritative record of what TTT has made true. The event stream is the primary provenance surface \u2014 the canonical answer to \"who contributed what, when\" comes from replaying events, not from in-memory state.</p>"},{"location":"api/events/#event-types","title":"Event types","text":"Event When emitted <code>cto_created</code> A new CTO now exists and is canonical. <code>delta_merged</code> TTT has accepted and merged a Delta. <code>purpose_registered</code> TTT has accepted a Purpose registration. <code>purpose_completed</code> A Purpose has completed a unit of work for a CTO."},{"location":"api/events/#reference","title":"Reference","text":""},{"location":"api/events/#turnturnturn.events.HubEventType","title":"<code>HubEventType</code>","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Hub-authoritative lifecycle events.</p> Naming principle <ul> <li>event_type names what the hub has made true</li> <li>avoid receiver-relative terms (\u201creceived\u201d, \u201cseen\u201d, etc.)</li> </ul> Source code in <code>src/turnturnturn/events.py</code> <pre><code>class HubEventType(str, Enum):\n    \"\"\"\n    Hub-authoritative lifecycle events.\n\n    Naming principle:\n      - event_type names what *the hub* has made true\n      - avoid receiver-relative terms (\u201creceived\u201d, \u201cseen\u201d, etc.)\n    \"\"\"\n\n    CTO_CREATED = \"cto_created\"\n    DELTA_MERGED = \"delta_merged\"\n    PURPOSE_REGISTERED = \"purpose_registered\"\n    PURPOSE_COMPLETED = \"purpose_completed\"\n</code></pre>"},{"location":"api/events/#turnturnturn.events.HubEvent","title":"<code>HubEvent</code>  <code>dataclass</code>","text":"<p>The event envelope.</p> <p><code>turn_id</code> is nullable because some events are not scoped to a CTO (e.g., purpose registration, session lifecycle).</p> Source code in <code>src/turnturnturn/events.py</code> <pre><code>@dataclass(frozen=True)\nclass HubEvent:\n    \"\"\"\n    The event envelope.\n\n    `turn_id` is nullable because some events are not scoped to a CTO\n    (e.g., purpose registration, session lifecycle).\n    \"\"\"\n\n    event_type: HubEventType\n    event_id: UUID\n    created_at_ms: int\n\n    session_id: UUID | None = None\n    turn_id: UUID | None = None\n\n    payload: dict[str, Any] | None = None\n</code></pre>"},{"location":"api/events/#turnturnturn.events.payload_cto_created","title":"<code>payload_cto_created(*, cto_dict, submitted_by_purpose_id=None, submitted_by_purpose_name=None, submitted_by_label=None)</code>","text":"Source code in <code>src/turnturnturn/events.py</code> <pre><code>def payload_cto_created(\n    *,\n    cto_dict: dict[str, Any],\n    submitted_by_purpose_id: str | None = None,\n    submitted_by_purpose_name: str | None = None,\n    submitted_by_label: str | None = None,\n) -&gt; dict[str, Any]:\n    return {\n        \"_schema\": \"cto_created\",\n        \"_v\": 1,\n        \"cto\": cto_dict,\n        \"submitted_by_purpose_id\": submitted_by_purpose_id,\n        \"submitted_by_purpose_name\": submitted_by_purpose_name,\n        \"submitted_by_label\": submitted_by_label,\n    }\n</code></pre>"},{"location":"api/hub/","title":"Hub \u2014 TTT","text":"<p>The <code>TTT</code> class is the hub runtime. It is the sole authority for CTO creation, Delta merge, and HubEvent emission. All ingress goes through <code>start_turn()</code>.</p>"},{"location":"api/hub/#lifecycle","title":"Lifecycle","text":"<pre><code>start_turn(...)\n    \u2514\u2500 validate content profile\n    \u2514\u2500 create CTO\n    \u2514\u2500 emit cto_created\n    \u2514\u2500 multicast to registered Purposes\n</code></pre>"},{"location":"api/hub/#reference","title":"Reference","text":""},{"location":"api/hub/#turnturnturn.hub.TTT","title":"<code>TTT</code>  <code>dataclass</code>","text":"<p>TurnTurnTurn hub runtime.</p> <p>Positioning (v0):   - TTT is the hub: authoritative CTO creation + Delta merge + event emission.   - Purposes are registered agenda-bearing actors that receive HubEvents.   - Ingress happens via start_turn(); the hub creates CTOs directly.</p> Source code in <code>src/turnturnturn/hub.py</code> <pre><code>@dataclass\nclass TTT:\n    \"\"\"\n    TurnTurnTurn hub runtime.\n\n    Positioning (v0):\n      - TTT is the hub: authoritative CTO creation + Delta merge + event emission.\n      - Purposes are registered agenda-bearing actors that receive HubEvents.\n      - Ingress happens via start_turn(); the hub creates CTOs directly.\n    \"\"\"\n\n    registrations: dict[UUID, PurposeRegistration]\n\n    @classmethod\n    def create(cls) -&gt; \"TTT\":\n        return cls(registrations={})\n\n    async def register_purpose(\n        self,\n        purpose: PurposeProtocol,\n        *,\n        subscriptions: list[dict[str, Any]] | None = None,\n    ) -&gt; None:\n        # v0: in-memory registry only. Later: emit PURPOSE_REGISTERED, persist, auth.\n        subs = subscriptions or []\n        self.registrations[purpose.id] = PurposeRegistration(\n            purpose=purpose,\n            token=purpose.token,\n            subscriptions=subs,\n        )\n\n    async def start_turn(\n        self,\n        session_id: UUID,\n        content_profile: str,\n        content: dict[str, Any],\n        *,\n        request_id: str | None = None,\n        submitted_by_label: str | None = None,\n    ) -&gt; UUID:\n        \"\"\"\n        Validate content, create a CTO, emit cto_created, then dispatch.\n        Returns the new CTO's turn_id.\n        \"\"\"\n        validate_content_profile(content_profile, content)\n\n        cto = CTO(\n            turn_id=uuid4(),\n            session_id=session_id,\n            created_at_ms=now_ms(),\n            content_profile=content_profile,\n            content=dict(content),\n        )\n\n        event = HubEvent(\n            event_type=HubEventType.CTO_CREATED,\n            event_id=uuid4(),\n            created_at_ms=now_ms(),\n            session_id=cto.session_id,\n            turn_id=cto.turn_id,\n            payload=payload_cto_created(\n                cto_dict=cto.to_dict(),\n                submitted_by_label=submitted_by_label,\n            ),\n        )\n\n        await self._multicast(event)\n        # v0: no DAG yet; dispatch is \u201call subscribers for this event\u201d\n        return cto.turn_id\n\n    async def _multicast(self, event: HubEvent) -&gt; None:\n        \"\"\"\n        v0: naive broadcast to all registered purposes.\n\n        Later:\n          - subscription matching by event_type (+ filters)\n          - DAG eligibility gating\n          - persistence via TRTPurpose\n        \"\"\"\n        for reg in self.registrations.values():\n            await reg.purpose.take_turn(event)\n</code></pre>"},{"location":"api/hub/#turnturnturn.hub.TTT.start_turn","title":"<code>start_turn(session_id, content_profile, content, *, request_id=None, submitted_by_label=None)</code>  <code>async</code>","text":"<p>Validate content, create a CTO, emit cto_created, then dispatch. Returns the new CTO's turn_id.</p> Source code in <code>src/turnturnturn/hub.py</code> <pre><code>async def start_turn(\n    self,\n    session_id: UUID,\n    content_profile: str,\n    content: dict[str, Any],\n    *,\n    request_id: str | None = None,\n    submitted_by_label: str | None = None,\n) -&gt; UUID:\n    \"\"\"\n    Validate content, create a CTO, emit cto_created, then dispatch.\n    Returns the new CTO's turn_id.\n    \"\"\"\n    validate_content_profile(content_profile, content)\n\n    cto = CTO(\n        turn_id=uuid4(),\n        session_id=session_id,\n        created_at_ms=now_ms(),\n        content_profile=content_profile,\n        content=dict(content),\n    )\n\n    event = HubEvent(\n        event_type=HubEventType.CTO_CREATED,\n        event_id=uuid4(),\n        created_at_ms=now_ms(),\n        session_id=cto.session_id,\n        turn_id=cto.turn_id,\n        payload=payload_cto_created(\n            cto_dict=cto.to_dict(),\n            submitted_by_label=submitted_by_label,\n        ),\n    )\n\n    await self._multicast(event)\n    # v0: no DAG yet; dispatch is \u201call subscribers for this event\u201d\n    return cto.turn_id\n</code></pre>"},{"location":"api/protocols/","title":"Protocols","text":"<p>TTT uses structural subtyping (Protocols) rather than inheritance. This keeps the hub decoupled from concrete Purpose implementations.</p>"},{"location":"api/protocols/#hierarchy","title":"Hierarchy","text":"<pre><code>TurnTakerProtocol        \u2014 can receive HubEvents\n    \u2514\u2500 PurposeProtocol   \u2014 registered agenda-bearing actor (adds name, id, token)\n</code></pre> <p>Implement <code>PurposeProtocol</code> for anything you want to register with TTT. <code>TurnTakerProtocol</code> is available if you need a lighter capability role that participates in the event mesh without being a registered Purpose.</p>"},{"location":"api/protocols/#reference","title":"Reference","text":""},{"location":"api/protocols/#turnturnturn.protocols.TurnTakerProtocol","title":"<code>TurnTakerProtocol</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>A component that can receive HubEvents and optionally emit Deltas.</p> <p>NOTE: - \"TurnTaker\" is a capability role (can participate in the event mesh). - \"Purpose\" is the agenda-bearing registered actor (see PurposeProtocol).</p> Source code in <code>src/turnturnturn/protocols.py</code> <pre><code>@runtime_checkable\nclass TurnTakerProtocol(Protocol):\n    \"\"\"\n    A component that can receive HubEvents and optionally emit Deltas.\n\n    NOTE:\n    - \"TurnTaker\" is a capability role (can participate in the event mesh).\n    - \"Purpose\" is the agenda-bearing registered actor (see PurposeProtocol).\n    \"\"\"\n\n    async def take_turn(self, event: HubEvent) -&gt; None: ...\n</code></pre>"},{"location":"api/protocols/#turnturnturn.protocols.PurposeProtocol","title":"<code>PurposeProtocol</code>","text":"<p>               Bases: <code>TurnTakerProtocol</code>, <code>Protocol</code></p> <p>A registered agenda-bearing actor in the TTT mesh.</p> Identification <ul> <li>name: semantic kind (\"ca\", \"embeddingizer\", \"socratic\", ...)</li> <li>id: per-instance UUID (multiple instances can share the same name)</li> <li>token: nullable 12-char cryptographic token, assigned by hub</li> </ul> <p>The hub uses (id, token) for authentication/authorization seams if enabled.</p> Source code in <code>src/turnturnturn/protocols.py</code> <pre><code>@runtime_checkable\nclass PurposeProtocol(TurnTakerProtocol, Protocol):\n    \"\"\"\n    A registered agenda-bearing actor in the TTT mesh.\n\n    Identification:\n      - name: semantic kind (\"ca\", \"embeddingizer\", \"socratic\", ...)\n      - id: per-instance UUID (multiple instances can share the same name)\n      - token: nullable 12-char cryptographic token, assigned by hub\n\n    The hub uses (id, token) for authentication/authorization seams if enabled.\n    \"\"\"\n\n    name: str\n    id: UUID\n    token: str | None\n</code></pre>"},{"location":"api/registry/","title":"Registry","text":"<p>The <code>PurposeRegistration</code> is the hub's internal record for each registered Purpose. You don't construct these directly \u2014 TTT creates them via <code>register_purpose()</code>.</p>"},{"location":"api/registry/#reference","title":"Reference","text":""},{"location":"api/registry/#turnturnturn.registry.PurposeRegistration","title":"<code>PurposeRegistration</code>  <code>dataclass</code>","text":"<p>Hub registry record for a Purpose instance.</p> Source code in <code>src/turnturnturn/registry.py</code> <pre><code>@dataclass(frozen=True)\nclass PurposeRegistration:\n    \"\"\"\n    Hub registry record for a Purpose instance.\n    \"\"\"\n\n    purpose: PurposeProtocol\n\n    # token is assigned/managed by hub; purpose.token may be None until assigned\n    token: str | None\n\n    # subscriptions are a v0 placeholder; later you\u2019ll likely formalize this\n    subscriptions: list[dict[str, Any]]\n</code></pre>"},{"location":"historical/TTT_specification_v0_2/","title":"TTT Specification v0.2","text":"<p>Status: draft (aligned to TTT_dataclasses_v0_6.py)</p>"},{"location":"historical/TTT_specification_v0_2/#overview","title":"Overview","text":"<p>TurnTurnTurn (TTT) is an event-driven, hub-and-purpose framework for processing conversational \"turns\". The hub is the authoritative component that merges producer-suggested changes (Deltas) into canonical per-turn state (CTO). Purposes are first-class peers that subscribe to events, propose Deltas, and consume HubEvents routed by the DAG router.</p>"},{"location":"historical/TTT_specification_v0_2/#goals","title":"Goals","text":"<ul> <li>Provide a deterministic, auditable core for turn processing.</li> <li>Keep core semantics small: append-only history, hub-authoritative merges, lightweight datamodels.</li> <li>Enable flexible topologies (fan-out, fan-in, dynamic subscriptions) via a DAG router.</li> <li>Support pluggable persistence and offline reconstruction from events.</li> </ul>"},{"location":"historical/TTT_specification_v0_2/#conventions","title":"Conventions","text":"<ul> <li>All event payloads follow <code>_schema</code> (str) and <code>_v</code> (int) to allow evolution.</li> <li>Identifiers:</li> <li><code>turn_id</code>: CTO identity (24 hex chars via <code>secrets.token_hex(12)</code>).</li> <li><code>purpose_id</code>: stable namespace for a Purpose type (string, e.g., \"ca\", \"embed\").</li> <li><code>purpose_token</code> (optional): registration token used to authenticate purpose-originated requests.</li> </ul>"},{"location":"historical/TTT_specification_v0_2/#data-model","title":"Data Model","text":"<p>CTO (Canonical Turn Object) - Fields (minimal):   - <code>turn_id: str</code>   - <code>timestamp: float</code>   - <code>text: str</code>   - <code>role: str</code>   - <code>observations: dict[str, Any]</code> \u2014 values are either a single Observation value (owned namespace) or a list (shared namespace).</p> <p>Observation - Lightweight wrapper describing attribution and storage semantics. - Fields:   - <code>owner: str</code> (purpose_id)   - <code>shared: bool</code> \u2014 <code>False</code> =&gt; owned namespace; <code>True</code> =&gt; shared namespace (list semantics)   - <code>value: Any</code> \u2014 semantic meaning depends on <code>_schema</code>.</p> <p>Delta - Producers (Purposes) create Deltas describing intended mutations. - Fields:   - <code>source_turn_id: str</code>   - <code>purpose_id: str</code>   - <code>invocation_id: str | None</code> (hub-generated for dispatch correlation)   - <code>kind: str</code>   - <code>payload: dict[str, Any]</code> (must include <code>_schema</code> and <code>_v</code> where applicable)   - <code>timestamp: float</code> - Important: Deltas are proposals. They do not themselves mutate <code>cto.observations</code>; the hub must apply/merge them.</p> <p>Purpose - Represents an actor that subscribes to HubEvents and emits Deltas. - Registration includes <code>purpose_id</code>, <code>subscriptions</code>, and optional <code>capabilities</code>.</p> <p>HubEvent - Emitted by the hub for lifecycle transitions and important actions. - Fields include <code>event_id</code>, <code>event_type</code> (EventType), <code>turn_id | None</code>, <code>payload</code>, <code>timestamp</code>.</p>"},{"location":"historical/TTT_specification_v0_2/#core-semantics","title":"Core Semantics","text":"<p>Hub Authority - The hub is the only component permitted to modify <code>cto.observations</code> and <code>ctos</code> state. - Purposes only submit Deltas describing changes; the hub validates and applies them.</p> <p>Append-only - All applied contributions are recorded and preserved. The hub never silently discards prior contributions when merging. - Owned namespaces: the hub records contributions associated with the owning <code>purpose_id</code>. Other purposes may read these entries but cannot propose writes to them. - Shared namespaces: the hub enforces that the stored value is a list; every contribution is appended to the list.</p> <p>Merge Rules (hub) - Deterministic, documented behavior implemented by the hub. Example rules:   - Owned-namespace contribution: hub appends an Observation entry under <code>observations[purpose_id]</code>, preserving prior contributions in event history (if desired, the hub may maintain internal per-purpose append history separate from <code>observations</code> value for compact read models).   - Shared contribution: hub ensures <code>observations[shared_key]</code> is a list and appends; initializes a single-element list if first contribution.   - Structured payloads: hub may call registered merge functions (pluggable) while preserving event history \u2014 these functions must be deterministic and designed to compose with append-only guarantees. - Rejections: malformed Deltas, unauthorized writes (attempt to write to a different purpose's owned namespace), or invalid payloads are rejected with a HubEvent (failure) and logged.</p> <p>Provenance - The authoritative trail is the event stream (HubEvents and persisted Deltas). The <code>observations</code> dict is a denormalized view for convenient consumption; provenance must be reconstructed by querying the events.</p>"},{"location":"historical/TTT_specification_v0_2/#dag-router-subscription-model","title":"DAG Router &amp; Subscription Model","text":"<ul> <li>Purposes register interest declaratively (event types, purpose_ids, or schema patterns).</li> <li>The hub emits HubEvents after applying Deltas; the router matches events to subscribed Purposes and dispatches <code>Purpose</code> invocations.</li> <li>Subscription patterns support wildcards (<code>*</code>, <code>turn_*</code>, <code>purpose_*</code>) and schema filters. A minimal in-process dispatch mechanism will be provided initially; later this can become async or cross-process.</li> </ul>"},{"location":"historical/TTT_specification_v0_2/#api-surface-sketch","title":"API Surface (sketch)","text":"<p>Hub (in <code>src/turnturnturn/hub.py</code>) - <code>class Hub:</code>   - <code>add_cto(cto: CTO) -&gt; None</code> \u2014 persist a new CTO in-memory or via storage.   - <code>get_cto(turn_id: str) -&gt; CTO | None</code>.   - <code>submit_delta(delta: Delta) -&gt; HubEvent</code> \u2014 validate, apply, persist, emit.   - <code>apply_delta(cto: CTO, delta: Delta) -&gt; list[HubEvent]</code> \u2014 apply one or more HubEvents as a result of merge.   - <code>register_purpose(purpose_id: str, subscriptions: list[str], token: str | None)</code>.   - <code>list_events(turn_id: str) -&gt; list[HubEvent]</code> \u2014 readback for provenance.</p> <p>Router (in <code>src/turnturnturn/router.py</code>) - <code>class Router:</code>   - <code>register(purpose_id: str, callback: Callable[[HubEvent], None], subscriptions: list[str])</code>   - <code>dispatch(event: HubEvent) -&gt; None</code> \u2014 route to matching callbacks.</p> <p>Helpers - <code>apply_shared_append(cto, key, value)</code> - <code>apply_owned_contribution(cto, purpose_id, value)</code> - <code>validate_delta(delta)</code></p> <p>Storage &amp; Persistence - v0: in-memory for rapid iteration, with an interface to swap in a durable store. - Event store: append-only list; CTO snapshot store: compact view built from events. - Provide utilities to replay events to reconstruct CTO state.</p>"},{"location":"historical/TTT_specification_v0_2/#security-governance","title":"Security &amp; Governance","text":"<ul> <li>Purposes authenticate with <code>purpose_token</code> during registration; hub validates tokens before accepting Deltas.</li> <li>Capabilities: registration may include capabilities (e.g., whether purpose can create certain schema types or publish to shared namespace). Hub enforces capabilities at write time.</li> </ul>"},{"location":"historical/TTT_specification_v0_2/#operational-concerns","title":"Operational Concerns","text":"<ul> <li>Backpressure: router and hub must support throttling; Deltas may be queued for persistence and merging.</li> <li>Idempotency: Deltas should include stable identifiers where retries may occur; hub deduplicates on <code>invocation_id</code>/Delta id.</li> <li>Observability: hub emits diagnostic HubEvents for errors, rejections, and lifecycle transitions.</li> </ul>"},{"location":"historical/TTT_specification_v0_2/#testing","title":"Testing","text":"<ul> <li>Unit tests for <code>apply_shared_append</code> and <code>apply_owned_contribution</code> covering append semantics, initialization, and invalid writes.</li> <li>Integration tests: submit sequences of Deltas and assert final CTO snapshot and event log content.</li> </ul>"},{"location":"historical/TTT_specification_v0_2/#appendix-design-decisions","title":"Appendix \u2014 Design Decisions","text":"<ol> <li>Hub-authoritative merges</li> <li> <p>Rationale: centralizes correctness (append-only invariants, provenance) and simplifies reasoning for Purpose authors.</p> </li> <li> <p>Purposes-as-peers</p> </li> <li> <p>Rationale: avoids rigid upstream/downstream hierarchy; supports dynamic composition, cross-cutting concerns, and runtime extensibility.</p> </li> <li> <p>Append-only default</p> </li> <li> <p>Rationale: preserves auditability and enables easy replay. Replacements can be modeled by append with a special <code>_schema</code> that marks a correction if needed.</p> </li> <li> <p><code>_schema</code> + <code>_v</code></p> </li> <li> <p>Rationale: explicit schema versioning avoids brittle implicit contracts and allows gradual evolution.</p> </li> <li> <p>Start in\u2011memory, pluggable persistence</p> </li> <li>Rationale: accelerates iteration and tests; provides clear upgrade path for production stores.</li> </ol>"},{"location":"historical/TTT_specification_v0_2/#next-steps-implementation-roadmap","title":"Next steps (implementation roadmap)","text":"<ol> <li>Implement <code>src/turnturnturn/hub.py</code> with core APIs and in-memory stores (CTO map, event list).</li> <li>Add <code>src/turnturnturn/router.py</code> with subscription registry and simple dispatch.</li> <li>Add tests under <code>tests/</code> covering merge rules and end\u2011to\u2011end flow.</li> <li> <p>Provide an example script demonstrating <code>take_turn</code> \u2192 hub merge \u2192 HubEvent dispatch \u2192 purpose callback.</p> </li> <li> <p>Reserved extension keys: payloads MAY include <code>_meta</code> for future extensions; hubs MUST ignore unknown keys.</p> </li> </ol>"},{"location":"historical/TTT_specification_v0_2/#purposeregistrationevent-control-plane","title":"PurposeRegistrationEvent (control-plane)","text":"<p>Purposes may (re)register at any time via <code>take_turn(PurposeRegistrationEvent, purpose_token=...)</code>. The event is accepted only if authenticated by the hub (purpose_token is out-of-band and never included in the HubEvent payload).</p> <p>Semantics: registration replaces the current subscription set for the given <code>purpose_id</code> (latest accepted state wins). Registration history is preserved in the HubEvent stream.</p> <p>Payload shape (conceptual): - <code>_schema: \"purpose_registration\"</code> - <code>_v: 1</code> - <code>purpose_id: str</code> - <code>subscriptions: list[str]</code>  (REPLACE semantics) - <code>request_id?: str</code>          (optional idempotency key) - <code>_meta?: dict[str, Any]</code>    (reserved extension surface; hubs must ignore unknown keys)</p>"},{"location":"historical/TTT_specification_v0_3/","title":"TTT Specification v0.1","text":"<p>Status: draft (aligned to TTT_dataclasses_v0_5.py)</p>"},{"location":"historical/TTT_specification_v0_3/#overview","title":"Overview","text":"<p>TurnTurnTurn (TTT) is an event-driven, hub-and-purpose framework for processing conversational \"turns\". The hub is the authoritative component that merges producer-suggested changes (Deltas) into canonical per-turn state (CTO). Purposes are first-class peers that subscribe to events, propose Deltas, and consume HubEvents routed by the DAG router.</p>"},{"location":"historical/TTT_specification_v0_3/#goals","title":"Goals","text":"<ul> <li>Provide a deterministic, auditable core for turn processing.</li> <li>Keep core semantics small: append-only history, hub-authoritative merges, lightweight datamodels.</li> <li>Enable flexible topologies (fan-out, fan-in, dynamic subscriptions) via a DAG router.</li> <li>Support pluggable persistence and offline reconstruction from events.</li> </ul>"},{"location":"historical/TTT_specification_v0_3/#conventions","title":"Conventions","text":"<ul> <li>All event payloads follow <code>_schema</code> (str) and <code>_v</code> (int) to allow evolution.</li> <li>Identifiers:</li> <li><code>turn_id</code>: CTO identity (24 hex chars via <code>secrets.token_hex(12)</code>).</li> <li><code>purpose_id</code>: stable namespace for a Purpose type (string, e.g., \"ca\", \"embed\").</li> <li><code>purpose_token</code> (optional): registration token used to authenticate purpose-originated requests.</li> </ul>"},{"location":"historical/TTT_specification_v0_3/#data-model","title":"Data Model","text":"<p>CTO (Canonical Turn Object) - Fields (minimal):   - <code>turn_id: str</code>   - <code>timestamp: float</code>   - <code>text: str</code>   - <code>role: str</code>   - <code>observations: dict[str, Any]</code> \u2014 values are either a single Observation value (owned namespace) or a list (shared namespace).</p> <p>Observation - Lightweight wrapper describing attribution and storage semantics. - Fields:   - <code>owner: str</code> (purpose_id)   - <code>shared: bool</code> \u2014 <code>False</code> =&gt; owned namespace; <code>True</code> =&gt; shared namespace (list semantics)   - <code>value: Any</code> \u2014 semantic meaning depends on <code>_schema</code>.</p> <p>Delta - Producers (Purposes) create Deltas describing intended mutations. - Fields:   - <code>source_turn_id: str</code>   - <code>purpose_id: str</code>   - <code>invocation_id: str | None</code> (hub-generated for dispatch correlation)   - <code>kind: str</code>   - <code>payload: dict[str, Any]</code> (must include <code>_schema</code> and <code>_v</code> where applicable)   - <code>timestamp: float</code> - Important: Deltas are proposals. They do not themselves mutate <code>cto.observations</code>; the hub must apply/merge them.</p> <p>Purpose - Represents an actor that subscribes to HubEvents and emits Deltas. - Registration includes <code>purpose_id</code>, <code>subscriptions</code>, and optional <code>capabilities</code>.</p> <p>HubEvent - Emitted by the hub for lifecycle transitions and important actions. - Fields include <code>event_id</code>, <code>event_type</code> (EventType), <code>turn_id | None</code>, <code>payload</code>, <code>timestamp</code>.</p>"},{"location":"historical/TTT_specification_v0_3/#core-semantics","title":"Core Semantics","text":"<p>Hub Authority - The hub is the only component permitted to modify <code>cto.observations</code> and <code>ctos</code> state. - Purposes only submit Deltas describing changes; the hub validates and applies them.</p> <p>Append-only - All applied contributions are recorded and preserved. The hub never silently discards prior contributions when merging. - Owned namespaces: the hub records contributions associated with the owning <code>purpose_id</code>. Other purposes may read these entries but cannot propose writes to them. - Shared namespaces: the hub enforces that the stored value is a list; every contribution is appended to the list.</p> <p>Merge Rules (hub) - Deterministic, documented behavior implemented by the hub. Example rules:   - Owned-namespace contribution: hub appends an Observation entry under <code>observations[purpose_id]</code>, preserving prior contributions in event history (if desired, the hub may maintain internal per-purpose append history separate from <code>observations</code> value for compact read models).   - Shared contribution: hub ensures <code>observations[shared_key]</code> is a list and appends; initializes a single-element list if first contribution.   - Structured payloads: hub may call registered merge functions (pluggable) while preserving event history \u2014 these functions must be deterministic and designed to compose with append-only guarantees. - Rejections: malformed Deltas, unauthorized writes (attempt to write to a different purpose's owned namespace), or invalid payloads are rejected with a HubEvent (failure) and logged.</p> <p>Provenance - The authoritative trail is the event stream (HubEvents and persisted Deltas). The <code>observations</code> dict is a denormalized view for convenient consumption; provenance must be reconstructed by querying the events.</p>"},{"location":"historical/TTT_specification_v0_3/#dag-router-subscription-model","title":"DAG Router &amp; Subscription Model","text":"<ul> <li>Purposes register interest declaratively (event types, purpose_ids, or schema patterns).</li> <li>The hub emits HubEvents after applying Deltas; the router matches events to subscribed Purposes and dispatches <code>Purpose</code> invocations.</li> <li>Subscription patterns support wildcards (<code>*</code>, <code>turn_*</code>, <code>purpose_*</code>) and schema filters. A minimal in-process dispatch mechanism will be provided initially; later this can become async or cross-process.</li> </ul>"},{"location":"historical/TTT_specification_v0_3/#api-surface-sketch","title":"API Surface (sketch)","text":"<p>Hub (in <code>src/turnturnturn/hub.py</code>) - <code>class Hub:</code>   - <code>add_cto(cto: CTO) -&gt; None</code> \u2014 persist a new CTO in-memory or via storage.   - <code>get_cto(turn_id: str) -&gt; CTO | None</code>.   - <code>submit_delta(delta: Delta) -&gt; HubEvent</code> \u2014 validate, apply, persist, emit.   - <code>apply_delta(cto: CTO, delta: Delta) -&gt; list[HubEvent]</code> \u2014 apply one or more HubEvents as a result of merge.   - <code>register_purpose(purpose_id: str, subscriptions: list[str], token: str | None)</code>.   - <code>list_events(turn_id: str) -&gt; list[HubEvent]</code> \u2014 readback for provenance.</p> <p>Router (in <code>src/turnturnturn/router.py</code>) - <code>class Router:</code>   - <code>register(purpose_id: str, callback: Callable[[HubEvent], None], subscriptions: list[str])</code>   - <code>dispatch(event: HubEvent) -&gt; None</code> \u2014 route to matching callbacks.</p> <p>Helpers - <code>apply_shared_append(cto, key, value)</code> - <code>apply_owned_contribution(cto, purpose_id, value)</code> - <code>validate_delta(delta)</code></p> <p>Storage &amp; Persistence - v0: in-memory for rapid iteration, with an interface to swap in a durable store. - Event store: append-only list; CTO snapshot store: compact view built from events. - Provide utilities to replay events to reconstruct CTO state.</p>"},{"location":"historical/TTT_specification_v0_3/#security-governance","title":"Security &amp; Governance","text":"<ul> <li>Purposes authenticate with <code>purpose_token</code> during registration; hub validates tokens before accepting Deltas.</li> <li>Capabilities: registration may include capabilities (e.g., whether purpose can create certain schema types or publish to shared namespace). Hub enforces capabilities at write time.</li> </ul>"},{"location":"historical/TTT_specification_v0_3/#operational-concerns","title":"Operational Concerns","text":"<ul> <li>Backpressure: router and hub must support throttling; Deltas may be queued for persistence and merging.</li> <li>Idempotency: Deltas should include stable identifiers where retries may occur; hub deduplicates on <code>invocation_id</code>/Delta id.</li> <li>Observability: hub emits diagnostic HubEvents for errors, rejections, and lifecycle transitions.</li> </ul>"},{"location":"historical/TTT_specification_v0_3/#testing","title":"Testing","text":"<ul> <li>Unit tests for <code>apply_shared_append</code> and <code>apply_owned_contribution</code> covering append semantics, initialization, and invalid writes.</li> <li>Integration tests: submit sequences of Deltas and assert final CTO snapshot and event log content.</li> </ul>"},{"location":"historical/TTT_specification_v0_3/#appendix-design-decisions","title":"Appendix \u2014 Design Decisions","text":"<ol> <li>Hub-authoritative merges</li> <li> <p>Rationale: centralizes correctness (append-only invariants, provenance) and simplifies reasoning for Purpose authors.</p> </li> <li> <p>Purposes-as-peers</p> </li> <li> <p>Rationale: avoids rigid upstream/downstream hierarchy; supports dynamic composition, cross-cutting concerns, and runtime extensibility.</p> </li> <li> <p>Append-only default</p> </li> <li> <p>Rationale: preserves auditability and enables easy replay. Replacements can be modeled by append with a special <code>_schema</code> that marks a correction if needed.</p> </li> <li> <p><code>_schema</code> + <code>_v</code></p> </li> <li> <p>Rationale: explicit schema versioning avoids brittle implicit contracts and allows gradual evolution.</p> </li> <li> <p>Start in\u2011memory, pluggable persistence</p> </li> <li>Rationale: accelerates iteration and tests; provides clear upgrade path for production stores.</li> </ol>"},{"location":"historical/TTT_specification_v0_3/#next-steps-implementation-roadmap","title":"Next steps (implementation roadmap)","text":"<ol> <li>Implement <code>src/turnturnturn/hub.py</code> with core APIs and in-memory stores (CTO map, event list).</li> <li>Add <code>src/turnturnturn/router.py</code> with subscription registry and simple dispatch.</li> <li>Add tests under <code>tests/</code> covering merge rules and end\u2011to\u2011end flow.</li> <li>Provide an example script demonstrating <code>take_turn</code> \u2192 hub merge \u2192 HubEvent dispatch \u2192 purpose callback.</li> </ol>"},{"location":"historical/TTT_specification_v0_4/","title":"TTT Specification","text":""},{"location":"historical/TTT_specification_v0_4/#overview","title":"Overview","text":"<p>TurnTurnTurn (TTT) is an event-driven, hub-and-purpose framework for processing conversational \"turns\". The hub is the authoritative component that merges producer-suggested changes (Deltas) into canonical per-turn state (CTO). Purposes are first-class peers that subscribe to events, propose Deltas, and consume HubEvents routed by the DAG router.</p>"},{"location":"historical/TTT_specification_v0_4/#goals","title":"Goals","text":"<ul> <li>Provide a deterministic, auditable core for turn processing.</li> <li>Keep core semantics small: append-only history, hub-authoritative merges, lightweight datamodels.</li> <li>Enable flexible topologies (fan-out, fan-in, dynamic subscriptions) via a DAG router.</li> <li>Support pluggable persistence and offline reconstruction from events.</li> </ul>"},{"location":"historical/TTT_specification_v0_4/#conventions","title":"Conventions","text":"<ul> <li>Canonical identifiers: <code>turn_id</code> is the only valid turn identity key.</li> <li>Subscriptions: match on <code>HubEvent.event_type</code> patterns (routing), then apply optional attribute filters (filtering).</li> <li><code>SubscriptionLike</code> (v0): either a string <code>event_type</code> pattern, or an object with:</li> <li><code>event_type: str</code> (pattern; may be \"*\")</li> <li>optional filters such as <code>turn_id: str</code> and <code>purpose_id: str</code></li> <li>hubs MUST ignore unknown keys in subscription objects.</li> <li>All event payloads follow <code>_schema</code> (str) and <code>_v</code> (int) to allow evolution.</li> <li>Identifiers:</li> <li><code>turn_id</code>: CTO identity (24 hex chars via <code>secrets.token_hex(12)</code>).</li> <li><code>purpose_id</code>: stable namespace for a Purpose type (string, e.g., \"ca\", \"embed\").</li> <li><code>purpose_token</code> (optional): registration token used to authenticate purpose-originated requests.</li> </ul>"},{"location":"historical/TTT_specification_v0_4/#data-model","title":"Data Model","text":"<p>CTO (Canonical Turn Object) - Fields (minimal):   - <code>turn_id: str</code>   - <code>timestamp: float</code>   - <code>text: str</code>   - <code>role: str</code>   - <code>observations: dict[str, Any]</code> \u2014 values are either a single Observation value (owned namespace) or a list (shared namespace).</p> <p>Observation - Lightweight wrapper describing attribution and storage semantics. - Fields:   - <code>owner: str</code> (purpose_id)   - <code>shared: bool</code> \u2014 <code>False</code> =&gt; owned namespace; <code>True</code> =&gt; shared namespace (list semantics)   - <code>value: Any</code> \u2014 semantic meaning depends on <code>_schema</code>.</p> <p>Delta - Producers (Purposes) create Deltas describing intended mutations. - Fields:   - <code>source_turn_id: str</code>   - <code>purpose_id: str</code>   - <code>invocation_id: str | None</code> (hub-generated for dispatch correlation)   - <code>kind: str</code>   - <code>payload: dict[str, Any]</code> (must include <code>_schema</code> and <code>_v</code> where applicable)   - <code>timestamp: float</code> - Important: Deltas are proposals. They do not themselves mutate <code>cto.observations</code>; the hub must apply/merge them.</p> <p>Purpose - Represents an actor that subscribes to HubEvents and emits Deltas. - Registration includes <code>purpose_id</code>, <code>subscriptions</code>, and optional <code>capabilities</code>.</p> <p>HubEvent - Emitted by the hub for lifecycle transitions and important actions. - Fields include <code>event_id</code>, <code>event_type</code> (EventType), <code>turn_id | None</code>, <code>payload</code>, <code>timestamp</code>.</p>"},{"location":"historical/TTT_specification_v0_4/#core-semantics","title":"Core Semantics","text":"<p>Hub Authority - The hub is the only component permitted to modify <code>cto.observations</code> and <code>ctos</code> state. - Purposes only submit Deltas describing changes; the hub validates and applies them.</p> <p>Append-only - All applied contributions are recorded and preserved. The hub never silently discards prior contributions when merging. - Owned namespaces: the hub records contributions associated with the owning <code>purpose_id</code>. Other purposes may read these entries but cannot propose writes to them. - Shared namespaces: the hub enforces that the stored value is a list; every contribution is appended to the list.</p> <p>Merge Rules (hub) - Deterministic, documented behavior implemented by the hub. Example rules:   - Owned-namespace contribution: hub appends an Observation entry under <code>observations[purpose_id]</code>, preserving prior contributions in event history (if desired, the hub may maintain internal per-purpose append history separate from <code>observations</code> value for compact read models).   - Shared contribution: hub ensures <code>observations[shared_key]</code> is a list and appends; initializes a single-element list if first contribution.   - Structured payloads: hub may call registered merge functions (pluggable) while preserving event history \u2014 these functions must be deterministic and designed to compose with append-only guarantees. - Rejections: malformed Deltas, unauthorized writes (attempt to write to a different purpose's owned namespace), or invalid payloads are rejected with a HubEvent (failure) and logged.</p> <p>Provenance - The authoritative trail is the event stream (HubEvents and persisted Deltas). The <code>observations</code> dict is a denormalized view for convenient consumption; provenance must be reconstructed by querying the events.</p>"},{"location":"historical/TTT_specification_v0_4/#dag-router-subscription-model","title":"DAG Router &amp; Subscription Model","text":"<ul> <li>Purposes register interest declaratively (event types, purpose_ids, or schema patterns).</li> <li>The hub emits HubEvents after applying Deltas; the router matches events to subscribed Purposes and dispatches <code>Purpose</code> invocations.</li> <li>Subscription patterns support wildcards (<code>*</code>, <code>turn_*</code>, <code>purpose_*</code>) and schema filters. A minimal in-process dispatch mechanism will be provided initially; later this can become async or cross-process.</li> </ul>"},{"location":"historical/TTT_specification_v0_4/#api-surface-sketch","title":"API Surface (sketch)","text":"<p>Hub (in <code>src/turnturnturn/hub.py</code>) - <code>class Hub:</code>   - <code>add_cto(cto: CTO) -&gt; None</code> \u2014 persist a new CTO in-memory or via storage.   - <code>get_cto(turn_id: str) -&gt; CTO | None</code>.   - <code>submit_delta(delta: Delta) -&gt; HubEvent</code> \u2014 validate, apply, persist, emit.   - <code>apply_delta(cto: CTO, delta: Delta) -&gt; list[HubEvent]</code> \u2014 apply one or more HubEvents as a result of merge.   - <code>register_purpose(purpose_id: str, subscriptions: list[SubscriptionLike], token: str | None)</code>.   - <code>list_events(turn_id: str) -&gt; list[HubEvent]</code> \u2014 readback for provenance.</p> <p>Router (in <code>src/turnturnturn/router.py</code>) - <code>class Router:</code>   - <code>register(purpose_id: str, callback: Callable[[HubEvent], None], subscriptions: list[SubscriptionLike])</code>   - <code>dispatch(event: HubEvent) -&gt; None</code> \u2014 route to matching callbacks.</p> <p>Helpers - <code>apply_shared_append(cto, key, value)</code> - <code>apply_owned_contribution(cto, purpose_id, value)</code> - <code>validate_delta(delta)</code></p> <p>Storage &amp; Persistence - v0: in-memory for rapid iteration, with an interface to swap in a durable store. - Event store: append-only list; CTO snapshot store: compact view built from events. - Provide utilities to replay events to reconstruct CTO state.</p>"},{"location":"historical/TTT_specification_v0_4/#security-governance","title":"Security &amp; Governance","text":"<ul> <li>Purposes authenticate with <code>purpose_token</code> during registration; hub validates tokens before accepting Deltas.</li> <li>Capabilities: registration may include capabilities (e.g., whether purpose can create certain schema types or publish to shared namespace). Hub enforces capabilities at write time.</li> </ul>"},{"location":"historical/TTT_specification_v0_4/#operational-concerns","title":"Operational Concerns","text":"<ul> <li>Backpressure: router and hub must support throttling; Deltas may be queued for persistence and merging.</li> <li>Idempotency: Deltas should include stable identifiers where retries may occur; hub deduplicates on <code>invocation_id</code>/Delta id.</li> <li>Observability: hub emits diagnostic HubEvents for errors, rejections, and lifecycle transitions.</li> </ul>"},{"location":"historical/TTT_specification_v0_4/#testing","title":"Testing","text":"<ul> <li>Unit tests for <code>apply_shared_append</code> and <code>apply_owned_contribution</code> covering append semantics, initialization, and invalid writes.</li> <li>Integration tests: submit sequences of Deltas and assert final CTO snapshot and event log content.</li> </ul>"},{"location":"historical/TTT_specification_v0_4/#appendix-design-decisions","title":"Appendix \u2014 Design Decisions","text":"<ol> <li>Hub-authoritative merges</li> <li> <p>Rationale: centralizes correctness (append-only invariants, provenance) and simplifies reasoning for Purpose authors.</p> </li> <li> <p>Purposes-as-peers</p> </li> <li> <p>Rationale: avoids rigid upstream/downstream hierarchy; supports dynamic composition, cross-cutting concerns, and runtime extensibility.</p> </li> <li> <p>Append-only default</p> </li> <li> <p>Rationale: preserves auditability and enables easy replay. Replacements can be modeled by append with a special <code>_schema</code> that marks a correction if needed.</p> </li> <li> <p><code>_schema</code> + <code>_v</code></p> </li> <li> <p>Rationale: explicit schema versioning avoids brittle implicit contracts and allows gradual evolution.</p> </li> <li> <p>Start in\u2011memory, pluggable persistence</p> </li> <li>Rationale: accelerates iteration and tests; provides clear upgrade path for production stores.</li> </ol>"},{"location":"historical/TTT_specification_v0_4/#next-steps-implementation-roadmap","title":"Next steps (implementation roadmap)","text":"<ol> <li>Implement <code>src/turnturnturn/hub.py</code> with core APIs and in-memory stores (CTO map, event list).</li> <li>Add <code>src/turnturnturn/router.py</code> with subscription registry and simple dispatch.</li> <li>Add tests under <code>tests/</code> covering merge rules and end\u2011to\u2011end flow.</li> <li>Provide an example script demonstrating <code>take_turn</code> \u2192 hub merge \u2192 HubEvent dispatch \u2192 purpose callback.</li> </ol>"},{"location":"historical/ttt_architecture_v0_12/","title":"TurnTurnTurn (TTT) \u2014 Architecture Document","text":""},{"location":"historical/ttt_architecture_v0_12/#version-012-march-2026","title":"Version 0.12 \u2014 March 2026","text":""},{"location":"historical/ttt_architecture_v0_12/#source-design-session-between-bill-and-claude-anthropic","title":"Source: Design session between Bill and Claude (Anthropic)","text":"<p>v0.6 note: Reinstates the provider registration + subscriptions layer from v0.4 as an explicit hub capability (TTT.register / ChangeSubscriptionsEvent) while keeping the v0.2 core dataclasses (CTO/Observation/Delta) and HubEvent stream as the authoritative record.</p>"},{"location":"historical/ttt_architecture_v0_12/#0-background-and-motivation","title":"0. Background and Motivation","text":"<p>This document describes the architecture of TurnTurnTurn (TTT), a lightweight, domain-neutral Python middleware library for conversational turn processing. It emerged from design work on Adjacency (a structured elicitation research instrument) and the need for a reusable infrastructure layer that could serve that project and others.</p> <p>An earlier document (Alignment Middleware for Human\u2013LLM Dialogue) sketched a broader alignment and detection pipeline. TTT is not that system. TTT is the foundational layer that such a system would be built on \u2014 the part that handles canonical turn representation, enrichment, event routing, and persistence, without presupposing any particular analytical purpose.</p> <p>Sections of the earlier document that remain relevant \u2014 particularly the trace mutation detection signals, alignment snapshot design, DecisionCard/provenance accumulator, and the integration roadmap \u2014 are preserved in Appendix A of this document for reference. Those components are best understood as Purpose implementations that would run on top of TTT, not as architectural concerns of TTT itself.</p>"},{"location":"historical/ttt_architecture_v0_12/#1-design-principles","title":"1. Design Principles","text":"<p>Raw text is the substrate. The CTO is the unit of computation. Every turn becomes a Canonical Turn Object immediately on receipt. All downstream processing operates based on CTOs, never on raw strings.</p> <p>The event stream is the authoritative record. CTOs and Deltas travel on typed events. The event stream is what you replay to reconstruct any derived artifact. Full CTO observation state is always derivable from the event stream.</p> <p>Append-only. Never override; hub is authoritative. The CTO identity fields are immutable at construction. The <code>observations</code> dict is never written directly by Purposes or other external components \u2014 the hub is the sole authority that merges incoming Deltas into the CTO observation state. Purposes emit Deltas (events) describing intended changes; the hub receives those Deltas via the <code>take_turn</code> pathway, validates and merges them, and then emits HubEvents and dispatches follow-up purposes via the DAG.</p> <p>Owned vs shared namespaces. Each Purpose owns one owned namespace in <code>cto.observations</code>. Owned namespace entries are written by the hub on behalf of that purpose; other purposes may read those entries but may not propose writes to them. Purposes have no mechanism to propose updates outside their own owned namespace or the shared namespace \u2014 the hub enforces that rule. Shared namespaces are represented by lists maintained by the hub; when a purpose contributes to a shared key the hub appends the contribution to the list (<code>[a]</code> \u2192 <code>[a, b]</code>). The hub enforces the invariant that shared entries are lists and that owned entries are authored only by the hub acting for the owning purpose. The hub preserves append history and provenance via the event stream; higher\u2011level conflict resolution (semantics of merging structured payloads) is left to downstream resolvers when needed.</p> <p>Provenance is a query, not a field. Who contributed what, and when, is answerable from the event stream and HubEvents; the <code>observations</code> dict itself intentionally contains values only so that in\u2011memory CTOs are compact and easy to consume. TRTPurpose (and other adapters) persist events; provenance is reconstructed by querying that event log or by reading HubEvents emitted during processing.</p> <p>Domain\u2011neutral core. TTT knows nothing about domain semantics such as trace mutations, conversation analysis, or LLM alignment \u2014 those responsibilities belong to Purpose implementations. The core provides the event / DAG routing, merge semantics, and enforcement guarantees. The core ships with exactly one built\u2011in Purpose adapter: TRTPurpose (the TTT integration point for TurnReTurn).</p> <p><code>take_turn</code> is the one true path (routing &amp; dispatch). All inbound messages and lifecycle operations are expressed as events and enter the system through <code>take_turn</code>. <code>take_turn</code> is responsible for accepting an incoming event (Delta, registration, observation write request, etc.), validating it, handing it to the hub merge logic (which alone updates the CTO/observations), and then routing any resulting HubEvents through the DAG to subscribed Purposes. Convenience APIs such as <code>ttt.register()</code> are thin wrappers that construct and dispatch these events via <code>take_turn</code>.</p> <p>Loosely coupled via declared dependencies. Purposes declare what other Purposes they depend on. TTT manages execution order via a DAG resolver. Purposes never call each other directly.</p> <p>TTT is a domain-specific micro actor system built on asyncio. The architecture is conceptually an Actor model: every participant has a typed message interface, communicates via events, and does not know about other participants' implementations. Implemented directly on asyncio + dataclasses rather than an actor framework, which would add overhead without benefit at the expected scale.</p>"},{"location":"historical/ttt_architecture_v0_12/#2-conceptual-model","title":"2. Conceptual Model","text":"<pre><code>Calling code (e.g. Adjacency session runner)\n    \u2502\n    \u2502  ttt.register(purpose)          \u2190 convenience wrapper\n    \u2502  take_turn(HubEvent(\"register\")) \u2190 what actually happens\n    \u2502\n    \u2502  take_turn(HubEvent(\"turn_observed\", ...))\n    \u25bc\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502                TurnTurnTurn                  \u2502\n\u2502                                              \u2502\n\u2502  1. Construct CTO from turn_observed event   \u2502\n\u2502  2. Resolve DAG \u2014 find eligible Purposes     \u2502\n\u2502  3. Dispatch concurrently via asyncio        \u2502\n\u2502  4. Receive purpose_complete events back     \u2502\n\u2502  5. If delta present:                        \u2502\n\u2502     a. Validate token                        \u2502\n\u2502     b. Write purpose_data \u2192                  \u2502\n\u2502           observations[purpose_id]           \u2502\n\u2502     c. Append shared_data \u2192                  \u2502\n\u2502           observations[schema_id]            \u2502\n\u2502     d. Advance DAG \u2014 check new eligibles     \u2502\n\u2502  6. Emit purpose_resolved to subscribers     \u2502\n\u2502     (filtered by declared interest/wildcard) \u2502\n\u2502  7. When DAG exhausted:                      \u2502\n\u2502     emit turn_processing_complete            \u2502\n\u2502     or turn_processing_incomplete (timeout)  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n         \u2502                        \u25b2\n         \u2502 take_turn(HubEvent)    \u2502 take_turn(HubEvent)\n         \u25bc                        \u2502\n  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n  \u2502 TRTPurpose \u2502  \u2502  CAPurpose      \u2502  \u2502  TMPurpose    \u2502\n  \u2502 (built-in) \u2502  \u2502  depends_on=[]  \u2502  \u2502  depends_on=  \u2502\n  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518  \u2502  [\"embed\",\"ca\"]\u2502\n        \u2502                              \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n        \u2502                                      \u2502\n        \u25bc                              may emit speaker_feedback\n  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510          back to calling code\n  \u2502      TurnReTurn         \u2502\n  \u2502                         \u2502\n  \u2502  Stores:                \u2502\n  \u2502  \u251c\u2500\u2500 FileStore          \u2502\n  \u2502  \u2502   \u251c\u2500\u2500 JSONLBackend   \u2502\n  \u2502  \u2502   \u251c\u2500\u2500 JSONBackend    \u2502\n  \u2502  \u2502   \u2514\u2500\u2500 PlainTextBackend\u2502\n  \u2502  \u251c\u2500\u2500 MemoryStore        \u2502\n  \u2502  \u2502   \u2514\u2500\u2500 MessageDictBackend\u2502\n  \u2502  \u2514\u2500\u2500 SQLStore           \u2502\n  \u2502      \u251c\u2500\u2500 SQLiteBackend  \u2502\n  \u2502      \u2514\u2500\u2500 PostgresBackend\u2502\n  \u2502                         \u2502\n  \u2502  Query views:           \u2502\n  \u2502  \u251c\u2500\u2500 provenance()       \u2502\n  \u2502  \u251c\u2500\u2500 session_replay()   \u2502\n  \u2502  \u2514\u2500\u2500 purpose_audit()    \u2502\n  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"historical/ttt_architecture_v0_12/#25-applicability-beyond-any-single-use-case","title":"2.5 Applicability Beyond Any Single Use Case","text":"<p>TTT is not a pipeline framework. Because calling code is itself registered as a Purpose, TTT forms a peer mesh of Purposes coordinated by the hub:</p> <ul> <li>Every actor is a first-class node in the same event graph (no privileged \u201cupstream/downstream\u201d layer).</li> <li>Purposes do not call each other; they subscribe to events and propose observations via Deltas.</li> <li>The hub is the sole authority that merges proposed changes into <code>cto.observations</code>, preserving append history and provenance via the event stream.</li> </ul> <p>In practice, \u201cupstream/downstream\u201d is only a description of a particular topology (who subscribes to whom), not an architectural hierarchy.</p>"},{"location":"historical/ttt_architecture_v0_12/#251-what-this-unlocks","title":"2.5.1 What this unlocks","text":"<ul> <li>Composition &amp; orchestration: assemble complex behaviors from small, focused Purposes that each add a narrow observation set.</li> <li>Late binding: add/remove Purposes at runtime via registration/subscription changes without rewriting orchestration code.</li> <li>Cross-cutting concerns: privacy, policy checks, logging, and auditing can be implemented as separate Purposes that subscribe broadly.</li> <li>Multi-agent enrichment: multiple independent analyzers can contribute to shared keys (append-only lists) without coordination beyond schema contracts.</li> <li>Replay and re-analysis: new Purposes can be run over historical event logs to derive new artifacts without modifying prior records.</li> </ul>"},{"location":"historical/ttt_architecture_v0_12/#252-example-topologies","title":"2.5.2 Example topologies","text":"<p>These are illustrative \u201cstarter meshes\u201d that demonstrate the generality of CTO + event routing beyond any single domain.</p>"},{"location":"historical/ttt_architecture_v0_12/#topology-a-observability-evaluation-mesh-llm-app-debugging","title":"Topology A \u2014 Observability + Evaluation Mesh (LLM app debugging)","text":"<ul> <li>Calling code (as Purpose) emits <code>turn_observed</code> / lifecycle events.</li> <li><code>TRTPurpose</code> persists all HubEvents (JSONL/SQL).</li> <li><code>CostPurpose</code> annotates token usage / latency metrics.</li> <li><code>EvalPurpose</code> computes lightweight quality signals (e.g., semantic similarity checks) and appends evaluation observations.</li> <li><code>AuditPurpose</code> subscribes to <code>*</code> and emits human-readable audit summaries.</li> </ul> <p>Outcome: - A replayable event log plus per-turn \u201cquality/audit\u201d observations, without any LLM-specific semantics in the core.</p>"},{"location":"historical/ttt_architecture_v0_12/#topology-b-decision-ledger-mesh-decisioncards-as-projections","title":"Topology B \u2014 Decision Ledger Mesh (DecisionCards as projections)","text":"<ul> <li><code>DecisionExtractPurpose</code> proposes decision-move observations (append-only evidence).</li> <li><code>DecisionResolvePurpose</code> materializes a \u201ccurrent snapshot\u201d (replaceable view) from evidence.</li> <li><code>ReviewPurpose</code> (human-in-the-loop) subscribes to conflicts/unresolved items and appends review actions.</li> <li><code>TRTPurpose</code> stores everything; re-resolution can be run deterministically when rules evolve.</li> </ul> <p>Outcome: - DecisionCards become a downstream projection over the authoritative log; multiple extractors can coexist.</p>"},{"location":"historical/ttt_architecture_v0_12/#topology-c-federated-enrichment-mesh-edge-central","title":"Topology C \u2014 Federated Enrichment Mesh (edge \u2192 central)","text":"<ul> <li>Edge node runs <code>EmbedPurpose</code> / lightweight heuristics locally and emits Deltas/events.</li> <li>Central hub persists HubEvents and merges shared observations.</li> <li>Optional <code>SearchIndexPurpose</code> builds full-text indices; optional vector index is maintained as a separate projection.</li> <li>A <code>PolicyPurpose</code> enforces redaction/sanitization by proposing redaction observations (never overwriting raw text).</li> </ul> <p>Outcome: - Extensible enrichment with preserved provenance and the ability to recompute or augment annotations later.</p>"},{"location":"historical/ttt_architecture_v0_12/#253-what-purposes-must-agree-on-minimal-governance","title":"2.5.3 What Purposes must agree on (minimal governance)","text":"<p>To keep the peer mesh tractable, Purposes should follow two conventions:</p> <ul> <li>Schema/versioning: event payloads and observation values include <code>_schema</code> and <code>_v</code> when the shape matters.</li> <li>Merge expectations: shared keys are append-only lists; owned keys are written only by the hub on behalf of the owning Purpose.</li> </ul>"},{"location":"historical/ttt_architecture_v0_12/#3-core-protocols","title":"3. Core Protocols","text":""},{"location":"historical/ttt_architecture_v0_12/#31-turntakerprotocol","title":"3.1 TurnTakerProtocol","text":"<p>The fundamental interface. Everything that participates in the event stream implements it: Purposes, TTT itself, and calling code.</p> <pre><code>class TurnTakerProtocol(Protocol):\n    async def take_turn(self, event: HubEvent) -&gt; None: ...\n</code></pre> <p><code>take_turn</code> accepts a <code>HubEvent</code> and returns nothing. Participants that want to send results or feedback back to TTT call <code>ttt.take_turn(event)</code> directly. The interface is uniform across all participants \u2014 no special return types, no polymorphic signatures.</p>"},{"location":"historical/ttt_architecture_v0_12/#32-purposeprotocol","title":"3.2 PurposeProtocol","text":"<p>Extends <code>TurnTakerProtocol</code>. Purpose metadata is declared as class attributes and communicated to TTT via the <code>register</code> event.</p> <pre><code>class PurposeProtocol(TurnTakerProtocol, Protocol):\n    purpose_id: str      # stable machine identifier; becomes namespace key\n    purpose_name: str    # human-readable label for logging and audit views\n\n    async def take_turn(self, event: HubEvent) -&gt; None: ...\n</code></pre> <p>Registration metadata (<code>private</code>, <code>depends_on</code>, <code>subscriptions</code>) is carried in the <code>register</code> event payload. This allows the same Purpose implementation to be registered with different configurations in different sessions.</p>"},{"location":"historical/ttt_architecture_v0_12/#33-storeprotocol","title":"3.3 StoreProtocol","text":"<pre><code>class StoreProtocol(Protocol):\n    async def write(self, event: HubEvent) -&gt; None: ...\n    async def flush(self) -&gt; None: ...\n    async def query(self, **kwargs) -&gt; list[dict]: ...  # optional\n</code></pre>"},{"location":"historical/ttt_architecture_v0_12/#4-event-model","title":"4. Event Model","text":"<p>TTT/Adjacency now treats the hub-emitted event stream as the authoritative record of turn processing. Events are emitted on each hub state transition and are persisted by downstream stores (e.g., TurnReTurn backends).</p>"},{"location":"historical/ttt_architecture_v0_12/#41-hubevent-authoritative-event-record","title":"4.1 HubEvent (authoritative event record)","text":"<pre><code>@dataclass\nclass HubEvent:\n    event_id:   str            # 24-char hex (secrets.token_hex(12))\n    event_type: EventType      # Enum (pipeline + annotation)\n    turn_id:   str | None     # CTO turn_id when event is turn-scoped\n    payload:    dict[str, Any] # event-specific; convention: includes _schema and _v\n    timestamp:  float          # unix epoch\n</code></pre> <p>Token terminology: - <code>turn_id</code> identifies a single CTO (per turn). - <code>purpose_token</code> identifies a registered Purpose run (may span many turns/sessions).</p> <p>Events are intentionally small and JSON-safe (or trivially serializable via <code>to_dict()</code> on embedded objects). Payloads should follow the <code>_schema</code> + <code>_v</code> convention for future migration/debugging.</p>"},{"location":"historical/ttt_architecture_v0_12/#42-eventtype-vocabulary-aligned-to-dataclasses-v05","title":"4.2 EventType vocabulary (aligned to dataclasses v0.5)","text":"<p>Pipeline (DAG) lifecycle:</p> <ul> <li><code>turn_received</code></li> <li><code>purpose_registered</code></li> <li><code>subscriptions_changed</code></li> <li><code>purpose_fired</code></li> <li><code>delta_merged</code></li> <li><code>purpose_complete</code></li> </ul> <p>Out-of-band annotation:</p> <ul> <li><code>turn_observation_recorded</code></li> </ul>"},{"location":"historical/ttt_architecture_v0_12/#43-provider-registration-and-subscriptions-hub-managed","title":"4.3 Provider registration and subscriptions (hub-managed)","text":"<p>Although the HubEvent stream remains the authoritative record, TTT supports a provider registration layer to make the system extensible and safe across process boundaries.</p>"},{"location":"historical/ttt_architecture_v0_12/#431-tttregister","title":"4.3.1 TTT.register()","text":"<p>A Purpose registers itself with the hub by providing:</p> <ul> <li><code>purpose_id: str</code> (stable identifier; used as the purpose namespace)</li> <li><code>subscriptions: list[str]</code> (event patterns; may include globs, e.g., <code>Events.*</code>)</li> <li>optional <code>capabilities</code> (e.g., whether it can emit out-of-band observations)</li> </ul> <p>The hub returns a purpose token:</p> <ul> <li><code>purpose_token: str</code> \u2014 24-char hex (<code>secrets.token_hex(12)</code>)</li> </ul> <p>The purpose token is then attached to any purpose-originating calls back into the hub (e.g., posting a Delta, requesting subscription changes). This is an identity and trust-boundary mechanism; it is not intended to be cryptographic security in hostile settings.</p>"},{"location":"historical/ttt_architecture_v0_12/#432-subscription-patterns","title":"4.3.2 Subscription patterns","text":"<p>Subscriptions are matched against <code>HubEvent.event_type</code> (and optionally other routing keys) using simple glob semantics.</p> <p>Examples: - <code>turn_received</code> - <code>delta_merged</code> - <code>Events.*</code> (all events) - <code>turn_observation_recorded</code></p> <p>A subscription may also include routing hints via payload keys (implementation-defined). If you want to subscribe to events that reference a specific turn, use <code>token_id</code>-scoped routing (the hub already emits <code>token_id</code> on turn-scoped events); do not rely on raw text matching.</p>"},{"location":"historical/ttt_architecture_v0_12/#433-changing-subscriptions-at-runtime","title":"4.3.3 Changing subscriptions at runtime","text":"<p>Extension seam: registration payloads MAY include <code>_meta</code> for future extensions; the hub MUST ignore unknown keys.</p> <p>Credential seam: <code>purpose_token</code> is passed out-of-band to <code>take_turn(...)</code> and is never included in HubEvents or persisted payloads.</p> <p>TTT supports dynamic subscription updates via a dedicated hub call:</p> <ul> <li><code>TTT.take_turn(ChangeSubscriptionsEvent(...))</code></li> </ul> <p>Payload shape (conceptual): - <code>purpose_id: str</code> - <code>purpose_token: str</code> - <code>delete_subs: list[str]</code> (optional) - <code>add_subs: list[str]</code> (optional)</p> <p>This call is only accepted if <code>purpose_token</code> matches the currently registered token for <code>purpose_id</code>. The hub applies the update atomically for that provider.</p> <p>Note: the strings inside <code>add_subs</code> / <code>delete_subs</code> are subscription patterns (e.g., <code>Events.*</code>), not turn identifiers. If you need turn scoping, the hub should filter/route based on the event\u2019s <code>token_id</code> (CTO turn_id) or a separate explicit filter mechanism.</p>"},{"location":"historical/ttt_architecture_v0_12/#434-purpose-identity-in-events","title":"4.3.4 Purpose identity in events","text":"<p>Purpose identity is carried as the purpose namespace (<code>purpose_id</code>) inside: - <code>Delta.purpose_id</code> - <code>Observation.owner</code> - and, when needed, in <code>HubEvent.payload[\"purpose_id\"]</code></p> <p>The purpose token is not intended to be written into every HubEvent payload; it is used to authenticate purpose-originating requests to the hub.</p>"},{"location":"historical/ttt_architecture_v0_12/#5-data-model","title":"5. Data Model","text":""},{"location":"historical/ttt_architecture_v0_12/#51-cto-canonical-turn-object","title":"5.1 CTO \u2014 Canonical Turn Object","text":"<p>The CTO is the stable per-turn state container. It has a minimal identity surface plus accumulating metadata.</p> <pre><code>@dataclass\nclass CTO:\n    turn_id:     str                      # 24-char hex; generated by hub per turn\n    timestamp:   float                    # unix epoch; hub sets if None\n    text:        str                      # raw turn text (verbatim)\n    role:        str                      # participant role label\n    observations: dict[str, list[Observation]] = field(default_factory=dict)\n</code></pre> <p>Semantics: - <code>cto.observations</code> is append-only. Every key maps to a list; the hub appends new <code>Observation</code> entries. - Provenance is reconstructed from the HubEvent stream.</p>"},{"location":"historical/ttt_architecture_v0_12/#52-observation-attributable-annotation-unit","title":"5.2 Observation \u2014 attributable annotation unit","text":"<pre><code>@dataclass\nclass Observation:\n    owner:  str   # purpose namespace (e.g. \"ue_detector\")\n    shared: bool  # True =&gt; readable by other providers; hub enforces immutability\n    value:  Any   # JSON-safe provider-defined content\n</code></pre> <p>Semantics: - Shared observations are write-once after first creation (hub enforces). - Owned/private observations may be updated by the owning provider only (hub enforces). - Provenance is still primarily a query over the event stream; the <code>owner/shared</code> fields support correctness and enforcement at write time.</p>"},{"location":"historical/ttt_architecture_v0_12/#53-delta-provider-mutation-record","title":"5.3 Delta \u2014 provider mutation record","text":"<p>Providers report work via a Delta, which the hub can merge into the CTO and record in the event stream.</p> <pre><code>@dataclass\nclass Delta:\n    source_turn_id: str          # CTO turn_id the delta was derived from\n    purpose_id:      str         # purpose namespace that produced this delta\n    invocation_id:   str | None  # hub-generated per dispatch (optional)\n    kind:            str         # purpose-defined (e.g. \"observation_added\")\n    payload:         dict[str,Any]# JSON-safe; convention: includes _schema and _v\n    timestamp:       float       # unix epoch at delta creation\n</code></pre> <p>Delta merge policy is implemented by the hub (the only component that may mutate <code>cto.observations</code>). The hub applies deterministic rules when merging incoming Deltas:</p> <p>-- Owned writes: when a delta carries an Observation intended for an owned   namespace the hub records the contribution on behalf of the owning purpose   following append\u2011only semantics. The hub never silently overwrites an   existing contribution; it records the new contribution and preserves history   for provenance. -- Shared appends: when a delta targets a shared namespace the hub appends   the contribution to the list maintained at that key. The hub enforces the   invariant that shared keys always map to lists and that all contributions   are appended rather than replacing prior entries. -- Merge semantics: the hub applies deterministic, documented merge behavior   (append-only by default). For structured payloads the hub may use explicit   merge functions, but these are always implemented to preserve event history   rather than discarding prior contributions. Higher\u2011level reconciliation and   domain-specific resolution remain the responsibility of downstream   components.</p> <p>These policies keep the core small and deterministic while enabling varied downstream strategies for reconciliation and enrichment.</p>"},{"location":"historical/ttt_architecture_v0_12/#54-dagnode-status-machine-eligibility","title":"5.4 DAGNode status machine (eligibility)","text":"<p>DAG nodes follow an explicit state machine:</p> <p><code>waiting \u2192 eligible \u2192 dispatched \u2192 resolved</code></p> <p>Eligibility checks must reflect <code>status == eligible</code> (hub-driven transitions).</p>"},{"location":"historical/ttt_architecture_v0_12/#6-ttt-internals","title":"6. TTT Internals","text":""},{"location":"historical/ttt_architecture_v0_12/#61-subscription-model-switch-with-wildcard-support","title":"6.1 Subscription model \u2014 Switch with wildcard support","text":"<p>TTT is a switch: subscribers declare which event types and/or purpose_ids they care about at registration time. TTT only forwards events to subscribers whose declared interests match.</p> <p>Wildcard subscriptions: <code>\"*\"</code> matches everything. <code>\"turn_*\"</code> matches all turn lifecycle events. <code>\"purpose_*\"</code> matches all Purpose events.</p> <p>TRTPurpose declares <code>subscriptions: [\"*\"]</code> \u2014 it receives all events regardless of type or purpose_id.</p> <p><code>private=True</code> means TTT does not forward <code>purpose_resolved</code> to other subscribers. TRTPurpose still receives it regardless of privacy setting.</p>"},{"location":"historical/ttt_architecture_v0_12/#62-authentication-trust-boundary-v06","title":"6.2 Authentication / trust boundary (v0.6)","text":"<p>TTT assumes the hub is the authority for lifecycle events and state mutation. Providers/Purposes are extensions that can compute and propose changes (Deltas) but do not directly mutate shared state without hub mediation.</p> <ul> <li>Providers register via <code>TTT.register(purpose_id, subscriptions)</code> and receive a <code>purpose_token</code> (<code>secrets.token_hex(12)</code>).</li> <li>Any purpose-originating request (posting a Delta, requesting subscription changes) must include <code>purpose_id</code> + <code>purpose_token</code>.</li> <li>The hub validates the token against its registry before accepting the request.</li> </ul> <p>This mechanism is primarily for correctness and accidental misuse prevention (e.g., preventing a misconfigured provider from impersonating another). If you need a hostile-environment security model, treat this as scaffolding and add cryptographic authentication at the transport layer.</p>"},{"location":"historical/ttt_architecture_v0_12/#63-dag-execution","title":"6.3 DAG execution","text":"<p>Purposes declare <code>depends_on: list[str]</code> in their <code>register</code> payload. TTT maintains a per-turn resolution graph and dispatches Purposes as dependencies are satisfied.</p> <pre><code>Turn arrives \u2192\n    embed (depends_on=[])             \u2192 dispatched immediately\n    ca    (depends_on=[])             \u2192 dispatched immediately\n    tm    (depends_on=[\"embed\",\"ca\"]) \u2192 waiting\n\nembed resolves \u2192 tm still waiting for ca\nca resolves    \u2192 tm now eligible \u2192 dispatched\ntm resolves    \u2192 DAG exhausted \u2192 turn_processing_complete\n</code></pre> <p>Independent Purposes run concurrently via <code>asyncio.gather</code>. Dependent Purposes await their prerequisites.</p>"},{"location":"historical/ttt_architecture_v0_12/#64-utilities-tttutils","title":"6.4 Utilities \u2014 <code>ttt.utils</code>","text":"<pre><code># Event stream reconstruction\ndef apply_delta(cto: CTO, delta: Delta, purpose_id: str) -&gt; CTO: ...\ndef reconstruct(turn_id: str, events: list[HubEvent]) -&gt; CTO: ...\n\n# Shared namespace resolution\ndef resolve_shared(\n    namespace: str,\n    events: list[HubEvent],\n    resolver: Callable[[list], Any]\n) -&gt; Any: ...\n\n# Provenance formatting\ndef format_provenance(event: HubEvent) -&gt; dict: ...\n# Returns a flat, serializable dict of provenance-relevant fields\n# for a single event. Callers serialize to JSON or log string as needed.\n# Example output:\n# {\n#   \"event_type\": \"purpose_resolved\",\n#   \"event_id\":   \"a3f7...\",\n#   \"timestamp\":  1234567890.123,\n#   \"turn_id\":    \"t_001\",\n#   \"purpose_id\": \"ca\",\n#   \"purpose_name\": \"Conversation Analysis Annotator\",\n#   \"entries_added\": 2,\n#   \"shared_keys_updated\": []\n# }\n</code></pre> <p><code>format_provenance</code> takes one event and returns a flat dict. It knows which fields are meaningful for provenance across event types and normalizes them into a consistent shape. Backends call it when writing log entries. Calling code can call it directly.</p>"},{"location":"historical/ttt_architecture_v0_12/#7-turnreturn-and-trtpurpose","title":"7. TurnReTurn and TRTPurpose","text":"<p>TurnReTurn is the persistence, retrieval, formatting, and output product. It is the brand and the system.</p> <p>TRTPurpose is TurnReTurn's TTT integration point \u2014 a <code>PurposeProtocol</code> implementation that plugs into the DAG and feeds TurnReTurn's Stores. Someone who wants TurnReTurn's persistence features but has their own event infrastructure can use TurnReTurn without TRTPurpose.</p> <pre><code>class TRTPurpose:\n    purpose_id = \"trt\"\n    purpose_name = \"TurnReTurn Persistence\"\n    # registered with: private=True, depends_on=[], subscriptions=[\"*\"]\n\n    def register_store(self, store: StoreProtocol) -&gt; None: ...\n    async def take_turn(self, event: HubEvent) -&gt; None: ...\n</code></pre> <p>TRTPurpose is also the host for query views \u2014 since it holds the durable event stream, provenance, session replay, and Purpose audit are naturally expressed as queries against it.</p> <pre><code># Query views (available when a queryable Store is registered)\ndef provenance(self, turn_id: str) -&gt; list[dict]: ...\ndef session_replay(self, session_id: str) -&gt; list[HubEvent]: ...\ndef purpose_audit(self, session_id: str) -&gt; list[dict]: ...\n</code></pre>"},{"location":"historical/ttt_architecture_v0_12/#71-store-and-backend-layering","title":"7.1 Store and Backend layering","text":"<p>A Store is the logical persistence target. A Backend is the specific driver implementing a Store. This mirrors SQLAlchemy's dialect pattern.</p> <pre><code>TurnReTurn\n    \u2514\u2500\u2500 TRTPurpose\n    \u2514\u2500\u2500 Stores\n          \u251c\u2500\u2500 FileStore\n          \u2502     \u251c\u2500\u2500 JSONLBackend\n          \u2502     \u251c\u2500\u2500 JSONBackend\n          \u2502     \u2514\u2500\u2500 PlainTextBackend\n          \u251c\u2500\u2500 MemoryStore\n          \u2502     \u2514\u2500\u2500 MessageDictBackend\n          \u2514\u2500\u2500 SQLStore\n                \u251c\u2500\u2500 SQLiteBackend\n                \u2514\u2500\u2500 PostgresBackend\n</code></pre> <p>Multiple Stores can be registered simultaneously. TRTPurpose fans out to all of them on each event.</p>"},{"location":"historical/ttt_architecture_v0_12/#8-package-structure","title":"8. Package Structure","text":"<pre><code>ttt/                          # pip install ttt\n    core/\n        cto.py                # CTO, Delta\n        events.py             # HubEvent, well-known event_type constants\n        protocols.py          # TurnTakerProtocol, PurposeProtocol,\n                              # StoreProtocol\n        hub.py                # TTT hub: DAG resolver, switch, token auth\n        utils.py              # apply_delta, reconstruct, resolve_shared,\n                              # format_provenance\n\ntrt/                          # pip install ttt[persist]\n    purpose.py                # TRTPurpose\n    stores/\n        file_store.py         # FileStore\n        memory_store.py       # MemoryStore\n        backends/\n            jsonl.py          # JSONLBackend (under FileStore)\n            json_.py          # JSONBackend (under FileStore)\n            plaintext.py      # PlainTextBackend (under FileStore)\n            messagedict.py    # MessageDictBackend (under MemoryStore)\n\ntrt_sql/                      # pip install ttt[persist-sql]\n    stores/\n        sql_store.py          # SQLStore\n        backends/\n            sqlite.py         # SQLiteBackend\n            postgres.py       # PostgresBackend\n</code></pre>"},{"location":"historical/ttt_architecture_v0_12/#9-example-integration-adjacency-as-one-client","title":"9. Example Integration (Adjacency as one client)","text":"<p>Adjacency is one consumer of TTT, but it is not architecturally special. In TTT terms:</p> <ul> <li>The Adjacency session runner can be modeled as a Purpose that emits <code>turn_observed</code> events via <code>take_turn</code>.</li> <li>Adjacency-specific analyzers are simply additional Purposes that subscribe to relevant events and propose observations.</li> <li>Feedback loops (e.g., a <code>speaker_feedback</code> event) are implemented as subscriptions: calling code receives feedback the same way any other Purpose does.</li> </ul> <p>Adjacency\u2019s existing session log (JSONL) becomes a TurnReTurn FileStore output rather than a separately managed artifact. The log format can be preserved; the machinery producing it is replaced by TRTPurpose + JSONLBackend.</p>"},{"location":"historical/ttt_architecture_v0_12/#10-installation","title":"10. Installation","text":"<pre><code>pip install ttt                  # core only \u2014 no storage dependencies\npip install ttt[persist]         # + TurnReTurn with FileStore, MemoryStore\npip install ttt[persist-sql]     # + SQLStore with SQLite and Postgres backends\n</code></pre>"},{"location":"historical/ttt_architecture_v0_12/#11-what-ttt-is-not","title":"11. What TTT Is Not","text":"<ul> <li>Not an API gateway or HTTP proxy</li> <li>Not an LLM alignment system</li> <li>Not an observability platform (though it can feed one via a Store)</li> <li>Not a replacement for OpenTelemetry, Langfuse, or Phoenix \u2014 it can emit   to any of these via a Store or subscriber</li> <li>Not an actor framework \u2014 it uses actor model concepts implemented directly   on asyncio</li> </ul>"},{"location":"historical/ttt_architecture_v0_12/#appendix-a-reference-material-from-earlier-document","title":"Appendix A \u2014 Reference Material from Earlier Document","text":"<p>The following sections from Alignment Middleware for Human\u2013LLM Dialogue (March 2026) remain useful as reference for Purpose implementations that would run on top of TTT. They are preserved here without modification.</p>"},{"location":"historical/ttt_architecture_v0_12/#a1-trace-candidate-schema","title":"A.1 Trace Candidate Schema","text":"<p>Relevant to a TraceMutation Purpose implementation.</p> <pre><code>trace_candidate:\n  trace_id: ...\n  span: {message_id, char_start, char_end}\n  type: [decision|constraint|definition|plan|ownership|other]\n  owner: [user|model|joint|unknown]\n  text: ...\n  normalized: ...\n  timestamp: ...\n  confidence: 0..1\n</code></pre>"},{"location":"historical/ttt_architecture_v0_12/#a2-alignment-signals-for-an-alignment-purpose","title":"A.2 Alignment Signals (for an Alignment Purpose)","text":"<ol> <li>User-constraint preservation score \u2014 compare embeddings of current    model response vs nearest constraint traces; low similarity + high    confidence restatement =&gt; risk.</li> <li>Negation risk flag (UE-leaning) \u2014 detect negation operators in user    traces; if model restates without negation or with polarity flip, raise    risk.</li> <li>Provenance compression score (GD-leaning) \u2014 track pronoun/possessive    shifts around accountability moments (\"we/our\" \u2192 \"you/your\").</li> <li>Commitment continuity score \u2014 compare \"what we decided\" summaries    across time; detect discontinuous jumps.</li> </ol>"},{"location":"historical/ttt_architecture_v0_12/#a3-mutation-candidate-event-schema-for-a-tracemutation-purpose","title":"A.3 Mutation Candidate Event Schema (for a TraceMutation Purpose)","text":"<pre><code>mutation_candidate:\n  kind: [UE|GD|unknown]\n  source_trace_id: ...\n  degenerate_trace_span: ...\n  evidence:\n    - similarity_drop: ...\n    - contradiction_score: ...\n    - provenance_shift: ...\n  severity: [low|med|high]\n  proposed_action: ...\n</code></pre>"},{"location":"historical/ttt_architecture_v0_12/#a4-decisioncard-provenance-accumulator","title":"A.4 DecisionCard / Provenance Accumulator","text":"<p>A DecisionCard Purpose would separate (1) append-only provenance from (2) replaceable resolved snapshots, supporting deterministic re-resolution when evidence evolves.</p> <p>Key design invariants: - CTO is verbatim-first and immutable (enforced by TTT core) - Provenance and annotations are append-only and span-referenced - \"Current state\" is always a materialized view with explicit <code>resolver_id</code>   and <code>evidence_bundle_id</code>, so it can be re-resolved deterministically</p>"},{"location":"historical/ttt_architecture_v0_12/#a5-alignment-snapshot-contents-for-an-alignment-purpose","title":"A.5 Alignment Snapshot Contents (for an Alignment Purpose)","text":"<ul> <li><code>snapshot_id</code>, <code>turn_id</code>, <code>decision_ids</code> touched</li> <li><code>anchor_trace_ids</code> (constraints/definitions/commitments)</li> <li><code>alignment_scores</code> (per-signal + composite)</li> <li><code>state_summary_text</code> (compact) + <code>state_summary_embedding</code></li> <li><code>recent_drift_delta</code> (change from last snapshot)</li> </ul>"},{"location":"historical/ttt_architecture_v0_12/#a6-llmz-guidance-packet-for-a-guidance-purpose","title":"A.6 LLMZ Guidance Packet (for a Guidance Purpose)","text":"<pre><code>[LLMZ]\nGOAL: preserve shared-record integrity; avoid unilateral restatement of\n      user constraints.\nANCHORS:\n- (trace_id=...) \"constraint text\" [MUST-PRESERVE]\nCHECKS:\n- Before proposing plan, restate anchored clauses verbatim.\n- Do not negate deferred commitments.\nREPAIR:\n- If conflict detected, ask one clarification Q; otherwise silently align.\n[/LLMZ]\n</code></pre>"},{"location":"historical/ttt_architecture_v0_12/#a7-integration-roadmap-adapted","title":"A.7 Integration Roadmap (adapted)","text":"<p>Phase 1 \u2014 Working pipe - TTT core + TRTPurpose + JSONLBackend - Adjacency session runner emitting <code>turn_observed</code> events - DAG execution with zero-dependency Purposes</p> <p>Phase 2 \u2014 Trace-aware - TraceMutation Purpose (heuristic first pass) - Embedding Purpose (SBERT + FAISS in-process) - CA annotation Purpose - DecisionCard Purpose (append-only evidence accumulation) - Shared namespace usage for open CA schema contributions</p> <p>Phase 3 \u2014 Guided - Alignment signal Purpose - <code>speaker_feedback</code> \u2192 Adjacency session runner loop - LLMZ packet generation Purpose - Offline replay and heavier analysis passes - Resolver Purposes for shared namespace canonicalization</p> <p>TTT Architecture v0.4 \u2014 March 2026 Drafted from design session. Companion document: Adjacency ADR v1.0.</p> <p>v0.4 changes from v0.3: - <code>ttt.register()</code> as convenience wrapper clarified \u2014 <code>take_turn</code> is the one true path; <code>register()</code> constructs and dispatches internally - <code>subscription_update</code> event added for mid-session subscription changes - <code>purpose_id</code> removed from <code>purpose_complete</code> payload \u2014 resolved from token by TTT; prevents spoofing - <code>format_provenance(event) -&gt; dict</code> added to <code>ttt.utils</code>; per-event, flat, serializable, caller serializes to JSON or log string - <code>provenance()</code> list traversal utility removed from <code>ttt.utils</code> \u2014 provenance is a TurnReTurn query view, not a core utility - Design principles updated: <code>take_turn</code> is the one true path</p>"},{"location":"historical/ttt_architecture_v0_12/#addendum-e-postgres-substrate-authoritative-log-projections","title":"Addendum E \u2014 Postgres Substrate (Authoritative Log + Projections)","text":"<p>This addendum specifies a minimal Postgres-first persistence layout consistent with the core invariants:</p> <ul> <li>Append-only authoritative record (no overwrites; replayable)</li> <li>Purpose-run tokens (<code>purpose_token</code>) are run-scoped and can span many turns/sessions</li> <li>CTO is per-turn and identified by <code>(conversation_id, turn_index)</code> (or an optional <code>turn_id</code>)</li> <li>Extensibility is achieved with JSONB payloads + selective indexing (no schema churn)</li> </ul>"},{"location":"historical/ttt_architecture_v0_12/#e1-identity-model","title":"E.1 Identity model","text":""},{"location":"historical/ttt_architecture_v0_12/#e11-conversation-turn-identity-recommended","title":"E.1.1 Conversation + Turn identity (recommended)","text":"<ul> <li><code>conversation_id</code> \u2014 UUID (or 64\u201396-bit hex string) identifying a conversation/session grouping</li> <li><code>turn_index</code> \u2014 integer, monotonically increasing within <code>conversation_id</code></li> </ul> <p>Primary key: - <code>(conversation_id, turn_index)</code></p> <p>Optional convenience: - <code>turn_uid</code> computed as <code>conversation_id || ':' || turn_index</code></p>"},{"location":"historical/ttt_architecture_v0_12/#e12-purpose-run-identity","title":"E.1.2 Purpose-run identity","text":"<ul> <li><code>purpose_id</code> \u2014 stable identifier for the Purpose type (e.g., <code>\"CA\"</code>, <code>\"embeddingizer\"</code>, <code>\"TRTPurpose\"</code>)</li> <li><code>purpose_token</code> \u2014 24-char hex string returned by <code>TTT.register()</code> and used to authenticate Purpose-originating requests</li> </ul> <p>A single <code>purpose_token</code> may cover: - many turns, - and multiple conversation sessions, depending on how you scope \u201crun.\u201d</p>"},{"location":"historical/ttt_architecture_v0_12/#e2-tables-minimal-viable","title":"E.2 Tables (minimal viable)","text":""},{"location":"historical/ttt_architecture_v0_12/#e21-hub_events-authoritative-append-only-log","title":"E.2.1 <code>hub_events</code> \u2014 authoritative append-only log","text":"<p>The event stream is the source of truth. Everything else can be derived.</p> <pre><code>create table hub_events (\n  event_id        bigserial primary key,\n  event_time      timestamptz not null default now(),\n\n  -- Routing / grouping\n  conversation_id uuid null,\n  turn_index      int null,\n  purpose_id      text null,\n\n  -- Core event type (enum or text)\n  event_type      text not null,\n\n  -- JSONB payload for event-specific content\n  payload         jsonb not null,\n\n  -- Integrity\n  payload_hash    bytea null\n);\n</code></pre> <p>Notes: - <code>conversation_id</code>/<code>turn_index</code> are nullable to support non-turn-scoped events (e.g., <code>purpose_registered</code>, <code>subscriptions_changed</code>). - <code>payload</code> is JSONB and should include <code>_schema</code> and <code>_v</code> when the event\u2019s internal structure matters. - You can add <code>purpose_token</code> to <code>payload</code> for requests, but do not store it on accepted events unless you need an audit trail; typically you store only <code>purpose_id</code> and keep <code>purpose_token</code> in the registry table.</p>"},{"location":"historical/ttt_architecture_v0_12/#e22-ctos-per-turn-state-snapshot-optional-but-practical","title":"E.2.2 <code>ctos</code> \u2014 per-turn state snapshot (optional but practical)","text":"<p>This is a materialized convenience table. It is allowed to be \u201clatest view\u201d because the authoritative record is still <code>hub_events</code>.</p> <pre><code>create table ctos (\n  conversation_id uuid not null,\n  turn_index      int not null,\n\n  -- stable per-turn fields\n  created_at      timestamptz not null default now(),\n  speaker_role    text not null,         -- \"human\" | \"model\" | \"system\" (or richer labels)\n  text            text not null,         -- raw turn text (verbatim)\n  text_hash       bytea null,\n\n  -- observations are append-only lists per key\n  observations    jsonb not null default '{}'::jsonb,\n\n  primary key (conversation_id, turn_index)\n);\n</code></pre> <p>Observations encoding: - <code>observations</code> is a JSON object keyed by observation key; each value is a JSON array of Observation objects:   - <code>[{ \"owner\": \"...\", \"shared\": true, \"value\": ... }, ...]</code></p> <p>This matches the \u201cevery value is a list; hub appends\u201d invariant.</p>"},{"location":"historical/ttt_architecture_v0_12/#e23-purpose_runs-registry-for-purpose-tokens-and-subscriptions","title":"E.2.3 <code>purpose_runs</code> \u2014 registry for Purpose tokens and subscriptions","text":"<pre><code>create table purpose_runs (\n  purpose_id      text not null,\n  purpose_token   char(24) not null,\n\n  -- run scope / lifecycle\n  created_at      timestamptz not null default now(),\n  last_seen_at    timestamptz null,\n  is_active       boolean not null default true,\n\n  -- subscriptions expressed as patterns; stored as jsonb array of strings\n  subscriptions   jsonb not null default '[]'::jsonb,\n\n  primary key (purpose_id, purpose_token)\n);\n</code></pre> <p>Notes: - This table is the trust boundary: hub validates <code>(purpose_id, purpose_token)</code> for Purpose-originating requests. - If you later want \u201cone active token per purpose_id,\u201d enforce via a partial unique index on <code>purpose_id where is_active</code>.</p>"},{"location":"historical/ttt_architecture_v0_12/#e3-indexes-day-1","title":"E.3 Indexes (day 1)","text":"<p>For replay and common queries:</p> <pre><code>create index hub_events_conv_turn_time\n  on hub_events (conversation_id, turn_index, event_time);\n\ncreate index hub_events_type_time\n  on hub_events (event_type, event_time);\n\n-- If you frequently query by purpose_id (e.g., debugging a Purpose)\ncreate index hub_events_purpose_time\n  on hub_events (purpose_id, event_time);\n</code></pre> <p>For JSONB filtering (only add when needed):</p> <pre><code>-- Example: find events whose payload contains a key/value pattern\ncreate index hub_events_payload_gin\n  on hub_events using gin (payload);\n</code></pre> <p>For CTO retrieval:</p> <pre><code>create index ctos_conv_time\n  on ctos (conversation_id, created_at);\n</code></pre>"},{"location":"historical/ttt_architecture_v0_12/#e4-replay-model","title":"E.4 Replay model","text":"<p>Replay is a query over <code>hub_events</code>:</p> <ul> <li>\u201cReplay a conversation\u201d: <code>where conversation_id = ... order by turn_index, event_time, event_id</code></li> <li>\u201cReplay a turn\u201d: <code>where conversation_id = ... and turn_index = ... order by event_time, event_id</code></li> </ul> <p>Because the authoritative stream is append-only, strict ordering requirements are minimal. Recommended tie-break: - <code>event_time</code>, then <code>event_id</code> (monotone).</p>"},{"location":"historical/ttt_architecture_v0_12/#e5-projections-derived-consumers","title":"E.5 Projections (derived consumers)","text":"<p>Treat everything beyond the core as derived projections:</p> <ul> <li>DecisionCard materialization (from DecisionMove events)</li> <li>Alignment snapshots</li> <li>Trace-probe / adjacency annotations</li> <li>Search indices</li> </ul> <p>Each projection: - reads from <code>hub_events</code> (and optionally <code>ctos</code>), - writes to its own tables/materialized views, - can be re-run as rules evolve.</p>"},{"location":"historical/ttt_architecture_v0_12/#e6-when-to-add-a-vector-index-optional","title":"E.6 When to add a vector index (optional)","text":"<p>When you need semantic retrieval at scale: - store embeddings out-of-row (vector DB or Postgres pgvector), - store only embedding references in event payloads and/or CTO observations.</p> <p>If you want to stay Postgres-only, pgvector is a reasonable next step; otherwise keep the vector index as a separate projection.</p>"},{"location":"historical/ttt_architecture_v0_13/","title":"TurnTurnTurn (TTT) \u2014 Architecture Document","text":""},{"location":"historical/ttt_architecture_v0_13/#version-011-march-2026","title":"Version 0.11 \u2014 March 2026","text":""},{"location":"historical/ttt_architecture_v0_13/#source-design-session-between-bill-and-claude-anthropic","title":"Source: Design session between Bill and Claude (Anthropic)","text":"<p>v0.6 note: Reinstates the provider registration + subscriptions layer from v0.4 as an explicit hub capability (TTT.register / ChangeSubscriptionsEvent) while keeping the v0.2 core dataclasses (CTO/Observation/Delta) and HubEvent stream as the authoritative record.</p>"},{"location":"historical/ttt_architecture_v0_13/#0-background-and-motivation","title":"0. Background and Motivation","text":"<p>This document describes the architecture of TurnTurnTurn (TTT), a lightweight, domain-neutral Python middleware library for conversational turn processing. It emerged from design work on Adjacency (a structured elicitation research instrument) and the need for a reusable infrastructure layer that could serve that project and others.</p> <p>An earlier document (Alignment Middleware for Human\u2013LLM Dialogue) sketched a broader alignment and detection pipeline. TTT is not that system. TTT is the foundational layer that such a system would be built on \u2014 the part that handles canonical turn representation, enrichment, event routing, and persistence, without presupposing any particular analytical purpose.</p> <p>Sections of the earlier document that remain relevant \u2014 particularly the trace mutation detection signals, alignment snapshot design, DecisionCard/provenance accumulator, and the integration roadmap \u2014 are preserved in Appendix A of this document for reference. Those components are best understood as Purpose implementations that would run on top of TTT, not as architectural concerns of TTT itself.</p>"},{"location":"historical/ttt_architecture_v0_13/#1-design-principles","title":"1. Design Principles","text":"<p>Raw text is the substrate. The CTO is the unit of computation. Every turn becomes a Canonical Turn Object immediately on receipt. All downstream processing operates based on CTOs, never on raw strings.</p> <p>The event stream is the authoritative record. CTOs and Deltas travel on typed events. The event stream is what you replay to reconstruct any derived artifact. Full CTO observation state is always derivable from the event stream.</p> <p>Append-only. Never override; hub is authoritative. The CTO identity fields are immutable at construction. The <code>observations</code> dict is never written directly by Purposes or other external components \u2014 the hub is the sole authority that merges incoming Deltas into the CTO observation state. Purposes emit Deltas (events) describing intended changes; the hub receives those Deltas via the <code>take_turn</code> pathway, validates and merges them, and then emits HubEvents and dispatches follow-up purposes via the DAG.</p> <p>Owned vs shared namespaces. Each Purpose owns one owned namespace in <code>cto.observations</code>. Owned namespace entries are written by the hub on behalf of that purpose; other purposes may read those entries but may not propose writes to them. Purposes have no mechanism to propose updates outside their own owned namespace or the shared namespace \u2014 the hub enforces that rule. Shared namespaces are represented by lists maintained by the hub; when a purpose contributes to a shared key the hub appends the contribution to the list (<code>[a]</code> \u2192 <code>[a, b]</code>). The hub enforces the invariant that shared entries are lists and that owned entries are authored only by the hub acting for the owning purpose. The hub preserves append history and provenance via the event stream; higher\u2011level conflict resolution (semantics of merging structured payloads) is left to downstream resolvers when needed.</p> <p>Provenance is a query, not a field. Who contributed what, and when, is answerable from the event stream and HubEvents; the <code>observations</code> dict itself intentionally contains values only so that in\u2011memory CTOs are compact and easy to consume. TRTPurpose (and other adapters) persist events; provenance is reconstructed by querying that event log or by reading HubEvents emitted during processing.</p> <p>Domain\u2011neutral core. TTT knows nothing about domain semantics such as trace mutations, conversation analysis, or LLM alignment \u2014 those responsibilities belong to Purpose implementations. The core provides the event / DAG routing, merge semantics, and enforcement guarantees. The core ships with exactly one built\u2011in Purpose adapter: TRTPurpose (the TTT integration point for TurnReTurn).</p> <p><code>take_turn</code> is the one true path (routing &amp; dispatch). All inbound messages and lifecycle operations are expressed as events and enter the system through <code>take_turn</code>. <code>take_turn</code> is responsible for accepting an incoming event (Delta, registration, observation write request, etc.), validating it, handing it to the hub merge logic (which alone updates the CTO/observations), and then routing any resulting HubEvents through the DAG to subscribed Purposes. Convenience APIs such as <code>ttt.register()</code> are thin wrappers that construct and dispatch these events via <code>take_turn</code>.</p> <p>Loosely coupled via declared dependencies. Purposes declare what other Purposes they depend on. TTT manages execution order via a DAG resolver. Purposes never call each other directly.</p> <p>TTT is a domain-specific micro actor system built on asyncio. The architecture is conceptually an Actor model: every participant has a typed message interface, communicates via events, and does not know about other participants' implementations. Implemented directly on asyncio + dataclasses rather than an actor framework, which would add overhead without benefit at the expected scale.</p>"},{"location":"historical/ttt_architecture_v0_13/#2-conceptual-model","title":"2. Conceptual Model","text":"<pre><code>Calling code (e.g. Adjacency session runner)\n    \u2502\n    \u2502  ttt.register(purpose)          \u2190 convenience wrapper\n    \u2502  take_turn(HubEvent(\"register\")) \u2190 what actually happens\n    \u2502\n    \u2502  take_turn(HubEvent(\"turn_observed\", ...))\n    \u25bc\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502                TurnTurnTurn                  \u2502\n\u2502                                              \u2502\n\u2502  1. Construct CTO from turn_observed event   \u2502\n\u2502  2. Resolve DAG \u2014 find eligible Purposes     \u2502\n\u2502  3. Dispatch concurrently via asyncio        \u2502\n\u2502  4. Receive purpose_complete events back     \u2502\n\u2502  5. If delta present:                        \u2502\n\u2502     a. Validate token                        \u2502\n\u2502     b. Write purpose_data \u2192                  \u2502\n\u2502           observations[purpose_id]           \u2502\n\u2502     c. Append shared_data \u2192                  \u2502\n\u2502           observations[schema_id]            \u2502\n\u2502     d. Advance DAG \u2014 check new eligibles     \u2502\n\u2502  6. Emit purpose_resolved to subscribers     \u2502\n\u2502     (filtered by declared interest/wildcard) \u2502\n\u2502  7. When DAG exhausted:                      \u2502\n\u2502     emit turn_processing_complete            \u2502\n\u2502     or turn_processing_incomplete (timeout)  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n         \u2502                        \u25b2\n         \u2502 take_turn(HubEvent)    \u2502 take_turn(HubEvent)\n         \u25bc                        \u2502\n  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n  \u2502 TRTPurpose \u2502  \u2502  CAPurpose      \u2502  \u2502  TMPurpose    \u2502\n  \u2502 (built-in) \u2502  \u2502  depends_on=[]  \u2502  \u2502  depends_on=  \u2502\n  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518  \u2502  [\"embed\",\"ca\"]\u2502\n        \u2502                              \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n        \u2502                                      \u2502\n        \u25bc                              may emit speaker_feedback\n  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510          back to calling code\n  \u2502      TurnReTurn         \u2502\n  \u2502                         \u2502\n  \u2502  Stores:                \u2502\n  \u2502  \u251c\u2500\u2500 FileStore          \u2502\n  \u2502  \u2502   \u251c\u2500\u2500 JSONLBackend   \u2502\n  \u2502  \u2502   \u251c\u2500\u2500 JSONBackend    \u2502\n  \u2502  \u2502   \u2514\u2500\u2500 PlainTextBackend\u2502\n  \u2502  \u251c\u2500\u2500 MemoryStore        \u2502\n  \u2502  \u2502   \u2514\u2500\u2500 MessageDictBackend\u2502\n  \u2502  \u2514\u2500\u2500 SQLStore           \u2502\n  \u2502      \u251c\u2500\u2500 SQLiteBackend  \u2502\n  \u2502      \u2514\u2500\u2500 PostgresBackend\u2502\n  \u2502                         \u2502\n  \u2502  Query views:           \u2502\n  \u2502  \u251c\u2500\u2500 provenance()       \u2502\n  \u2502  \u251c\u2500\u2500 session_replay()   \u2502\n  \u2502  \u2514\u2500\u2500 purpose_audit()    \u2502\n  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"historical/ttt_architecture_v0_13/#25-applicability-beyond-any-single-use-case","title":"2.5 Applicability Beyond Any Single Use Case","text":"<p>TTT is not a pipeline framework. Because calling code is itself registered as a Purpose, TTT forms a peer mesh of Purposes coordinated by the hub:</p> <ul> <li>Every actor is a first-class node in the same event graph (no privileged \u201cupstream/downstream\u201d layer).</li> <li>Purposes do not call each other; they subscribe to events and propose observations via Deltas.</li> <li>The hub is the sole authority that merges proposed changes into <code>cto.observations</code>, preserving append history and provenance via the event stream.</li> </ul> <p>In practice, \u201cupstream/downstream\u201d is only a description of a particular topology (who subscribes to whom), not an architectural hierarchy.</p>"},{"location":"historical/ttt_architecture_v0_13/#251-what-this-unlocks","title":"2.5.1 What this unlocks","text":"<ul> <li>Composition &amp; orchestration: assemble complex behaviors from small, focused Purposes that each add a narrow observation set.</li> <li>Late binding: add/remove Purposes at runtime via registration/subscription changes without rewriting orchestration code.</li> <li>Cross-cutting concerns: privacy, policy checks, logging, and auditing can be implemented as separate Purposes that subscribe broadly.</li> <li>Multi-agent enrichment: multiple independent analyzers can contribute to shared keys (append-only lists) without coordination beyond schema contracts.</li> <li>Replay and re-analysis: new Purposes can be run over historical event logs to derive new artifacts without modifying prior records.</li> </ul>"},{"location":"historical/ttt_architecture_v0_13/#252-example-topologies","title":"2.5.2 Example topologies","text":"<p>These are illustrative \u201cstarter meshes\u201d that demonstrate the generality of CTO + event routing beyond any single domain.</p>"},{"location":"historical/ttt_architecture_v0_13/#topology-a-observability-evaluation-mesh-llm-app-debugging","title":"Topology A \u2014 Observability + Evaluation Mesh (LLM app debugging)","text":"<ul> <li>Calling code (as Purpose) emits <code>turn_observed</code> / lifecycle events.</li> <li><code>TRTPurpose</code> persists all HubEvents (JSONL/SQL).</li> <li><code>CostPurpose</code> annotates token usage / latency metrics.</li> <li><code>EvalPurpose</code> computes lightweight quality signals (e.g., semantic similarity checks) and appends evaluation observations.</li> <li><code>AuditPurpose</code> subscribes to <code>*</code> and emits human-readable audit summaries.</li> </ul> <p>Outcome: - A replayable event log plus per-turn \u201cquality/audit\u201d observations, without any LLM-specific semantics in the core.</p>"},{"location":"historical/ttt_architecture_v0_13/#topology-b-decision-ledger-mesh-decisioncards-as-projections","title":"Topology B \u2014 Decision Ledger Mesh (DecisionCards as projections)","text":"<ul> <li><code>DecisionExtractPurpose</code> proposes decision-move observations (append-only evidence).</li> <li><code>DecisionResolvePurpose</code> materializes a \u201ccurrent snapshot\u201d (replaceable view) from evidence.</li> <li><code>ReviewPurpose</code> (human-in-the-loop) subscribes to conflicts/unresolved items and appends review actions.</li> <li><code>TRTPurpose</code> stores everything; re-resolution can be run deterministically when rules evolve.</li> </ul> <p>Outcome: - DecisionCards become a downstream projection over the authoritative log; multiple extractors can coexist.</p>"},{"location":"historical/ttt_architecture_v0_13/#topology-c-federated-enrichment-mesh-edge-central","title":"Topology C \u2014 Federated Enrichment Mesh (edge \u2192 central)","text":"<ul> <li>Edge node runs <code>EmbedPurpose</code> / lightweight heuristics locally and emits Deltas/events.</li> <li>Central hub persists HubEvents and merges shared observations.</li> <li>Optional <code>SearchIndexPurpose</code> builds full-text indices; optional vector index is maintained as a separate projection.</li> <li>A <code>PolicyPurpose</code> enforces redaction/sanitization by proposing redaction observations (never overwriting raw text).</li> </ul> <p>Outcome: - Extensible enrichment with preserved provenance and the ability to recompute or augment annotations later.</p>"},{"location":"historical/ttt_architecture_v0_13/#253-what-purposes-must-agree-on-minimal-governance","title":"2.5.3 What Purposes must agree on (minimal governance)","text":"<p>To keep the peer mesh tractable, Purposes should follow two conventions:</p> <ul> <li>Schema/versioning: event payloads and observation values include <code>_schema</code> and <code>_v</code> when the shape matters.</li> <li>Merge expectations: shared keys are append-only lists; owned keys are written only by the hub on behalf of the owning Purpose.</li> </ul>"},{"location":"historical/ttt_architecture_v0_13/#3-core-protocols","title":"3. Core Protocols","text":""},{"location":"historical/ttt_architecture_v0_13/#31-turntakerprotocol","title":"3.1 TurnTakerProtocol","text":"<p>The fundamental interface. Everything that participates in the event stream implements it: Purposes, TTT itself, and calling code.</p> <pre><code>class TurnTakerProtocol(Protocol):\n    async def take_turn(self, event: HubEvent) -&gt; None: ...\n</code></pre> <p><code>take_turn</code> accepts a <code>HubEvent</code> and returns nothing. Participants that want to send results or feedback back to TTT call <code>ttt.take_turn(event)</code> directly. The interface is uniform across all participants \u2014 no special return types, no polymorphic signatures.</p>"},{"location":"historical/ttt_architecture_v0_13/#32-purposeprotocol","title":"3.2 PurposeProtocol","text":"<p>Extends <code>TurnTakerProtocol</code>. Purpose metadata is declared as class attributes and communicated to TTT via the <code>register</code> event.</p> <pre><code>class PurposeProtocol(TurnTakerProtocol, Protocol):\n    purpose_id: str      # stable machine identifier; becomes namespace key\n    purpose_name: str    # human-readable label for logging and audit views\n\n    async def take_turn(self, event: HubEvent) -&gt; None: ...\n</code></pre> <p>Registration metadata (<code>private</code>, <code>depends_on</code>, <code>subscriptions</code>) is carried in the <code>register</code> event payload. This allows the same Purpose implementation to be registered with different configurations in different sessions.</p>"},{"location":"historical/ttt_architecture_v0_13/#33-storeprotocol","title":"3.3 StoreProtocol","text":"<pre><code>class StoreProtocol(Protocol):\n    async def write(self, event: HubEvent) -&gt; None: ...\n    async def flush(self) -&gt; None: ...\n    async def query(self, **kwargs) -&gt; list[dict]: ...  # optional\n</code></pre>"},{"location":"historical/ttt_architecture_v0_13/#4-event-model","title":"4. Event Model","text":"<p>TTT/Adjacency now treats the hub-emitted event stream as the authoritative record of turn processing. Events are emitted on each hub state transition and are persisted by downstream stores (e.g., TurnReTurn backends).</p>"},{"location":"historical/ttt_architecture_v0_13/#41-hubevent-authoritative-event-record","title":"4.1 HubEvent (authoritative event record)","text":"<pre><code>@dataclass\nclass HubEvent:\n    event_id:   str            # 24-char hex (secrets.token_hex(12))\n    event_type: EventType      # Enum (pipeline + annotation)\n    turn_id:   str | None     # CTO turn_id when event is turn-scoped\n    payload:    dict[str, Any] # event-specific; convention: includes _schema and _v\n    timestamp:  float          # unix epoch\n</code></pre> <p>Token terminology: - <code>turn_id</code> identifies a single CTO (per turn). - <code>purpose_token</code> identifies a registered Purpose run (may span many turns/sessions).</p> <p>Events are intentionally small and JSON-safe (or trivially serializable via <code>to_dict()</code> on embedded objects). Payloads should follow the <code>_schema</code> + <code>_v</code> convention for future migration/debugging.</p>"},{"location":"historical/ttt_architecture_v0_13/#42-eventtype-vocabulary-aligned-to-dataclasses-v05","title":"4.2 EventType vocabulary (aligned to dataclasses v0.5)","text":"<p>Pipeline (DAG) lifecycle:</p> <ul> <li><code>turn_received</code></li> <li><code>purpose_registration</code></li> <li><code>subscriptions_changed</code></li> <li><code>purpose_fired</code></li> <li><code>delta_merged</code></li> <li><code>purpose_complete</code></li> </ul> <p>Out-of-band annotation:</p> <ul> <li><code>turn_observation_recorded</code></li> </ul>"},{"location":"historical/ttt_architecture_v0_13/#43-provider-registration-and-subscriptions-hub-managed","title":"4.3 Provider registration and subscriptions (hub-managed)","text":"<p>Although the HubEvent stream remains the authoritative record, TTT supports a provider registration layer to make the system extensible and safe across process boundaries.</p>"},{"location":"historical/ttt_architecture_v0_13/#431-tttregister","title":"4.3.1 TTT.register()","text":"<p>A Purpose registers itself with the hub by providing:</p> <ul> <li><code>purpose_id: str</code> (stable identifier; used as the purpose namespace)</li> <li><code>subscriptions: list[str]</code> (event patterns; may include globs, e.g., <code>Events.*</code>)</li> <li>optional <code>capabilities</code> (e.g., whether it can emit out-of-band observations)</li> </ul> <p>The hub returns a purpose token:</p> <ul> <li><code>purpose_token: str</code> \u2014 24-char hex (<code>secrets.token_hex(12)</code>)</li> </ul> <p>The purpose token is then attached to any purpose-originating calls back into the hub (e.g., posting a Delta, requesting subscription changes). This is an identity and trust-boundary mechanism; it is not intended to be cryptographic security in hostile settings.</p>"},{"location":"historical/ttt_architecture_v0_13/#432-subscription-patterns","title":"4.3.2 Subscription patterns","text":"<p>Subscriptions are matched against <code>HubEvent.event_type</code> (and optionally other routing keys) using simple glob semantics.</p> <p>Examples: - <code>turn_received</code> - <code>delta_merged</code> - <code>Events.*</code> (all events) - <code>turn_observation_recorded</code></p> <p>A subscription may also include routing hints via payload keys (implementation-defined). If you want to subscribe to events that reference a specific turn, use <code>token_id</code>-scoped routing (the hub already emits <code>token_id</code> on turn-scoped events); do not rely on raw text matching.</p>"},{"location":"historical/ttt_architecture_v0_13/#433-changing-subscriptions-at-runtime","title":"4.3.3 Changing subscriptions at runtime","text":"<p>TTT supports dynamic subscription updates via a dedicated hub call:</p> <ul> <li><code>TTT.take_turn(ChangeSubscriptionsEvent(...))</code></li> </ul> <p>Payload shape (conceptual): - <code>purpose_id: str</code> - <code>purpose_token: str</code> - <code>delete_subs: list[str]</code> (optional) - <code>add_subs: list[str]</code> (optional)</p> <p>This call is only accepted if <code>purpose_token</code> matches the currently registered token for <code>purpose_id</code>. The hub applies the update atomically for that provider.</p> <p>Note: the strings inside <code>add_subs</code> / <code>delete_subs</code> are subscription patterns (e.g., <code>Events.*</code>), not turn identifiers. If you need turn scoping, the hub should filter/route based on the event\u2019s <code>token_id</code> (CTO turn_id) or a separate explicit filter mechanism.</p>"},{"location":"historical/ttt_architecture_v0_13/#434-purpose-identity-in-events","title":"4.3.4 Purpose identity in events","text":"<p>Purpose identity is carried as the purpose namespace (<code>purpose_id</code>) inside: - <code>Delta.purpose_id</code> - <code>Observation.owner</code> - and, when needed, in <code>HubEvent.payload[\"purpose_id\"]</code></p> <p>The purpose token is not intended to be written into every HubEvent payload; it is used to authenticate purpose-originating requests to the hub.</p>"},{"location":"historical/ttt_architecture_v0_13/#5-data-model","title":"5. Data Model","text":""},{"location":"historical/ttt_architecture_v0_13/#51-cto-canonical-turn-object","title":"5.1 CTO \u2014 Canonical Turn Object","text":"<p>The CTO is the stable per-turn state container. It has a minimal identity surface plus accumulating metadata.</p> <pre><code>@dataclass\nclass CTO:\n    turn_id:     str                      # 24-char hex; generated by hub per turn\n    timestamp:   float                    # unix epoch; hub sets if None\n    text:        str                      # raw turn text (verbatim)\n    role:        str                      # participant role label\n    observations: dict[str, list[Observation]] = field(default_factory=dict)\n</code></pre> <p>Semantics: - <code>cto.observations</code> is append-only. Every key maps to a list; the hub appends new <code>Observation</code> entries. - Provenance is reconstructed from the HubEvent stream.</p>"},{"location":"historical/ttt_architecture_v0_13/#52-observation-attributable-annotation-unit","title":"5.2 Observation \u2014 attributable annotation unit","text":"<pre><code>@dataclass\nclass Observation:\n    owner:  str   # purpose namespace (e.g. \"ue_detector\")\n    shared: bool  # True =&gt; readable by other providers; hub enforces immutability\n    value:  Any   # JSON-safe provider-defined content\n</code></pre> <p>Semantics: - Shared observations are write-once after first creation (hub enforces). - Owned/private observations may be updated by the owning provider only (hub enforces). - Provenance is still primarily a query over the event stream; the <code>owner/shared</code> fields support correctness and enforcement at write time.</p>"},{"location":"historical/ttt_architecture_v0_13/#53-delta-provider-mutation-record","title":"5.3 Delta \u2014 provider mutation record","text":"<p>Providers report work via a Delta, which the hub can merge into the CTO and record in the event stream.</p> <pre><code>@dataclass\nclass Delta:\n    source_turn_id: str          # CTO turn_id the delta was derived from\n    purpose_id:      str         # purpose namespace that produced this delta\n    invocation_id:   str | None  # hub-generated per dispatch (optional)\n    kind:            str         # purpose-defined (e.g. \"observation_added\")\n    payload:         dict[str,Any]# JSON-safe; convention: includes _schema and _v\n    timestamp:       float       # unix epoch at delta creation\n</code></pre> <p>Delta merge policy is implemented by the hub (the only component that may mutate <code>cto.observations</code>). The hub applies deterministic rules when merging incoming Deltas:</p> <p>-- Owned writes: when a delta carries an Observation intended for an owned   namespace the hub records the contribution on behalf of the owning purpose   following append\u2011only semantics. The hub never silently overwrites an   existing contribution; it records the new contribution and preserves history   for provenance. -- Shared appends: when a delta targets a shared namespace the hub appends   the contribution to the list maintained at that key. The hub enforces the   invariant that shared keys always map to lists and that all contributions   are appended rather than replacing prior entries. -- Merge semantics: the hub applies deterministic, documented merge behavior   (append-only by default). For structured payloads the hub may use explicit   merge functions, but these are always implemented to preserve event history   rather than discarding prior contributions. Higher\u2011level reconciliation and   domain-specific resolution remain the responsibility of downstream   components.</p> <p>These policies keep the core small and deterministic while enabling varied downstream strategies for reconciliation and enrichment.</p>"},{"location":"historical/ttt_architecture_v0_13/#54-dagnode-status-machine-eligibility","title":"5.4 DAGNode status machine (eligibility)","text":"<p>DAG nodes follow an explicit state machine:</p> <p><code>waiting \u2192 eligible \u2192 dispatched \u2192 resolved</code></p> <p>Eligibility checks must reflect <code>status == eligible</code> (hub-driven transitions).</p>"},{"location":"historical/ttt_architecture_v0_13/#6-ttt-internals","title":"6. TTT Internals","text":""},{"location":"historical/ttt_architecture_v0_13/#61-subscription-model-switch-with-wildcard-support","title":"6.1 Subscription model \u2014 Switch with wildcard support","text":"<p>TTT is a switch: subscribers declare which event types and/or purpose_ids they care about at registration time. TTT only forwards events to subscribers whose declared interests match.</p> <p>Wildcard subscriptions: <code>\"*\"</code> matches everything. <code>\"turn_*\"</code> matches all turn lifecycle events. <code>\"purpose_*\"</code> matches all Purpose events.</p> <p>TRTPurpose declares <code>subscriptions: [\"*\"]</code> \u2014 it receives all events regardless of type or purpose_id.</p> <p><code>private=True</code> means TTT does not forward <code>purpose_resolved</code> to other subscribers. TRTPurpose still receives it regardless of privacy setting.</p>"},{"location":"historical/ttt_architecture_v0_13/#62-authentication-trust-boundary-v06","title":"6.2 Authentication / trust boundary (v0.6)","text":"<p>TTT assumes the hub is the authority for lifecycle events and state mutation. Providers/Purposes are extensions that can compute and propose changes (Deltas) but do not directly mutate shared state without hub mediation.</p> <ul> <li>Providers register via <code>TTT.register(purpose_id, subscriptions)</code> and receive a <code>purpose_token</code> (<code>secrets.token_hex(12)</code>).</li> <li>Any purpose-originating request (posting a Delta, requesting subscription changes) must include <code>purpose_id</code> + <code>purpose_token</code>.</li> <li>The hub validates the token against its registry before accepting the request.</li> </ul> <p>This mechanism is primarily for correctness and accidental misuse prevention (e.g., preventing a misconfigured provider from impersonating another). If you need a hostile-environment security model, treat this as scaffolding and add cryptographic authentication at the transport layer.</p>"},{"location":"historical/ttt_architecture_v0_13/#63-dag-execution","title":"6.3 DAG execution","text":"<p>Purposes declare <code>depends_on: list[str]</code> in their <code>register</code> payload. TTT maintains a per-turn resolution graph and dispatches Purposes as dependencies are satisfied.</p> <pre><code>Turn arrives \u2192\n    embed (depends_on=[])             \u2192 dispatched immediately\n    ca    (depends_on=[])             \u2192 dispatched immediately\n    tm    (depends_on=[\"embed\",\"ca\"]) \u2192 waiting\n\nembed resolves \u2192 tm still waiting for ca\nca resolves    \u2192 tm now eligible \u2192 dispatched\ntm resolves    \u2192 DAG exhausted \u2192 turn_processing_complete\n</code></pre> <p>Independent Purposes run concurrently via <code>asyncio.gather</code>. Dependent Purposes await their prerequisites.</p>"},{"location":"historical/ttt_architecture_v0_13/#64-utilities-tttutils","title":"6.4 Utilities \u2014 <code>ttt.utils</code>","text":"<pre><code># Event stream reconstruction\ndef apply_delta(cto: CTO, delta: Delta, purpose_id: str) -&gt; CTO: ...\ndef reconstruct(turn_id: str, events: list[HubEvent]) -&gt; CTO: ...\n\n# Shared namespace resolution\ndef resolve_shared(\n    namespace: str,\n    events: list[HubEvent],\n    resolver: Callable[[list], Any]\n) -&gt; Any: ...\n\n# Provenance formatting\ndef format_provenance(event: HubEvent) -&gt; dict: ...\n# Returns a flat, serializable dict of provenance-relevant fields\n# for a single event. Callers serialize to JSON or log string as needed.\n# Example output:\n# {\n#   \"event_type\": \"purpose_resolved\",\n#   \"event_id\":   \"a3f7...\",\n#   \"timestamp\":  1234567890.123,\n#   \"turn_id\":    \"t_001\",\n#   \"purpose_id\": \"ca\",\n#   \"purpose_name\": \"Conversation Analysis Annotator\",\n#   \"entries_added\": 2,\n#   \"shared_keys_updated\": []\n# }\n</code></pre> <p><code>format_provenance</code> takes one event and returns a flat dict. It knows which fields are meaningful for provenance across event types and normalizes them into a consistent shape. Backends call it when writing log entries. Calling code can call it directly.</p>"},{"location":"historical/ttt_architecture_v0_13/#7-turnreturn-and-trtpurpose","title":"7. TurnReTurn and TRTPurpose","text":"<p>TurnReTurn is the persistence, retrieval, formatting, and output product. It is the brand and the system.</p> <p>TRTPurpose is TurnReTurn's TTT integration point \u2014 a <code>PurposeProtocol</code> implementation that plugs into the DAG and feeds TurnReTurn's Stores. Someone who wants TurnReTurn's persistence features but has their own event infrastructure can use TurnReTurn without TRTPurpose.</p> <pre><code>class TRTPurpose:\n    purpose_id = \"trt\"\n    purpose_name = \"TurnReTurn Persistence\"\n    # registered with: private=True, depends_on=[], subscriptions=[\"*\"]\n\n    def register_store(self, store: StoreProtocol) -&gt; None: ...\n    async def take_turn(self, event: HubEvent) -&gt; None: ...\n</code></pre> <p>TRTPurpose is also the host for query views \u2014 since it holds the durable event stream, provenance, session replay, and Purpose audit are naturally expressed as queries against it.</p> <pre><code># Query views (available when a queryable Store is registered)\ndef provenance(self, turn_id: str) -&gt; list[dict]: ...\ndef session_replay(self, session_id: str) -&gt; list[HubEvent]: ...\ndef purpose_audit(self, session_id: str) -&gt; list[dict]: ...\n</code></pre>"},{"location":"historical/ttt_architecture_v0_13/#71-store-and-backend-layering","title":"7.1 Store and Backend layering","text":"<p>A Store is the logical persistence target. A Backend is the specific driver implementing a Store. This mirrors SQLAlchemy's dialect pattern.</p> <pre><code>TurnReTurn\n    \u2514\u2500\u2500 TRTPurpose\n    \u2514\u2500\u2500 Stores\n          \u251c\u2500\u2500 FileStore\n          \u2502     \u251c\u2500\u2500 JSONLBackend\n          \u2502     \u251c\u2500\u2500 JSONBackend\n          \u2502     \u2514\u2500\u2500 PlainTextBackend\n          \u251c\u2500\u2500 MemoryStore\n          \u2502     \u2514\u2500\u2500 MessageDictBackend\n          \u2514\u2500\u2500 SQLStore\n                \u251c\u2500\u2500 SQLiteBackend\n                \u2514\u2500\u2500 PostgresBackend\n</code></pre> <p>Multiple Stores can be registered simultaneously. TRTPurpose fans out to all of them on each event.</p>"},{"location":"historical/ttt_architecture_v0_13/#8-package-structure","title":"8. Package Structure","text":"<pre><code>ttt/                          # pip install ttt\n    core/\n        cto.py                # CTO, Delta\n        events.py             # HubEvent, well-known event_type constants\n        protocols.py          # TurnTakerProtocol, PurposeProtocol,\n                              # StoreProtocol\n        hub.py                # TTT hub: DAG resolver, switch, token auth\n        utils.py              # apply_delta, reconstruct, resolve_shared,\n                              # format_provenance\n\ntrt/                          # pip install ttt[persist]\n    purpose.py                # TRTPurpose\n    stores/\n        file_store.py         # FileStore\n        memory_store.py       # MemoryStore\n        backends/\n            jsonl.py          # JSONLBackend (under FileStore)\n            json_.py          # JSONBackend (under FileStore)\n            plaintext.py      # PlainTextBackend (under FileStore)\n            messagedict.py    # MessageDictBackend (under MemoryStore)\n\ntrt_sql/                      # pip install ttt[persist-sql]\n    stores/\n        sql_store.py          # SQLStore\n        backends/\n            sqlite.py         # SQLiteBackend\n            postgres.py       # PostgresBackend\n</code></pre>"},{"location":"historical/ttt_architecture_v0_13/#9-example-integration-adjacency-as-one-client","title":"9. Example Integration (Adjacency as one client)","text":"<p>Adjacency is one consumer of TTT, but it is not architecturally special. In TTT terms:</p> <ul> <li>The Adjacency session runner can be modeled as a Purpose that emits <code>turn_observed</code> events via <code>take_turn</code>.</li> <li>Adjacency-specific analyzers are simply additional Purposes that subscribe to relevant events and propose observations.</li> <li>Feedback loops (e.g., a <code>speaker_feedback</code> event) are implemented as subscriptions: calling code receives feedback the same way any other Purpose does.</li> </ul> <p>Adjacency\u2019s existing session log (JSONL) becomes a TurnReTurn FileStore output rather than a separately managed artifact. The log format can be preserved; the machinery producing it is replaced by TRTPurpose + JSONLBackend.</p>"},{"location":"historical/ttt_architecture_v0_13/#10-installation","title":"10. Installation","text":"<pre><code>pip install ttt                  # core only \u2014 no storage dependencies\npip install ttt[persist]         # + TurnReTurn with FileStore, MemoryStore\npip install ttt[persist-sql]     # + SQLStore with SQLite and Postgres backends\n</code></pre>"},{"location":"historical/ttt_architecture_v0_13/#11-what-ttt-is-not","title":"11. What TTT Is Not","text":"<ul> <li>Not an API gateway or HTTP proxy</li> <li>Not an LLM alignment system</li> <li>Not an observability platform (though it can feed one via a Store)</li> <li>Not a replacement for OpenTelemetry, Langfuse, or Phoenix \u2014 it can emit   to any of these via a Store or subscriber</li> <li>Not an actor framework \u2014 it uses actor model concepts implemented directly   on asyncio</li> </ul>"},{"location":"historical/ttt_architecture_v0_13/#appendix-a-reference-material-from-earlier-document","title":"Appendix A \u2014 Reference Material from Earlier Document","text":"<p>The following sections from Alignment Middleware for Human\u2013LLM Dialogue (March 2026) remain useful as reference for Purpose implementations that would run on top of TTT. They are preserved here without modification.</p>"},{"location":"historical/ttt_architecture_v0_13/#a1-trace-candidate-schema","title":"A.1 Trace Candidate Schema","text":"<p>Relevant to a TraceMutation Purpose implementation.</p> <pre><code>trace_candidate:\n  trace_id: ...\n  span: {message_id, char_start, char_end}\n  type: [decision|constraint|definition|plan|ownership|other]\n  owner: [user|model|joint|unknown]\n  text: ...\n  normalized: ...\n  timestamp: ...\n  confidence: 0..1\n</code></pre>"},{"location":"historical/ttt_architecture_v0_13/#a2-alignment-signals-for-an-alignment-purpose","title":"A.2 Alignment Signals (for an Alignment Purpose)","text":"<ol> <li>User-constraint preservation score \u2014 compare embeddings of current    model response vs nearest constraint traces; low similarity + high    confidence restatement =&gt; risk.</li> <li>Negation risk flag (UE-leaning) \u2014 detect negation operators in user    traces; if model restates without negation or with polarity flip, raise    risk.</li> <li>Provenance compression score (GD-leaning) \u2014 track pronoun/possessive    shifts around accountability moments (\"we/our\" \u2192 \"you/your\").</li> <li>Commitment continuity score \u2014 compare \"what we decided\" summaries    across time; detect discontinuous jumps.</li> </ol>"},{"location":"historical/ttt_architecture_v0_13/#a3-mutation-candidate-event-schema-for-a-tracemutation-purpose","title":"A.3 Mutation Candidate Event Schema (for a TraceMutation Purpose)","text":"<pre><code>mutation_candidate:\n  kind: [UE|GD|unknown]\n  source_trace_id: ...\n  degenerate_trace_span: ...\n  evidence:\n    - similarity_drop: ...\n    - contradiction_score: ...\n    - provenance_shift: ...\n  severity: [low|med|high]\n  proposed_action: ...\n</code></pre>"},{"location":"historical/ttt_architecture_v0_13/#a4-decisioncard-provenance-accumulator","title":"A.4 DecisionCard / Provenance Accumulator","text":"<p>A DecisionCard Purpose would separate (1) append-only provenance from (2) replaceable resolved snapshots, supporting deterministic re-resolution when evidence evolves.</p> <p>Key design invariants: - CTO is verbatim-first and immutable (enforced by TTT core) - Provenance and annotations are append-only and span-referenced - \"Current state\" is always a materialized view with explicit <code>resolver_id</code>   and <code>evidence_bundle_id</code>, so it can be re-resolved deterministically</p>"},{"location":"historical/ttt_architecture_v0_13/#a5-alignment-snapshot-contents-for-an-alignment-purpose","title":"A.5 Alignment Snapshot Contents (for an Alignment Purpose)","text":"<ul> <li><code>snapshot_id</code>, <code>turn_id</code>, <code>decision_ids</code> touched</li> <li><code>anchor_trace_ids</code> (constraints/definitions/commitments)</li> <li><code>alignment_scores</code> (per-signal + composite)</li> <li><code>state_summary_text</code> (compact) + <code>state_summary_embedding</code></li> <li><code>recent_drift_delta</code> (change from last snapshot)</li> </ul>"},{"location":"historical/ttt_architecture_v0_13/#a6-llmz-guidance-packet-for-a-guidance-purpose","title":"A.6 LLMZ Guidance Packet (for a Guidance Purpose)","text":"<pre><code>[LLMZ]\nGOAL: preserve shared-record integrity; avoid unilateral restatement of\n      user constraints.\nANCHORS:\n- (trace_id=...) \"constraint text\" [MUST-PRESERVE]\nCHECKS:\n- Before proposing plan, restate anchored clauses verbatim.\n- Do not negate deferred commitments.\nREPAIR:\n- If conflict detected, ask one clarification Q; otherwise silently align.\n[/LLMZ]\n</code></pre>"},{"location":"historical/ttt_architecture_v0_13/#a7-integration-roadmap-adapted","title":"A.7 Integration Roadmap (adapted)","text":"<p>Phase 1 \u2014 Working pipe - TTT core + TRTPurpose + JSONLBackend - Adjacency session runner emitting <code>turn_observed</code> events - DAG execution with zero-dependency Purposes</p> <p>Phase 2 \u2014 Trace-aware - TraceMutation Purpose (heuristic first pass) - Embedding Purpose (SBERT + FAISS in-process) - CA annotation Purpose - DecisionCard Purpose (append-only evidence accumulation) - Shared namespace usage for open CA schema contributions</p> <p>Phase 3 \u2014 Guided - Alignment signal Purpose - <code>speaker_feedback</code> \u2192 Adjacency session runner loop - LLMZ packet generation Purpose - Offline replay and heavier analysis passes - Resolver Purposes for shared namespace canonicalization</p> <p>TTT Architecture v0.4 \u2014 March 2026 Drafted from design session. Companion document: Adjacency ADR v1.0.</p> <p>v0.4 changes from v0.3: - <code>ttt.register()</code> as convenience wrapper clarified \u2014 <code>take_turn</code> is the one true path; <code>register()</code> constructs and dispatches internally - <code>subscription_update</code> event added for mid-session subscription changes - <code>purpose_id</code> removed from <code>purpose_complete</code> payload \u2014 resolved from token by TTT; prevents spoofing - <code>format_provenance(event) -&gt; dict</code> added to <code>ttt.utils</code>; per-event, flat, serializable, caller serializes to JSON or log string - <code>provenance()</code> list traversal utility removed from <code>ttt.utils</code> \u2014 provenance is a TurnReTurn query view, not a core utility - Design principles updated: <code>take_turn</code> is the one true path</p>"},{"location":"historical/ttt_architecture_v0_13/#addendum-e-postgres-substrate-authoritative-log-projections","title":"Addendum E \u2014 Postgres Substrate (Authoritative Log + Projections)","text":"<p>This addendum specifies a minimal Postgres-first persistence layout consistent with the core invariants:</p> <ul> <li>Append-only authoritative record (no overwrites; replayable)</li> <li>Purpose-run tokens (<code>purpose_token</code>) are run-scoped and can span many turns/sessions</li> <li>CTO is per-turn and identified by <code>(conversation_id, turn_index)</code> (or an optional <code>turn_id</code>)</li> <li>Extensibility is achieved with JSONB payloads + selective indexing (no schema churn)</li> </ul>"},{"location":"historical/ttt_architecture_v0_13/#e1-identity-model","title":"E.1 Identity model","text":""},{"location":"historical/ttt_architecture_v0_13/#e11-conversation-turn-identity-recommended","title":"E.1.1 Conversation + Turn identity (recommended)","text":"<ul> <li><code>conversation_id</code> \u2014 UUID (or 64\u201396-bit hex string) identifying a conversation/session grouping</li> <li><code>turn_index</code> \u2014 integer, monotonically increasing within <code>conversation_id</code></li> </ul> <p>Primary key: - <code>(conversation_id, turn_index)</code></p> <p>Optional convenience: - <code>turn_uid</code> computed as <code>conversation_id || ':' || turn_index</code></p>"},{"location":"historical/ttt_architecture_v0_13/#e12-purpose-run-identity","title":"E.1.2 Purpose-run identity","text":"<ul> <li><code>purpose_id</code> \u2014 stable identifier for the Purpose type (e.g., <code>\"CA\"</code>, <code>\"embeddingizer\"</code>, <code>\"TRTPurpose\"</code>)</li> <li><code>purpose_token</code> \u2014 24-char hex string returned by <code>TTT.register()</code> and used to authenticate Purpose-originating requests</li> </ul> <p>A single <code>purpose_token</code> may cover: - many turns, - and multiple conversation sessions, depending on how you scope \u201crun.\u201d</p>"},{"location":"historical/ttt_architecture_v0_13/#e2-tables-minimal-viable","title":"E.2 Tables (minimal viable)","text":""},{"location":"historical/ttt_architecture_v0_13/#e21-hub_events-authoritative-append-only-log","title":"E.2.1 <code>hub_events</code> \u2014 authoritative append-only log","text":"<p>The event stream is the source of truth. Everything else can be derived.</p> <pre><code>create table hub_events (\n  event_id        bigserial primary key,\n  event_time      timestamptz not null default now(),\n\n  -- Routing / grouping\n  conversation_id uuid null,\n  turn_index      int null,\n  purpose_id      text null,\n\n  -- Core event type (enum or text)\n  event_type      text not null,\n\n  -- JSONB payload for event-specific content\n  payload         jsonb not null,\n\n  -- Integrity\n  payload_hash    bytea null\n);\n</code></pre> <p>Notes: - <code>conversation_id</code>/<code>turn_index</code> are nullable to support non-turn-scoped events (e.g., <code>purpose_registration</code>, <code>subscriptions_changed</code>). - <code>payload</code> is JSONB and should include <code>_schema</code> and <code>_v</code> when the event\u2019s internal structure matters. - You can add <code>purpose_token</code> to <code>payload</code> for requests, but do not store it on accepted events unless you need an audit trail; typically you store only <code>purpose_id</code> and keep <code>purpose_token</code> in the registry table.</p>"},{"location":"historical/ttt_architecture_v0_13/#e22-ctos-per-turn-state-snapshot-optional-but-practical","title":"E.2.2 <code>ctos</code> \u2014 per-turn state snapshot (optional but practical)","text":"<p>This is a materialized convenience table. It is allowed to be \u201clatest view\u201d because the authoritative record is still <code>hub_events</code>.</p> <pre><code>create table ctos (\n  conversation_id uuid not null,\n  turn_index      int not null,\n\n  -- stable per-turn fields\n  created_at      timestamptz not null default now(),\n  speaker_role    text not null,         -- \"human\" | \"model\" | \"system\" (or richer labels)\n  text            text not null,         -- raw turn text (verbatim)\n  text_hash       bytea null,\n\n  -- observations are append-only lists per key\n  observations    jsonb not null default '{}'::jsonb,\n\n  primary key (conversation_id, turn_index)\n);\n</code></pre> <p>Observations encoding: - <code>observations</code> is a JSON object keyed by observation key; each value is a JSON array of Observation objects:   - <code>[{ \"owner\": \"...\", \"shared\": true, \"value\": ... }, ...]</code></p> <p>This matches the \u201cevery value is a list; hub appends\u201d invariant.</p>"},{"location":"historical/ttt_architecture_v0_13/#e23-purpose_runs-registry-for-purpose-tokens-and-subscriptions","title":"E.2.3 <code>purpose_runs</code> \u2014 registry for Purpose tokens and subscriptions","text":"<pre><code>create table purpose_runs (\n  purpose_id      text not null,\n  purpose_token   char(24) not null,\n\n  -- run scope / lifecycle\n  created_at      timestamptz not null default now(),\n  last_seen_at    timestamptz null,\n  is_active       boolean not null default true,\n\n  -- subscriptions expressed as patterns; stored as jsonb array of strings\n  subscriptions   jsonb not null default '[]'::jsonb,\n\n  primary key (purpose_id, purpose_token)\n);\n</code></pre> <p>Notes: - This table is the trust boundary: hub validates <code>(purpose_id, purpose_token)</code> for Purpose-originating requests. - If you later want \u201cone active token per purpose_id,\u201d enforce via a partial unique index on <code>purpose_id where is_active</code>.</p>"},{"location":"historical/ttt_architecture_v0_13/#e3-indexes-day-1","title":"E.3 Indexes (day 1)","text":"<p>For replay and common queries:</p> <pre><code>create index hub_events_conv_turn_time\n  on hub_events (conversation_id, turn_index, event_time);\n\ncreate index hub_events_type_time\n  on hub_events (event_type, event_time);\n\n-- If you frequently query by purpose_id (e.g., debugging a Purpose)\ncreate index hub_events_purpose_time\n  on hub_events (purpose_id, event_time);\n</code></pre> <p>For JSONB filtering (only add when needed):</p> <pre><code>-- Example: find events whose payload contains a key/value pattern\ncreate index hub_events_payload_gin\n  on hub_events using gin (payload);\n</code></pre> <p>For CTO retrieval:</p> <pre><code>create index ctos_conv_time\n  on ctos (conversation_id, created_at);\n</code></pre>"},{"location":"historical/ttt_architecture_v0_13/#e4-replay-model","title":"E.4 Replay model","text":"<p>Replay is a query over <code>hub_events</code>:</p> <ul> <li>\u201cReplay a conversation\u201d: <code>where conversation_id = ... order by turn_index, event_time, event_id</code></li> <li>\u201cReplay a turn\u201d: <code>where conversation_id = ... and turn_index = ... order by event_time, event_id</code></li> </ul> <p>Because the authoritative stream is append-only, strict ordering requirements are minimal. Recommended tie-break: - <code>event_time</code>, then <code>event_id</code> (monotone).</p>"},{"location":"historical/ttt_architecture_v0_13/#e5-projections-derived-consumers","title":"E.5 Projections (derived consumers)","text":"<p>Treat everything beyond the core as derived projections:</p> <ul> <li>DecisionCard materialization (from DecisionMove events)</li> <li>Alignment snapshots</li> <li>Trace-probe / adjacency annotations</li> <li>Search indices</li> </ul> <p>Each projection: - reads from <code>hub_events</code> (and optionally <code>ctos</code>), - writes to its own tables/materialized views, - can be re-run as rules evolve.</p>"},{"location":"historical/ttt_architecture_v0_13/#e6-when-to-add-a-vector-index-optional","title":"E.6 When to add a vector index (optional)","text":"<p>When you need semantic retrieval at scale: - store embeddings out-of-row (vector DB or Postgres pgvector), - store only embedding references in event payloads and/or CTO observations.</p> <p>If you want to stay Postgres-only, pgvector is a reasonable next step; otherwise keep the vector index as a separate projection.</p>"},{"location":"historical/ttt_architecture_v0_14/","title":"TurnTurnTurn (TTT) \u2014 Architecture Document","text":""},{"location":"historical/ttt_architecture_v0_14/#0-background-and-motivation","title":"0. Background and Motivation","text":"<p>This document describes the architecture of TurnTurnTurn (TTT), a lightweight, domain-neutral Python middleware library for conversational turn processing. It emerged from design work on Adjacency (a structured elicitation research instrument) and the need for a reusable infrastructure layer that could serve that project and others.</p> <p>An earlier document (Alignment Middleware for Human\u2013LLM Dialogue) sketched a broader alignment and detection pipeline. TTT is not that system. TTT is the foundational layer that such a system would be built on \u2014 the part that handles canonical turn representation, enrichment, event routing, and persistence, without presupposing any particular analytical purpose.</p> <p>Sections of the earlier document that remain relevant \u2014 particularly the trace mutation detection signals, alignment snapshot design, DecisionCard/provenance accumulator, and the integration roadmap \u2014 are preserved in Appendix A of this document for reference. Those components are best understood as Purpose implementations that would run on top of TTT, not as architectural concerns of TTT itself.</p>"},{"location":"historical/ttt_architecture_v0_14/#1-design-principles","title":"1. Design Principles","text":"<p>Raw text is the substrate. The CTO is the unit of computation. Every turn becomes a Canonical Turn Object immediately on receipt. All downstream processing operates based on CTOs, never on raw strings.</p> <p>The event stream is the authoritative record. CTOs and Deltas travel on typed events. The event stream is what you replay to reconstruct any derived artifact. Full CTO observation state is always derivable from the event stream.</p> <p>Append-only. Never override; hub is authoritative. The CTO identity fields are immutable at construction. The <code>observations</code> dict is never written directly by Purposes or other external components \u2014 the hub is the sole authority that merges incoming Deltas into the CTO observation state. Purposes emit Deltas (events) describing intended changes; the hub receives those Deltas via the <code>take_turn</code> pathway, validates and merges them, and then emits HubEvents and dispatches follow-up purposes via the DAG.</p> <p>Owned vs shared namespaces. Each Purpose owns one owned namespace in <code>cto.observations</code>. Owned namespace entries are written by the hub on behalf of that purpose; other purposes may read those entries but may not propose writes to them. Purposes have no mechanism to propose updates outside their own owned namespace or the shared namespace \u2014 the hub enforces that rule. Shared namespaces are represented by lists maintained by the hub; when a purpose contributes to a shared key the hub appends the contribution to the list (<code>[a]</code> \u2192 <code>[a, b]</code>). The hub enforces the invariant that shared entries are lists and that owned entries are authored only by the hub acting for the owning purpose. The hub preserves append history and provenance via the event stream; higher\u2011level conflict resolution (semantics of merging structured payloads) is left to downstream resolvers when needed.</p> <p>Provenance is a query, not a field. Who contributed what, and when, is answerable from the event stream and HubEvents; the <code>observations</code> dict itself intentionally contains values only so that in\u2011memory CTOs are compact and easy to consume. TRTPurpose (and other adapters) persist events; provenance is reconstructed by querying that event log or by reading HubEvents emitted during processing.</p> <p>Domain\u2011neutral core. TTT knows nothing about domain semantics such as trace mutations, conversation analysis, or LLM alignment \u2014 those responsibilities belong to Purpose implementations. The core provides the event / DAG routing, merge semantics, and enforcement guarantees. The core ships with exactly one built\u2011in Purpose adapter: TRTPurpose (the TTT integration point for TurnReTurn).</p> <p><code>take_turn</code> is the one true path (routing &amp; dispatch). All inbound messages and lifecycle operations are expressed as events and enter the system through <code>take_turn</code>. <code>take_turn</code> is responsible for accepting an incoming event (Delta, registration, observation write request, etc.), validating it, handing it to the hub merge logic (which alone updates the CTO/observations), and then routing any resulting HubEvents through the DAG to subscribed Purposes. Convenience APIs such as <code>ttt.register()</code> are thin wrappers that construct and dispatch these events via <code>take_turn</code>.</p> <p>Loosely coupled via declared dependencies. Purposes declare what other Purposes they depend on. TTT manages execution order via a DAG resolver. Purposes never call each other directly.</p> <p>TTT is a domain-specific micro actor system built on asyncio. The architecture is conceptually an Actor model: every participant has a typed message interface, communicates via events, and does not know about other participants' implementations. Implemented directly on asyncio + dataclasses rather than an actor framework, which would add overhead without benefit at the expected scale.</p>"},{"location":"historical/ttt_architecture_v0_14/#2-conceptual-model","title":"2. Conceptual Model","text":"<pre><code>Calling code (e.g. Adjacency session runner)\n    \u2502\n    \u2502  ttt.register(purpose)          \u2190 convenience wrapper\n    \u2502  take_turn(HubEvent(\"register\")) \u2190 what actually happens\n    \u2502\n    \u2502  take_turn(HubEvent(\"turn_observed\", ...))\n    \u25bc\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502                TurnTurnTurn                  \u2502\n\u2502                                              \u2502\n\u2502  1. Construct CTO from turn_observed event   \u2502\n\u2502  2. Resolve DAG \u2014 find eligible Purposes     \u2502\n\u2502  3. Dispatch concurrently via asyncio        \u2502\n\u2502  4. Receive purpose_complete events back     \u2502\n\u2502  5. If delta present:                        \u2502\n\u2502     a. Validate token                        \u2502\n\u2502     b. Write purpose_data \u2192                  \u2502\n\u2502           observations[purpose_id]           \u2502\n\u2502     c. Append shared_data \u2192                  \u2502\n\u2502           observations[schema_id]            \u2502\n\u2502     d. Advance DAG \u2014 check new eligibles     \u2502\n\u2502  6. Emit purpose_resolved to subscribers     \u2502\n\u2502     (filtered by declared interest/wildcard) \u2502\n\u2502  7. When DAG exhausted:                      \u2502\n\u2502     emit turn_processing_complete            \u2502\n\u2502     or turn_processing_incomplete (timeout)  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n         \u2502                        \u25b2\n         \u2502 take_turn(HubEvent)    \u2502 take_turn(HubEvent)\n         \u25bc                        \u2502\n  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n  \u2502 TRTPurpose \u2502  \u2502  CAPurpose      \u2502  \u2502  TMPurpose    \u2502\n  \u2502 (built-in) \u2502  \u2502  depends_on=[]  \u2502  \u2502  depends_on=  \u2502\n  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518  \u2502  [\"embed\",\"ca\"]\u2502\n        \u2502                              \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n        \u2502                                      \u2502\n        \u25bc                              may emit speaker_feedback\n  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510          back to calling code\n  \u2502      TurnReTurn         \u2502\n  \u2502                         \u2502\n  \u2502  Stores:                \u2502\n  \u2502  \u251c\u2500\u2500 FileStore          \u2502\n  \u2502  \u2502   \u251c\u2500\u2500 JSONLBackend   \u2502\n  \u2502  \u2502   \u251c\u2500\u2500 JSONBackend    \u2502\n  \u2502  \u2502   \u2514\u2500\u2500 PlainTextBackend\u2502\n  \u2502  \u251c\u2500\u2500 MemoryStore        \u2502\n  \u2502  \u2502   \u2514\u2500\u2500 MessageDictBackend\u2502\n  \u2502  \u2514\u2500\u2500 SQLStore           \u2502\n  \u2502      \u251c\u2500\u2500 SQLiteBackend  \u2502\n  \u2502      \u2514\u2500\u2500 PostgresBackend\u2502\n  \u2502                         \u2502\n  \u2502  Query views:           \u2502\n  \u2502  \u251c\u2500\u2500 provenance()       \u2502\n  \u2502  \u251c\u2500\u2500 session_replay()   \u2502\n  \u2502  \u2514\u2500\u2500 purpose_audit()    \u2502\n  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"historical/ttt_architecture_v0_14/#25-applicability-beyond-any-single-use-case","title":"2.5 Applicability Beyond Any Single Use Case","text":"<p>TTT is not a pipeline framework. Because calling code is itself registered as a Purpose, TTT forms a peer mesh of Purposes coordinated by the hub:</p> <ul> <li>Every actor is a first-class node in the same event graph (no privileged \u201cupstream/downstream\u201d layer).</li> <li>Purposes do not call each other; they subscribe to events and propose observations via Deltas.</li> <li>The hub is the sole authority that merges proposed changes into <code>cto.observations</code>, preserving append history and provenance via the event stream.</li> </ul> <p>In practice, \u201cupstream/downstream\u201d is only a description of a particular topology (who subscribes to whom), not an architectural hierarchy.</p>"},{"location":"historical/ttt_architecture_v0_14/#251-what-this-unlocks","title":"2.5.1 What this unlocks","text":"<ul> <li>Composition &amp; orchestration: assemble complex behaviors from small, focused Purposes that each add a narrow observation set.</li> <li>Late binding: add/remove Purposes at runtime via registration/subscription changes without rewriting orchestration code.</li> <li>Cross-cutting concerns: privacy, policy checks, logging, and auditing can be implemented as separate Purposes that subscribe broadly.</li> <li>Multi-agent enrichment: multiple independent analyzers can contribute to shared keys (append-only lists) without coordination beyond schema contracts.</li> <li>Replay and re-analysis: new Purposes can be run over historical event logs to derive new artifacts without modifying prior records.</li> </ul>"},{"location":"historical/ttt_architecture_v0_14/#252-example-topologies","title":"2.5.2 Example topologies","text":"<p>These are illustrative \u201cstarter meshes\u201d that demonstrate the generality of CTO + event routing beyond any single domain.</p>"},{"location":"historical/ttt_architecture_v0_14/#topology-a-observability-evaluation-mesh-llm-app-debugging","title":"Topology A \u2014 Observability + Evaluation Mesh (LLM app debugging)","text":"<ul> <li>Calling code (as Purpose) emits <code>turn_observed</code> / lifecycle events.</li> <li><code>TRTPurpose</code> persists all HubEvents (JSONL/SQL).</li> <li><code>CostPurpose</code> annotates token usage / latency metrics.</li> <li><code>EvalPurpose</code> computes lightweight quality signals (e.g., semantic similarity checks) and appends evaluation observations.</li> <li><code>AuditPurpose</code> subscribes to <code>*</code> and emits human-readable audit summaries.</li> </ul> <p>Outcome: - A replayable event log plus per-turn \u201cquality/audit\u201d observations, without any LLM-specific semantics in the core.</p>"},{"location":"historical/ttt_architecture_v0_14/#topology-b-decision-ledger-mesh-decisioncards-as-projections","title":"Topology B \u2014 Decision Ledger Mesh (DecisionCards as projections)","text":"<ul> <li><code>DecisionExtractPurpose</code> proposes decision-move observations (append-only evidence).</li> <li><code>DecisionResolvePurpose</code> materializes a \u201ccurrent snapshot\u201d (replaceable view) from evidence.</li> <li><code>ReviewPurpose</code> (human-in-the-loop) subscribes to conflicts/unresolved items and appends review actions.</li> <li><code>TRTPurpose</code> stores everything; re-resolution can be run deterministically when rules evolve.</li> </ul> <p>Outcome: - DecisionCards become a downstream projection over the authoritative log; multiple extractors can coexist.</p>"},{"location":"historical/ttt_architecture_v0_14/#topology-c-federated-enrichment-mesh-edge-central","title":"Topology C \u2014 Federated Enrichment Mesh (edge \u2192 central)","text":"<ul> <li>Edge node runs <code>EmbedPurpose</code> / lightweight heuristics locally and emits Deltas/events.</li> <li>Central hub persists HubEvents and merges shared observations.</li> <li>Optional <code>SearchIndexPurpose</code> builds full-text indices; optional vector index is maintained as a separate projection.</li> <li>A <code>PolicyPurpose</code> enforces redaction/sanitization by proposing redaction observations (never overwriting raw text).</li> </ul> <p>Outcome: - Extensible enrichment with preserved provenance and the ability to recompute or augment annotations later.</p>"},{"location":"historical/ttt_architecture_v0_14/#253-what-purposes-must-agree-on-minimal-governance","title":"2.5.3 What Purposes must agree on (minimal governance)","text":"<p>To keep the peer mesh tractable, Purposes should follow two conventions:</p> <ul> <li>Schema/versioning: event payloads and observation values include <code>_schema</code> and <code>_v</code> when the shape matters.</li> <li>Merge expectations: shared keys are append-only lists; owned keys are written only by the hub on behalf of the owning Purpose.</li> </ul>"},{"location":"historical/ttt_architecture_v0_14/#3-core-protocols","title":"3. Core Protocols","text":""},{"location":"historical/ttt_architecture_v0_14/#31-turntakerprotocol","title":"3.1 TurnTakerProtocol","text":"<p>The fundamental interface. Everything that participates in the event stream implements it: Purposes, TTT itself, and calling code.</p> <pre><code>class TurnTakerProtocol(Protocol):\n    async def take_turn(self, event: HubEvent) -&gt; None: ...\n</code></pre> <p><code>take_turn</code> accepts a <code>HubEvent</code> and returns nothing. Participants that want to send results or feedback back to TTT call <code>ttt.take_turn(event)</code> directly. The interface is uniform across all participants \u2014 no special return types, no polymorphic signatures.</p>"},{"location":"historical/ttt_architecture_v0_14/#32-purposeprotocol","title":"3.2 PurposeProtocol","text":"<p>Extends <code>TurnTakerProtocol</code>. Purpose metadata is declared as class attributes and communicated to TTT via the <code>register</code> event.</p> <pre><code>class PurposeProtocol(TurnTakerProtocol, Protocol):\n    purpose_id: str      # stable machine identifier; becomes namespace key\n    purpose_name: str    # human-readable label for logging and audit views\n\n    async def take_turn(self, event: HubEvent) -&gt; None: ...\n</code></pre> <p>Registration metadata (<code>private</code>, <code>depends_on</code>, <code>subscriptions</code>) is carried in the <code>register</code> event payload. This allows the same Purpose implementation to be registered with different configurations in different sessions.</p>"},{"location":"historical/ttt_architecture_v0_14/#33-storeprotocol","title":"3.3 StoreProtocol","text":"<pre><code>class StoreProtocol(Protocol):\n    async def write(self, event: HubEvent) -&gt; None: ...\n    async def flush(self) -&gt; None: ...\n    async def query(self, **kwargs) -&gt; list[dict]: ...  # optional\n</code></pre>"},{"location":"historical/ttt_architecture_v0_14/#4-event-model","title":"4. Event Model","text":"<p>TTT/Adjacency now treats the hub-emitted event stream as the authoritative record of turn processing. Events are emitted on each hub state transition and are persisted by downstream stores (e.g., TurnReTurn backends).</p>"},{"location":"historical/ttt_architecture_v0_14/#41-hubevent-authoritative-event-record","title":"4.1 HubEvent (authoritative event record)","text":"<pre><code>@dataclass\nclass HubEvent:\n    event_id:   str            # 24-char hex (secrets.token_hex(12))\n    event_type: EventType      # Enum (pipeline + annotation)\n    turn_id:   str | None     # CTO turn_id when event is turn-scoped\n    payload:    dict[str, Any] # event-specific; convention: includes _schema and _v\n    timestamp:  float          # unix epoch\n</code></pre> <p>Token terminology: - <code>turn_id</code> identifies a single CTO (per turn). - <code>purpose_token</code> identifies a registered Purpose run (may span many turns/sessions).</p> <p>Events are intentionally small and JSON-safe (or trivially serializable via <code>to_dict()</code> on embedded objects). Payloads should follow the <code>_schema</code> + <code>_v</code> convention for future migration/debugging.</p> <p>Pipeline (DAG) lifecycle:</p> <ul> <li><code>turn_received</code></li> <li><code>purpose_registration</code></li> <li><code>subscriptions_changed</code></li> <li><code>purpose_fired</code></li> <li><code>delta_merged</code></li> <li><code>purpose_complete</code></li> </ul> <p>Out-of-band annotation:</p> <ul> <li><code>turn_observation_recorded</code></li> </ul>"},{"location":"historical/ttt_architecture_v0_14/#43-provider-registration-and-subscriptions-hub-managed","title":"4.3 Provider registration and subscriptions (hub-managed)","text":"<p>Although the HubEvent stream remains the authoritative record, TTT supports a provider registration layer to make the system extensible and safe across process boundaries.</p>"},{"location":"historical/ttt_architecture_v0_14/#431-tttregister","title":"4.3.1 TTT.register()","text":"<p>A Purpose registers itself with the hub by providing:</p> <ul> <li><code>purpose_id: str</code> (stable identifier; used as the purpose namespace)</li> <li><code>subscriptions: list[SubscriptionLike]</code> (event patterns; may include globs, e.g., <code>Events.*</code>)</li> <li>optional <code>capabilities</code> (e.g., whether it can emit out-of-band observations)</li> </ul> <p>The hub returns a purpose token:</p> <ul> <li><code>purpose_token: str</code> \u2014 24-char hex (<code>secrets.token_hex(12)</code>)</li> </ul> <p>The purpose token is then attached to any purpose-originating calls back into the hub (e.g., posting a Delta, requesting subscription changes). This is an identity and trust-boundary mechanism; it is not intended to be cryptographic security in hostile settings.</p>"},{"location":"historical/ttt_architecture_v0_14/#432-subscription-patterns","title":"4.3.2 Subscription patterns","text":"<p>Subscriptions are matched against <code>HubEvent.event_type</code> using simple glob semantics.</p> <p>Canonical contract (routing vs filtering): - Route by <code>event_type</code> only. - Filter optionally by attributes (e.g., <code>turn_id</code>, <code>purpose_id</code>). - Turn scoping uses <code>turn_id</code> only. - Subscription entries MAY be either a string pattern or an object with <code>event_type</code> + optional filters.</p> <p>Examples: - <code>turn_received</code> - <code>delta_merged</code> - <code>Events.*</code> (all events) - <code>turn_observation_recorded</code></p> <p>Turn scoping is expressed via attribute filtering on <code>HubEvent.turn_id</code> (and optionally other well-known attributes), not by encoding a turn identifier into the <code>event_type</code> pattern. Do not rely on raw text matching.</p>"},{"location":"historical/ttt_architecture_v0_14/#433-changing-subscriptions-at-runtime","title":"4.3.3 Changing subscriptions at runtime","text":"<p>TTT supports dynamic subscription updates via a dedicated hub call:</p> <ul> <li><code>TTT.take_turn(ChangeSubscriptionsEvent(...))</code></li> </ul> <p>Payload shape (conceptual): - <code>purpose_id: str</code> - <code>purpose_token: str</code> - <code>delete_subs: list[str]</code> (optional) - <code>add_subs: list[str]</code> (optional)</p> <p>This call is only accepted if <code>purpose_token</code> matches the currently registered token for <code>purpose_id</code>. The hub applies the update atomically for that provider.</p> <p>Note: the strings inside <code>add_subs</code> / <code>delete_subs</code> are subscription patterns (e.g., <code>Events.*</code>), not turn identifiers. If you need turn scoping, the hub should filter/route based on the event\u2019s <code>turn_id</code> or a separate explicit filter mechanism.</p>"},{"location":"historical/ttt_architecture_v0_14/#434-purpose-identity-in-events","title":"4.3.4 Purpose identity in events","text":"<p>Purpose identity is carried as the purpose namespace (<code>purpose_id</code>) inside: - <code>Delta.purpose_id</code> - <code>Observation.owner</code> - and, when needed, in <code>HubEvent.payload[\"purpose_id\"]</code></p> <p>The purpose token is not intended to be written into every HubEvent payload; it is used to authenticate purpose-originating requests to the hub.</p>"},{"location":"historical/ttt_architecture_v0_14/#5-data-model","title":"5. Data Model","text":""},{"location":"historical/ttt_architecture_v0_14/#51-cto-canonical-turn-object","title":"5.1 CTO \u2014 Canonical Turn Object","text":"<p>The CTO is the stable per-turn state container. It has a minimal identity surface plus accumulating metadata.</p> <pre><code>@dataclass\nclass CTO:\n    turn_id:     str                      # 24-char hex; generated by hub per turn\n    timestamp:   float                    # unix epoch; hub sets if None\n    text:        str                      # raw turn text (verbatim)\n    role:        str                      # participant role label\n    observations: dict[str, list[Observation]] = field(default_factory=dict)\n</code></pre> <p>Semantics: - <code>cto.observations</code> is append-only. Every key maps to a list; the hub appends new <code>Observation</code> entries. - Provenance is reconstructed from the HubEvent stream.</p>"},{"location":"historical/ttt_architecture_v0_14/#52-observation-attributable-annotation-unit","title":"5.2 Observation \u2014 attributable annotation unit","text":"<pre><code>@dataclass\nclass Observation:\n    owner:  str   # purpose namespace (e.g. \"ue_detector\")\n    shared: bool  # True =&gt; readable by other providers; hub enforces immutability\n    value:  Any   # JSON-safe provider-defined content\n</code></pre> <p>Semantics: - Shared observations are write-once after first creation (hub enforces). - Owned/private observations may be updated by the owning provider only (hub enforces). - Provenance is still primarily a query over the event stream; the <code>owner/shared</code> fields support correctness and enforcement at write time.</p>"},{"location":"historical/ttt_architecture_v0_14/#53-delta-provider-mutation-record","title":"5.3 Delta \u2014 provider mutation record","text":"<p>Providers report work via a Delta, which the hub can merge into the CTO and record in the event stream.</p> <pre><code>@dataclass\nclass Delta:\n    source_turn_id: str          # CTO turn_id the delta was derived from\n    purpose_id:      str         # purpose namespace that produced this delta\n    invocation_id:   str | None  # hub-generated per dispatch (optional)\n    kind:            str         # purpose-defined (e.g. \"observation_added\")\n    payload:         dict[str,Any]# JSON-safe; convention: includes _schema and _v\n    timestamp:       float       # unix epoch at delta creation\n</code></pre> <p>Delta merge policy is implemented by the hub (the only component that may mutate <code>cto.observations</code>). The hub applies deterministic rules when merging incoming Deltas:</p> <p>-- Owned writes: when a delta carries an Observation intended for an owned   namespace the hub records the contribution on behalf of the owning purpose   following append\u2011only semantics. The hub never silently overwrites an   existing contribution; it records the new contribution and preserves history   for provenance. -- Shared appends: when a delta targets a shared namespace the hub appends   the contribution to the list maintained at that key. The hub enforces the   invariant that shared keys always map to lists and that all contributions   are appended rather than replacing prior entries. -- Merge semantics: the hub applies deterministic, documented merge behavior   (append-only by default). For structured payloads the hub may use explicit   merge functions, but these are always implemented to preserve event history   rather than discarding prior contributions. Higher\u2011level reconciliation and   domain-specific resolution remain the responsibility of downstream   components.</p> <p>These policies keep the core small and deterministic while enabling varied downstream strategies for reconciliation and enrichment.</p>"},{"location":"historical/ttt_architecture_v0_14/#54-dagnode-status-machine-eligibility","title":"5.4 DAGNode status machine (eligibility)","text":"<p>DAG nodes follow an explicit state machine:</p> <p><code>waiting \u2192 eligible \u2192 dispatched \u2192 resolved</code></p> <p>Eligibility checks must reflect <code>status == eligible</code> (hub-driven transitions).</p>"},{"location":"historical/ttt_architecture_v0_14/#6-ttt-internals","title":"6. TTT Internals","text":""},{"location":"historical/ttt_architecture_v0_14/#61-subscription-model-switch-with-wildcard-support","title":"6.1 Subscription model \u2014 Switch with wildcard support","text":"<p>(See Canonical contract (routing vs filtering) in \u00a74.3.2.)</p> <p>TTT is a switch: subscribers declare which event types and/or purpose_ids they care about at registration time. TTT only forwards events to subscribers whose declared interests match.</p> <p>Wildcard subscriptions: <code>\"*\"</code> matches everything. <code>\"turn_*\"</code> matches all turn lifecycle events. <code>\"purpose_*\"</code> matches all Purpose events.</p> <p>TRTPurpose declares <code>subscriptions: [\"*\"]</code> \u2014 it receives all events regardless of type or purpose_id.</p> <p><code>private=True</code> means TTT does not forward <code>purpose_resolved</code> to other subscribers. TRTPurpose still receives it regardless of privacy setting.</p> <p>TTT assumes the hub is the authority for lifecycle events and state mutation. Providers/Purposes are extensions that can compute and propose changes (Deltas) but do not directly mutate shared state without hub mediation.</p> <ul> <li>Providers register via <code>TTT.register(purpose_id, subscriptions)</code> and receive a <code>purpose_token</code> (<code>secrets.token_hex(12)</code>).</li> <li>Any purpose-originating request (posting a Delta, requesting subscription changes) must include <code>purpose_id</code> + <code>purpose_token</code>.</li> <li>The hub validates the token against its registry before accepting the request.</li> </ul> <p>This mechanism is primarily for correctness and accidental misuse prevention (e.g., preventing a misconfigured provider from impersonating another). If you need a hostile-environment security model, treat this as scaffolding and add cryptographic authentication at the transport layer.</p>"},{"location":"historical/ttt_architecture_v0_14/#63-dag-execution","title":"6.3 DAG execution","text":"<p>Purposes declare <code>depends_on: list[str]</code> in their <code>register</code> payload. TTT maintains a per-turn resolution graph and dispatches Purposes as dependencies are satisfied.</p> <pre><code>Turn arrives \u2192\n    embed (depends_on=[])             \u2192 dispatched immediately\n    ca    (depends_on=[])             \u2192 dispatched immediately\n    tm    (depends_on=[\"embed\",\"ca\"]) \u2192 waiting\n\nembed resolves \u2192 tm still waiting for ca\nca resolves    \u2192 tm now eligible \u2192 dispatched\ntm resolves    \u2192 DAG exhausted \u2192 turn_processing_complete\n</code></pre> <p>Independent Purposes run concurrently via <code>asyncio.gather</code>. Dependent Purposes await their prerequisites.</p>"},{"location":"historical/ttt_architecture_v0_14/#64-utilities-tttutils","title":"6.4 Utilities \u2014 <code>ttt.utils</code>","text":"<pre><code># Event stream reconstruction\ndef apply_delta(cto: CTO, delta: Delta, purpose_id: str) -&gt; CTO: ...\ndef reconstruct(turn_id: str, events: list[HubEvent]) -&gt; CTO: ...\n\n# Shared namespace resolution\ndef resolve_shared(\n    namespace: str,\n    events: list[HubEvent],\n    resolver: Callable[[list], Any]\n) -&gt; Any: ...\n\n# Provenance formatting\ndef format_provenance(event: HubEvent) -&gt; dict: ...\n# Returns a flat, serializable dict of provenance-relevant fields\n# for a single event. Callers serialize to JSON or log string as needed.\n# Example output:\n# {\n#   \"event_type\": \"purpose_resolved\",\n#   \"event_id\":   \"a3f7...\",\n#   \"timestamp\":  1234567890.123,\n#   \"turn_id\":    \"t_001\",\n#   \"purpose_id\": \"ca\",\n#   \"purpose_name\": \"Conversation Analysis Annotator\",\n#   \"entries_added\": 2,\n#   \"shared_keys_updated\": []\n# }\n</code></pre> <p><code>format_provenance</code> takes one event and returns a flat dict. It knows which fields are meaningful for provenance across event types and normalizes them into a consistent shape. Backends call it when writing log entries. Calling code can call it directly.</p>"},{"location":"historical/ttt_architecture_v0_14/#7-turnreturn-and-trtpurpose","title":"7. TurnReTurn and TRTPurpose","text":"<p>TurnReTurn is the persistence, retrieval, formatting, and output product. It is the brand and the system.</p> <p>TRTPurpose is TurnReTurn's TTT integration point \u2014 a <code>PurposeProtocol</code> implementation that plugs into the DAG and feeds TurnReTurn's Stores. Someone who wants TurnReTurn's persistence features but has their own event infrastructure can use TurnReTurn without TRTPurpose.</p> <pre><code>class TRTPurpose:\n    purpose_id = \"trt\"\n    purpose_name = \"TurnReTurn Persistence\"\n    # registered with: private=True, depends_on=[], subscriptions=[\"*\"]\n\n    def register_store(self, store: StoreProtocol) -&gt; None: ...\n    async def take_turn(self, event: HubEvent) -&gt; None: ...\n</code></pre> <p>TRTPurpose is also the host for query views \u2014 since it holds the durable event stream, provenance, session replay, and Purpose audit are naturally expressed as queries against it.</p> <pre><code># Query views (available when a queryable Store is registered)\ndef provenance(self, turn_id: str) -&gt; list[dict]: ...\ndef session_replay(self, session_id: str) -&gt; list[HubEvent]: ...\ndef purpose_audit(self, session_id: str) -&gt; list[dict]: ...\n</code></pre>"},{"location":"historical/ttt_architecture_v0_14/#71-store-and-backend-layering","title":"7.1 Store and Backend layering","text":"<p>A Store is the logical persistence target. A Backend is the specific driver implementing a Store. This mirrors SQLAlchemy's dialect pattern.</p> <pre><code>TurnReTurn\n    \u2514\u2500\u2500 TRTPurpose\n    \u2514\u2500\u2500 Stores\n          \u251c\u2500\u2500 FileStore\n          \u2502     \u251c\u2500\u2500 JSONLBackend\n          \u2502     \u251c\u2500\u2500 JSONBackend\n          \u2502     \u2514\u2500\u2500 PlainTextBackend\n          \u251c\u2500\u2500 MemoryStore\n          \u2502     \u2514\u2500\u2500 MessageDictBackend\n          \u2514\u2500\u2500 SQLStore\n                \u251c\u2500\u2500 SQLiteBackend\n                \u2514\u2500\u2500 PostgresBackend\n</code></pre> <p>Multiple Stores can be registered simultaneously. TRTPurpose fans out to all of them on each event.</p>"},{"location":"historical/ttt_architecture_v0_14/#8-package-structure","title":"8. Package Structure","text":"<pre><code>ttt/                          # pip install ttt\n    core/\n        cto.py                # CTO, Delta\n        events.py             # HubEvent, well-known event_type constants\n        protocols.py          # TurnTakerProtocol, PurposeProtocol,\n                              # StoreProtocol\n        hub.py                # TTT hub: DAG resolver, switch, token auth\n        utils.py              # apply_delta, reconstruct, resolve_shared,\n                              # format_provenance\n\ntrt/                          # pip install ttt[persist]\n    purpose.py                # TRTPurpose\n    stores/\n        file_store.py         # FileStore\n        memory_store.py       # MemoryStore\n        backends/\n            jsonl.py          # JSONLBackend (under FileStore)\n            json_.py          # JSONBackend (under FileStore)\n            plaintext.py      # PlainTextBackend (under FileStore)\n            messagedict.py    # MessageDictBackend (under MemoryStore)\n\ntrt_sql/                      # pip install ttt[persist-sql]\n    stores/\n        sql_store.py          # SQLStore\n        backends/\n            sqlite.py         # SQLiteBackend\n            postgres.py       # PostgresBackend\n</code></pre>"},{"location":"historical/ttt_architecture_v0_14/#9-example-integration-adjacency-as-one-client","title":"9. Example Integration (Adjacency as one client)","text":"<p>Adjacency is one consumer of TTT, but it is not architecturally special. In TTT terms:</p> <ul> <li>The Adjacency session runner can be modeled as a Purpose that emits <code>turn_observed</code> events via <code>take_turn</code>.</li> <li>Adjacency-specific analyzers are simply additional Purposes that subscribe to relevant events and propose observations.</li> <li>Feedback loops (e.g., a <code>speaker_feedback</code> event) are implemented as subscriptions: calling code receives feedback the same way any other Purpose does.</li> </ul> <p>Adjacency\u2019s existing session log (JSONL) becomes a TurnReTurn FileStore output rather than a separately managed artifact. The log format can be preserved; the machinery producing it is replaced by TRTPurpose + JSONLBackend.</p>"},{"location":"historical/ttt_architecture_v0_14/#10-installation","title":"10. Installation","text":"<pre><code>pip install ttt                  # core only \u2014 no storage dependencies\npip install ttt[persist]         # + TurnReTurn with FileStore, MemoryStore\npip install ttt[persist-sql]     # + SQLStore with SQLite and Postgres backends\n</code></pre>"},{"location":"historical/ttt_architecture_v0_14/#11-what-ttt-is-not","title":"11. What TTT Is Not","text":"<ul> <li>Not an API gateway or HTTP proxy</li> <li>Not an LLM alignment system</li> <li>Not an observability platform (though it can feed one via a Store)</li> <li>Not a replacement for OpenTelemetry, Langfuse, or Phoenix \u2014 it can emit   to any of these via a Store or subscriber</li> <li>Not an actor framework \u2014 it uses actor model concepts implemented directly   on asyncio</li> </ul>"},{"location":"historical/ttt_architecture_v0_14/#appendix-a-reference-material-from-earlier-document","title":"Appendix A \u2014 Reference Material from Earlier Document","text":"<p>The following sections from Alignment Middleware for Human\u2013LLM Dialogue (March 2026) remain useful as reference for Purpose implementations that would run on top of TTT. They are preserved here without modification.</p>"},{"location":"historical/ttt_architecture_v0_14/#a1-trace-candidate-schema","title":"A.1 Trace Candidate Schema","text":"<p>Relevant to a TraceMutation Purpose implementation.</p> <pre><code>trace_candidate:\n  trace_id: ...\n  span: {message_id, char_start, char_end}\n  type: [decision|constraint|definition|plan|ownership|other]\n  owner: [user|model|joint|unknown]\n  text: ...\n  normalized: ...\n  timestamp: ...\n  confidence: 0..1\n</code></pre>"},{"location":"historical/ttt_architecture_v0_14/#a2-alignment-signals-for-an-alignment-purpose","title":"A.2 Alignment Signals (for an Alignment Purpose)","text":"<ol> <li>User-constraint preservation score \u2014 compare embeddings of current    model response vs nearest constraint traces; low similarity + high    confidence restatement =&gt; risk.</li> <li>Negation risk flag (UE-leaning) \u2014 detect negation operators in user    traces; if model restates without negation or with polarity flip, raise    risk.</li> <li>Provenance compression score (GD-leaning) \u2014 track pronoun/possessive    shifts around accountability moments (\"we/our\" \u2192 \"you/your\").</li> <li>Commitment continuity score \u2014 compare \"what we decided\" summaries    across time; detect discontinuous jumps.</li> </ol>"},{"location":"historical/ttt_architecture_v0_14/#a3-mutation-candidate-event-schema-for-a-tracemutation-purpose","title":"A.3 Mutation Candidate Event Schema (for a TraceMutation Purpose)","text":"<pre><code>mutation_candidate:\n  kind: [UE|GD|unknown]\n  source_trace_id: ...\n  degenerate_trace_span: ...\n  evidence:\n    - similarity_drop: ...\n    - contradiction_score: ...\n    - provenance_shift: ...\n  severity: [low|med|high]\n  proposed_action: ...\n</code></pre>"},{"location":"historical/ttt_architecture_v0_14/#a4-decisioncard-provenance-accumulator","title":"A.4 DecisionCard / Provenance Accumulator","text":"<p>A DecisionCard Purpose would separate (1) append-only provenance from (2) replaceable resolved snapshots, supporting deterministic re-resolution when evidence evolves.</p> <p>Key design invariants: - CTO is verbatim-first and immutable (enforced by TTT core) - Provenance and annotations are append-only and span-referenced - \"Current state\" is always a materialized view with explicit <code>resolver_id</code>   and <code>evidence_bundle_id</code>, so it can be re-resolved deterministically</p>"},{"location":"historical/ttt_architecture_v0_14/#a5-alignment-snapshot-contents-for-an-alignment-purpose","title":"A.5 Alignment Snapshot Contents (for an Alignment Purpose)","text":"<ul> <li><code>snapshot_id</code>, <code>turn_id</code>, <code>decision_ids</code> touched</li> <li><code>anchor_trace_ids</code> (constraints/definitions/commitments)</li> <li><code>alignment_scores</code> (per-signal + composite)</li> <li><code>state_summary_text</code> (compact) + <code>state_summary_embedding</code></li> <li><code>recent_drift_delta</code> (change from last snapshot)</li> </ul>"},{"location":"historical/ttt_architecture_v0_14/#a6-llmz-guidance-packet-for-a-guidance-purpose","title":"A.6 LLMZ Guidance Packet (for a Guidance Purpose)","text":"<pre><code>[LLMZ]\nGOAL: preserve shared-record integrity; avoid unilateral restatement of\n      user constraints.\nANCHORS:\n- (trace_id=...) \"constraint text\" [MUST-PRESERVE]\nCHECKS:\n- Before proposing plan, restate anchored clauses verbatim.\n- Do not negate deferred commitments.\nREPAIR:\n- If conflict detected, ask one clarification Q; otherwise silently align.\n[/LLMZ]\n</code></pre>"},{"location":"historical/ttt_architecture_v0_14/#a7-integration-roadmap-adapted","title":"A.7 Integration Roadmap (adapted)","text":"<p>Phase 1 \u2014 Working pipe - TTT core + TRTPurpose + JSONLBackend - Adjacency session runner emitting <code>turn_observed</code> events - DAG execution with zero-dependency Purposes</p> <p>Phase 2 \u2014 Trace-aware - TraceMutation Purpose (heuristic first pass) - Embedding Purpose (SBERT + FAISS in-process) - CA annotation Purpose - DecisionCard Purpose (append-only evidence accumulation) - Shared namespace usage for open CA schema contributions</p> <p>Phase 3 \u2014 Guided - Alignment signal Purpose - <code>speaker_feedback</code> \u2192 Adjacency session runner loop - LLMZ packet generation Purpose - Offline replay and heavier analysis passes - Resolver Purposes for shared namespace canonicalization</p> <ul> <li><code>ttt.register()</code> as convenience wrapper clarified \u2014 <code>take_turn</code> is the one true path; <code>register()</code> constructs and dispatches internally</li> <li><code>subscription_update</code> event added for mid-session subscription changes</li> <li><code>purpose_id</code> removed from <code>purpose_complete</code> payload \u2014 resolved from token by TTT; prevents spoofing</li> <li><code>format_provenance(event) -&gt; dict</code> added to <code>ttt.utils</code>; per-event, flat, serializable, caller serializes to JSON or log string</li> <li><code>provenance()</code> list traversal utility removed from <code>ttt.utils</code> \u2014 provenance is a TurnReTurn query view, not a core utility</li> <li>Design principles updated: <code>take_turn</code> is the one true path</li> </ul>"},{"location":"historical/ttt_architecture_v0_14/#addendum-e-postgres-substrate-authoritative-log-projections","title":"Addendum E \u2014 Postgres Substrate (Authoritative Log + Projections)","text":"<p>This addendum specifies a minimal Postgres-first persistence layout consistent with the core invariants:</p> <ul> <li>Append-only authoritative record (no overwrites; replayable)</li> <li>Purpose-run tokens (<code>purpose_token</code>) are run-scoped and can span many turns/sessions</li> <li>CTO is per-turn and identified by <code>(conversation_id, turn_index)</code> (or an optional <code>turn_id</code>)</li> <li>Extensibility is achieved with JSONB payloads + selective indexing (no schema churn)</li> </ul>"},{"location":"historical/ttt_architecture_v0_14/#e1-identity-model","title":"E.1 Identity model","text":""},{"location":"historical/ttt_architecture_v0_14/#e11-conversation-turn-identity-recommended","title":"E.1.1 Conversation + Turn identity (recommended)","text":"<ul> <li><code>conversation_id</code> \u2014 UUID (or 64\u201396-bit hex string) identifying a conversation/session grouping</li> <li><code>turn_index</code> \u2014 integer, monotonically increasing within <code>conversation_id</code></li> </ul> <p>Primary key: - <code>(conversation_id, turn_index)</code></p> <p>Optional convenience: - <code>turn_uid</code> computed as <code>conversation_id || ':' || turn_index</code></p>"},{"location":"historical/ttt_architecture_v0_14/#e12-purpose-run-identity","title":"E.1.2 Purpose-run identity","text":"<ul> <li><code>purpose_id</code> \u2014 stable identifier for the Purpose type (e.g., <code>\"CA\"</code>, <code>\"embeddingizer\"</code>, <code>\"TRTPurpose\"</code>)</li> <li><code>purpose_token</code> \u2014 24-char hex string returned by <code>TTT.register()</code> and used to authenticate Purpose-originating requests</li> </ul> <p>A single <code>purpose_token</code> may cover: - many turns, - and multiple conversation sessions, depending on how you scope \u201crun.\u201d</p>"},{"location":"historical/ttt_architecture_v0_14/#e2-tables-minimal-viable","title":"E.2 Tables (minimal viable)","text":""},{"location":"historical/ttt_architecture_v0_14/#e21-hub_events-authoritative-append-only-log","title":"E.2.1 <code>hub_events</code> \u2014 authoritative append-only log","text":"<p>The event stream is the source of truth. Everything else can be derived.</p> <pre><code>create table hub_events (\n  event_id        bigserial primary key,\n  event_time      timestamptz not null default now(),\n\n  -- Routing / grouping\n  conversation_id uuid null,\n  turn_index      int null,\n  purpose_id      text null,\n\n  -- Core event type (enum or text)\n  event_type      text not null,\n\n  -- JSONB payload for event-specific content\n  payload         jsonb not null,\n\n  -- Integrity\n  payload_hash    bytea null\n);\n</code></pre> <p>Notes: - <code>conversation_id</code>/<code>turn_index</code> are nullable to support non-turn-scoped events (e.g., <code>purpose_registration</code>, <code>subscriptions_changed</code>). - <code>payload</code> is JSONB and should include <code>_schema</code> and <code>_v</code> when the event\u2019s internal structure matters. - You can add <code>purpose_token</code> to <code>payload</code> for requests, but do not store it on accepted events unless you need an audit trail; typically you store only <code>purpose_id</code> and keep <code>purpose_token</code> in the registry table.</p>"},{"location":"historical/ttt_architecture_v0_14/#e22-ctos-per-turn-state-snapshot-optional-but-practical","title":"E.2.2 <code>ctos</code> \u2014 per-turn state snapshot (optional but practical)","text":"<p>This is a materialized convenience table. It is allowed to be \u201clatest view\u201d because the authoritative record is still <code>hub_events</code>.</p> <pre><code>create table ctos (\n  conversation_id uuid not null,\n  turn_index      int not null,\n\n  -- stable per-turn fields\n  created_at      timestamptz not null default now(),\n  speaker_role    text not null,         -- \"human\" | \"model\" | \"system\" (or richer labels)\n  text            text not null,         -- raw turn text (verbatim)\n  text_hash       bytea null,\n\n  -- observations are append-only lists per key\n  observations    jsonb not null default '{}'::jsonb,\n\n  primary key (conversation_id, turn_index)\n);\n</code></pre> <p>Observations encoding: - <code>observations</code> is a JSON object keyed by observation key; each value is a JSON array of Observation objects:   - <code>[{ \"owner\": \"...\", \"shared\": true, \"value\": ... }, ...]</code></p> <p>This matches the \u201cevery value is a list; hub appends\u201d invariant.</p>"},{"location":"historical/ttt_architecture_v0_14/#e23-purpose_runs-registry-for-purpose-tokens-and-subscriptions","title":"E.2.3 <code>purpose_runs</code> \u2014 registry for Purpose tokens and subscriptions","text":"<pre><code>create table purpose_runs (\n  purpose_id      text not null,\n  purpose_token   char(24) not null,\n\n  -- run scope / lifecycle\n  created_at      timestamptz not null default now(),\n  last_seen_at    timestamptz null,\n  is_active       boolean not null default true,\n\n  -- subscriptions expressed as patterns; stored as jsonb array of strings\n  subscriptions   jsonb not null default '[]'::jsonb,\n\n  primary key (purpose_id, purpose_token)\n);\n</code></pre> <p>Notes: - This table is the trust boundary: hub validates <code>(purpose_id, purpose_token)</code> for Purpose-originating requests. - If you later want \u201cone active token per purpose_id,\u201d enforce via a partial unique index on <code>purpose_id where is_active</code>.</p>"},{"location":"historical/ttt_architecture_v0_14/#e3-indexes-day-1","title":"E.3 Indexes (day 1)","text":"<p>For replay and common queries:</p> <pre><code>create index hub_events_conv_turn_time\n  on hub_events (conversation_id, turn_index, event_time);\n\ncreate index hub_events_type_time\n  on hub_events (event_type, event_time);\n\n-- If you frequently query by purpose_id (e.g., debugging a Purpose)\ncreate index hub_events_purpose_time\n  on hub_events (purpose_id, event_time);\n</code></pre> <p>For JSONB filtering (only add when needed):</p> <pre><code>-- Example: find events whose payload contains a key/value pattern\ncreate index hub_events_payload_gin\n  on hub_events using gin (payload);\n</code></pre> <p>For CTO retrieval:</p> <pre><code>create index ctos_conv_time\n  on ctos (conversation_id, created_at);\n</code></pre>"},{"location":"historical/ttt_architecture_v0_14/#e4-replay-model","title":"E.4 Replay model","text":"<p>Replay is a query over <code>hub_events</code>:</p> <ul> <li>\u201cReplay a conversation\u201d: <code>where conversation_id = ... order by turn_index, event_time, event_id</code></li> <li>\u201cReplay a turn\u201d: <code>where conversation_id = ... and turn_index = ... order by event_time, event_id</code></li> </ul> <p>Because the authoritative stream is append-only, strict ordering requirements are minimal. Recommended tie-break: - <code>event_time</code>, then <code>event_id</code> (monotone).</p>"},{"location":"historical/ttt_architecture_v0_14/#e5-projections-derived-consumers","title":"E.5 Projections (derived consumers)","text":"<p>Treat everything beyond the core as derived projections:</p> <ul> <li>DecisionCard materialization (from DecisionMove events)</li> <li>Alignment snapshots</li> <li>Trace-probe / adjacency annotations</li> <li>Search indices</li> </ul> <p>Each projection: - reads from <code>hub_events</code> (and optionally <code>ctos</code>), - writes to its own tables/materialized views, - can be re-run as rules evolve.</p>"},{"location":"historical/ttt_architecture_v0_14/#e6-when-to-add-a-vector-index-optional","title":"E.6 When to add a vector index (optional)","text":"<p>When you need semantic retrieval at scale: - store embeddings out-of-row (vector DB or Postgres pgvector), - store only embedding references in event payloads and/or CTO observations.</p> <p>If you want to stay Postgres-only, pgvector is a reasonable next step; otherwise keep the vector index as a separate projection.</p>"}]}